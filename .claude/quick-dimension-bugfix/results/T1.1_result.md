# T1.1 — Confirm source owners and no-touch boundaries

- status: PASS
- date: 2026-08-02
- inputs_read: `.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`; `.claude/quick-dimension-bugfix/tasks/T1.1_confirm_source_owners.md`; `.claude/quick-dimension-bugfix/03_TASK_MANIFEST.md`; `.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`; `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs` (targeted ranges only); `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs` (targeted ranges only); `ArcTool.Core/Services/QuickDimensionChainCreationService.cs:17-151`; `ArcTool.Core/Models/QuickDimensionContract.cs:40-66,429-477`
- write_scope_touched: `.claude/quick-dimension-bugfix/results/T1.1_result.md`

## Findings

### 1) BUG-11 owner is one file: `QuickDimensionDoorWindowCandidateCollector.cs`
- Main wall-axis flow owner confirmed at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:206-342`.
  - `CollectWallAxisOpeningSource` is at `206-235` and enumerates wall-hosted FamilyInstances, then routes each instance into `TryProjectOpeningOntoWallAxis` at `231`.
  - `TryProjectOpeningOntoWallAxis` is at `237-342` and is the production wall-axis projection path. It gathers family refs, fallback refs, estimated points, then materializes `QuickDimensionCandidate` rows.
- Cross-association defect site confirmed at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:573-611`.

Quoted defect site:
```csharp
if (familyReference != null)
{
    referenceInfos.Add(new OpeningReferenceInfo(
        familyReference,
        fallbackPoint ?? estimatedPoint,
        label,
        QuickDimensionReferenceStrategy.FamilyInstanceLeftRight));
    return;
}
```
- This is the exact BUG-11 ownership point because the family-semantic `Reference` is paired with a point that can come from `fallbackPoint` or `estimatedPoint`, i.e. from a different derivation path than the named reference itself.
- Supporting symbol boundaries in the same file are confirmed:
  - `TryGetFamilyInstanceReferences`: `658-690` (exact). It preserves `GetReferences(Left)[0]` / `GetReferences(Right)[0]` selection and is therefore boundary-locked by R4, not a redesign target.
  - `TryGetHostWallOpeningReferences`: actual `692-822` (source map overstates this as `692-~888`). This symbol owns fallback pair selection + fallback points.
  - `TryGetEstimatedReferencePoints`: `944-982` (exact). This symbol owns bbox-estimated left/right points.
  - `OpeningReferencePair`: actual class span `1228-1250` (source map lists constructor/core range `1232-1244`).
  - `OpeningReferenceInfo`: actual class span `1286-1304` (source map lists constructor/core range `1286-1298`).

### 2) BUG-10 owner is the same collector, and the fix is metadata-only
- Candidate metadata is stamped in `TryProjectOpeningOntoWallAxis` when `QuickDimensionCandidate` is created at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:316-320`:
  - `elementId = instance.Id`
  - `hostElementId = hostWall.Id`
  - `reference = referenceInfo.Reference`
  - `referenceStrategy = referenceInfo.ReferenceStrategy`
- `QuickDimensionCandidate` already carries the exact metadata fields needed for BUG-10 at `ArcTool.Core/Models/QuickDimensionContract.cs:431-474`: `ElementId`, `Reference`, `ReferenceStrategy`, `ParameterOnDimensionLine`, `HostElementId`, `FamilyName`, `TypeName`.
- Attachment does not depend on `ElementId`. `QuickDimensionChainCreationService` builds the `ReferenceArray` from `candidate.Reference` only at `ArcTool.Core/Services/QuickDimensionChainCreationService.cs:75-79`.

Quoted attachment proof:
```csharp
ReferenceArray references = new ReferenceArray();
foreach (QuickDimensionCandidate candidate in result.Candidates)
{
    references.Append(candidate.Reference);
}
```
- Result: BUG-10 ownership stays in `QuickDimensionDoorWindowCandidateCollector.cs`; the bug is metadata-only because the model already holds both ids and chain creation ignores `candidate.ElementId` for attachment.
- No source conflict found with fallback geometry selection: `TryGetHostWallOpeningReferences` (`692-822`) owns fallback refs/points; BUG-10 must not alter that symbol’s geometry logic.

### 3) `actualSegmentCount` and future `valueSource` belong to `QuickDimensionReadOnlyXmlLogService.cs`
- Audit assembly owner confirmed in `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs:453-547`.
- `actualSegmentCount` defect is owned here because the file computes `reportedSegmentCount = dimension?.NumberOfSegments ?? 0` at `507`, then writes it into the top-level audit at `526` and into `<Segments>` at `698`.
- `BuildSegmentsAuditElement` is the only segment-audit emitter, at `665-699`. It currently writes `actualValueMm`, `expectedDeltaMm`, `differenceMm`, and `matched`, but no `valueSource`; therefore the enhancement owner is this method in this file.
- Strict audit semantics are also in this same file and are explicitly separate from the logging-only fixes:
  - `GetReferenceOrderRelation`: `807-831`
  - `ReferenceOwnersMatch`: actual `833-856` (map slightly overstates to `~860`)
  - `SegmentValuesMatch`: actual `942-975` (map slightly overstates to `~980`)

Quoted count/logging proof:
```csharp
int reportedSegmentCount = dimension?.NumberOfSegments ?? 0;
...
new XAttribute("actualSegmentCount", reportedSegmentCount),
...
new XAttribute("actualSegmentCount", reportedSegmentCount),
```

### 4) `QuickDimensionChainCreationService` is reference-only and not an edit target
- `CreateChainDimension` is confirmed at `ArcTool.Core/Services/QuickDimensionChainCreationService.cs:17-151`.
- This file owns dimension-line resolution, `Transaction`, `NewDimension`, rollback, and post-create reference-count checks.
- It does not own candidate association, fallback metadata, or audit XML assembly.
- The task manifest explicitly locks this boundary: `.claude/quick-dimension-bugfix/03_TASK_MANIFEST.md:79-81` says only the collector and XML log service are edit targets in this package order, and “No task edits `QuickDimensionChainCreationService.cs`.”

### 5) Explicit no-touch list for T1.1 downstream locking
The following areas are outside the allowed fix surface for this bugfix package step and must remain untouched:
1. Wall anchors.
2. Mid-run aggregation.
3. Dedupe / duplicate-station handling.
4. Chain-creation transaction logic in `QuickDimensionChainCreationService.CreateChainDimension`.
5. Strict audit semantics in `QuickDimensionReadOnlyXmlLogService`:
   - accepted order relations remain only `Exact` / complete `Reversed` / `Mismatch` as implemented by `GetReferenceOrderRelation` (`807-831`);
   - owner matching stays sequence-aware in `ReferenceOwnersMatch` (`833-856`);
   - segment tolerance logic stays in `SegmentValuesMatch` (`942-975`).

### 6) Scope-conflict check
- No scope conflict remains.
- `QuickDimensionDoorWindowCandidateCollector.cs` is the sole owner file for BUG-11 and BUG-10.
- `QuickDimensionReadOnlyXmlLogService.cs` is the sole owner file for `actualSegmentCount` and `valueSource` logging work.
- `QuickDimensionChainCreationService.cs` is read-only reference material only.
- `QuickDimensionContract.cs` already exposes the required metadata fields and existing `QuickDimensionReferenceStrategy`; no model expansion is required for T1.1.
- Manifest lock summary confirms serialized edit ownership:
  - collector edits only by `T2.3`, `T4.2`, `T5.1`
  - XML log service edits only by `T5.2`, `T5.3`
  - chain creation file never edited in this package (`03_TASK_MANIFEST.md:77-82`)

## Decision
Source ownership is now precise enough to proceed: BUG-11 and BUG-10 both belong to `QuickDimensionDoorWindowCandidateCollector.cs`, with BUG-11 centered on `TryProjectOpeningOntoWallAxis` + `AddOpeningReferenceInfo` and BUG-10 centered on opening candidate metadata materialization; `actualSegmentCount` and `valueSource` belong only to `QuickDimensionReadOnlyXmlLogService.cs`; `QuickDimensionChainCreationService.cs` is a no-touch reference file.

## Open questions
- None.

## Handoff for downstream tasks
- Use `QuickDimensionDoorWindowCandidateCollector.cs:237-342` and `573-611` as the BUG-11 fix surface; do not redesign `GetReferences(Left/Right)[0]` in `658-690`.
- Use `QuickDimensionDoorWindowCandidateCollector.cs:316-320` plus `QuickDimensionContract.cs:431-474` as the BUG-10 metadata-only surface; do not alter fallback geometry selection in `692-822`.
- Use `QuickDimensionReadOnlyXmlLogService.cs:507,526,665-699,698` as the `actualSegmentCount` / `valueSource` surface; do not relax `GetReferenceOrderRelation`, `ReferenceOwnersMatch`, or `SegmentValuesMatch`.