# T2.3 — Apply instrumentation patch

- status: PASS
- date: 2026-08-03
- inputs_read: `.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`; `.claude/quick-dimension-bugfix/tasks/T2.3_apply_instrumentation_patch.md`; `.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`; `.claude/quick-dimension-bugfix/results/T2.2_result.md`; `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:261-349`; `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:341-396`
- write_scope_touched: `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`, `.claude/quick-dimension-bugfix/results/T2.3_result.md`

## Findings
- Applied PATCH B exactly at the accepted-reference loop inside `TryProjectOpeningOntoWallAxis`; the new instrumentation block now sits between successful projection and `candidates.Add(...)` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:316-329`.
- Applied PATCH A exactly after `TryProjectOpeningOntoWallAxis`; the new helper `DeriveProvenance(...)` now exists at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:359-393`.
- The emitted diagnostic uses the confirmed available severity `QuickDimensionDiagnosticSeverity.Info` and includes the required `[PHASE2_INSTRUMENTATION]` prefix in the message text at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:320-328`.
- The instrumentation logs the required fields without changing production selection/collection behavior:
  - `Reference` identity via `referenceInfo.Reference?.ElementId.Value.ToString() ?? "null"` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:317`
  - `ReferenceOwner` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:324`
  - `ProjectionPoint` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:325`
  - `ProjectedStation` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:326`
  - `PointProvenance` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:327`
  - `Strategy` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:328`
- `DeriveProvenance(...)` matches T2.2’s planned decision table exactly:
  - `HostWallOpeningGeometry` => `Reference-Owned (HostWallOpeningGeometry)` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:369-370`
  - `FamilyInstanceLeftRight` left-side pointer checks against `fallbackReferences.LeftPoint` / `estimatedLeftPoint` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:374-379`
  - right-side pointer checks against `fallbackReferences.RightPoint` / `estimatedRightPoint` at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:381-386`
  - unknown-source / unknown-strategy fallbacks at `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:389-392`
- Candidate construction remains in place immediately after the new diagnostic block, confirming no collection semantics were moved or widened: `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:331-333`.
- No other source file was edited. No changes were made to named-reference index selection (`GetReferences(Left)[0]`, `GetReferences(Right)[0]`), fallback pair selection, wall anchors, mid-run logic, dedupe, chain creation, or audit semantics.

## Decision
The minimal instrumentation patch is now in place and verified in-source. The collector’s runtime behavior is preserved except for one additional `Info` diagnostic per accepted opening reference, plus the local helper used only to classify logged provenance.

## Open questions
None.

## Handoff for downstream tasks
- T2.4 can verify the patch by checking `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:316-329` and `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:359-393`, then compiling with the agreed MSBuild path.
- The expected operator-visible marker in XML/log output is the literal prefix `[PHASE2_INSTRUMENTATION]`.
- Downstream reviewers should treat this as diagnostics-only: named-reference selection, fallback selection, and candidate construction semantics were not changed.
