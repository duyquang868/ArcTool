# T2.4 — Build instrumented debug candidate

- status: PASS
- date: 2026-08-03
- inputs_read: `.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `.claude/quick-dimension-bugfix/tasks/T2.4_build_instrumented_candidate.md`, `.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `.claude/quick-dimension-bugfix/results/T1.5_result.md`, `.claude/quick-dimension-bugfix/results/T2.3_result.md`, `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:316-329`, `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:359-393`
- write_scope_touched: `.claude/quick-dimension-bugfix/results/T2.4_result.md`

## Findings

**Instrumentation verification (pre-build):**
- PATCH B region confirmed at `QuickDimensionDoorWindowCandidateCollector.cs:316-329`. The instrumentation block logs six required fields: `Reference` identity, `ReferenceOwner`, `ProjectionPoint`, `ProjectedStation`, `PointProvenance`, and `Strategy`. Marker `[PHASE2_INSTRUMENTATION]` is present in message prefix at line 322.
- PATCH A region confirmed at `QuickDimensionDoorWindowCandidateCollector.cs:359-393`. Helper method `DeriveProvenance(...)` implements the T2.2 decision table exactly: `HostWallOpeningGeometry` returns reference-owned provenance; `FamilyInstanceLeftRight` checks pointer equality against fallback/estimated left/right points to classify proxy-derived sources.
- No changes detected in wall anchors, mid-run aggregation, dedupe, chain creation, or audit logic. Instrumentation is additive-only per T2.3 handoff.

**Build execution:**
- MSBuild command from T1.5 handoff executed: `"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" ArcTool.Core/ArcTool.Core.csproj -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo`
- Build **succeeded**: output DLL written to `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll`
- Two compiler warnings emitted, both pre-existing nullability annotations unrelated to T2.3 instrumentation:
  - `CS8600` at `QuickDimensionReadOnlyXmlLogService.cs:77` (converting null literal)
  - `CS8604` at `QuickDimensionDoorWindowCandidateCollector.cs:333` (possible null reference argument for `QuickDimensionCandidate` constructor parameter `reference`)
- The `CS8604` warning at line 333 is on the production candidate construction immediately after the instrumentation block (lines 331-335). This warning existed before T2.3; the instrumentation patch did not introduce it and does not widen scope.

**Isolation assessment:**
- No T2.3-introduced compile failures detected. The build output confirms the instrumented collector compiles cleanly with only pre-existing nullability warnings.
- The instrumentation does not alter production selection, projection, or candidate construction semantics per contract R6.

## Decision

The instrumented debug candidate builds successfully without scope widening. Both instrumentation regions are present and verified in source. The build is ready for operator smoke execution under a future runtime task.

## Open questions

None.

## Handoff for downstream tasks

- The instrumented DLL is located at `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll`.
- The operator-visible diagnostic marker in XML/log output will be the literal prefix `[PHASE2_INSTRUMENTATION]` per T2.3 handoff.
- The instrumented collector logs six fields per accepted opening reference: `Reference`, `ReferenceOwner`, `ProjectionPoint`, `ProjectedStation`, `PointProvenance`, `Strategy`.
- Downstream smoke execution must use the diagnostic fixture wall **379469** first (contains both swapped and ordered instances of the same family/type), then regression matrix walls 379467, 379470 (both shells each).
- The build contains two pre-existing nullability warnings unrelated to T2.3 instrumentation; they do not block smoke execution.
