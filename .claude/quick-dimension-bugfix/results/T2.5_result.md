# T2.5 — Operator runbook for wall 379469 (both shells)

- status: PASS
- date: 2026-08-03
- inputs_read: `01_SHARED_CONTRACT.md`, `04_EVIDENCE_QUEUE.md`, `05_RESULT_SCHEMA.md`, `tasks/T2.5_operator_runbook_379469.md`, `results/T2.4_result.md`
- write_scope_touched: `results/T2.5_result.md`

## Findings

**Runbook requirements:**
- Command: `QuickDimensionCreateChainSmokeCommand` (confirmed as the diagnostic smoke command per T2.4 handoff and contract §6)
- Target wall: **379469** (diagnostic fixture of record per contract §6, contains both swapped and ordered instances of same family/type)
- Shell selections: **Left/Exterior** first, then **Right/Interior** (per domain model §3, operator picks side after selecting wall)
- Instrumented DLL location: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll` (per T2.4 handoff)
- Diagnostic marker: `[PHASE2_INSTRUMENTATION]` visible in output XML (per T2.4 handoff)

**Required operator returns (per contract §7.6 and evidence queue EV-1):**
1. 2 combined XML files (one per shell run; audit already appended per contract §3)
2. 2 created dimension element ids
3. 2 annotated screenshots showing segment values (mm precision per roadmap)
4. Commit or rollback observation per run
5. Optional journal excerpt (if anomalies observed)

**Execution-safety considerations:**
- Each run is one wall, one side selection, one chain dimension creation attempt per contract §5 domain model
- The instrumented build from T2.4 must be loaded in Revit before ribbon command execution
- XML output location is controlled by `QuickDimensionReadOnlyXmlLogService` default path (not specified in this task; operator uses existing file location convention from prior smokes)
- Transaction commit/rollback is observable in Revit UI and journal

## Decision

The runbook is complete and execution-safe. The master can ask EV-1 using this runbook verbatim with no additional explanation required.

## Open questions

None.

## Handoff for downstream tasks

### Operator instructions (ready for EV-1 request)

**Prerequisites:**
- Load the instrumented DLL from `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll` into Revit 2026
- Open the project containing wall **379469**

**Run 1: Left/Exterior shell**
1. Launch `QuickDimensionCreateChainSmokeCommand` from the ArcTool ribbon
2. Select wall **379469**
3. Pick the **Left/Exterior** side
4. Observe transaction commit or rollback in Revit UI
5. Record the created dimension element id (if committed)

**Run 2: Right/Interior shell**
1. Launch `QuickDimensionCreateChainSmokeCommand` from the ArcTool ribbon
2. Select wall **379469**
3. Pick the **Right/Interior** side
4. Observe transaction commit or rollback in Revit UI
5. Record the created dimension element id (if committed)

**Return to master:**
- [ ] Combined XML file from run 1 (audit appended; look for `[PHASE2_INSTRUMENTATION]` marker)
- [ ] Combined XML file from run 2 (audit appended; look for `[PHASE2_INSTRUMENTATION]` marker)
- [ ] Dimension element id from run 1
- [ ] Dimension element id from run 2
- [ ] Screenshot of run 1 dimension showing segment values in millimeters
- [ ] Screenshot of run 2 dimension showing segment values in millimeters
- [ ] Commit or rollback observation per run (state whether transaction committed or rolled back)
- [ ] Optional: journal excerpt if any anomaly observed during execution

**Expected instrumentation evidence:**
Each combined XML should contain `[PHASE2_INSTRUMENTATION]` diagnostic blocks logging six fields per accepted opening reference: `Reference`, `ReferenceOwner`, `ProjectionPoint`, `ProjectedStation`, `PointProvenance`, `Strategy`.
