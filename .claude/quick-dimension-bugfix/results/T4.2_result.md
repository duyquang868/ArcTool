# T4.2 — Apply BUG-11 production fix

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T4.2_apply_bug11_fix.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.1_result.md`, targeted reads of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`, MSBuild output, Revit API citations via Autodesk/RevitAPIDocs web lookup
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.2_result.md`

## Findings

1. Named-reference callers no longer depend on bbox-estimated proxy points.
   - `TryProjectOpeningOntoWallAxis` now passes `familyReferences.LeftPoint/RightPoint` into `AddOpeningReferenceInfo` and no longer blocks on estimated left/right points (`ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:260-277`).
   - The deprecated `TryCollectOpeningCandidates` path was updated the same way (`...QuickDimensionDoorWindowCandidateCollector.cs:449-477`).
   - `TryGetEstimatedReferencePoints` was removed because the named-reference path no longer consumes proxy-derived station points (`...QuickDimensionDoorWindowCandidateCollector.cs` grep-verified absent after edit).

2. The BUG-11 defect site now requires atomic reference + owned point for `FamilyInstanceLeftRight`.
   - `AddOpeningReferenceInfo` now accepts `familyPoint` and emits `QuickDimensionReferenceStrategy.FamilyInstanceLeftRight` only when both `familyReference` and `familyPoint` exist (`...QuickDimensionDoorWindowCandidateCollector.cs:572-609`).
   - Fallback-only `HostWallOpeningGeometry` behavior remains intact and still uses `fallbackReference + fallbackPoint` only (`...QuickDimensionDoorWindowCandidateCollector.cs:582-609`).
   - No local-pair-swap acceptance, dedupe changes, chain-creation changes, or audit-logic changes were introduced.

3. `TryGetFamilyInstanceReferences` keeps the required `[0]` selection unchanged and now resolves owned points from the same reference geometry.
   - Left path still uses `instance.GetReferences(FamilyInstanceReferenceType.Left)[0]` when available (`...QuickDimensionDoorWindowCandidateCollector.cs:667-674`).
   - Right path still uses `instance.GetReferences(FamilyInstanceReferenceType.Right)[0]` when available (`...QuickDimensionDoorWindowCandidateCollector.cs:685-692`).
   - The method now returns `OpeningReferencePair(leftReference, rightReference, leftPoint, rightPoint)` with owned points populated when station resolution succeeds (`...QuickDimensionDoorWindowCandidateCollector.cs:657-701`).

4. New helper `TryResolveReferenceOwnedPoint` derives the station point from the reference's own geometry, not from label semantics.
   - `instance.GetGeometryObjectFromReference(reference)` resolves the geometry from the same `FamilyInstance` that owns the named reference (`...QuickDimensionDoorWindowCandidateCollector.cs:704-757`).
   - If the geometry is a `Face`, the code projects the opening center point onto that face and uses `projection.XYZPoint` (`...QuickDimensionDoorWindowCandidateCollector.cs:722-730`).
   - If the geometry is an `Edge`, the code uses `edge.AsCurve()` and takes the midpoint when the curve is a `Line` (`...QuickDimensionDoorWindowCandidateCollector.cs:733-748`).

5. Provenance logging now matches the fixed semantics.
   - `DeriveProvenance` no longer compares named refs against estimated/fallback proxies.
   - It now reports `Reference-Owned (FamilyInstanceLeftRight)` for named refs and `Reference-Owned (HostWallOpeningGeometry)` for fallback refs (`...QuickDimensionDoorWindowCandidateCollector.cs:350-358`).
   - Existing PHASE2 instrumentation stays present in the main-flow log block (`...QuickDimensionDoorWindowCandidateCollector.cs:304-317`).

6. BUG-10 ownership and downstream behavior stayed untouched.
   - Candidate construction still uses the existing `instance.Id` / `hostWall.Id` metadata contract in both collector paths (`...QuickDimensionDoorWindowCandidateCollector.cs:319-323`, `...QuickDimensionDoorWindowCandidateCollector.cs:534-544`).
   - No change was made to fallback pair selection, wall anchors, dedupe, chain creation, or audit semantics.

7. Build verification passed.
   - Command used: `"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" ArcTool.Core/ArcTool.Core.csproj -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo`
   - Result: build succeeded and produced `ArcTool.Core.dll`.
   - Remaining warning is pre-existing and outside write scope: `QuickDimensionReadOnlyXmlLogService.cs(77,32) CS8600`.

8. API citations used for the geometry-resolution strategy.
   - `FamilyInstance` 2026 class page, showing `GetReferences(FamilyInstanceReferenceType)` in the member set: https://www.revitapidocs.com/2026/0d2231f8-91e6-794f-92ae-16aad8014b27.htm
   - Autodesk 2026 API reference for `Element.GetGeometryObjectFromReference(Reference)`: https://help.autodesk.com/cloudhelp/2026/ENU/Revit-API-MainReference/files/html/536b3d7a-ec8d-29f6-5957-751468c98dd0.htm
   - Revit API Docs 2026 for `Face.Project(XYZ)`: https://www.revitapidocs.com/2026/4bee3e30-74fa-3103-c2f4-d07618fbcedf.htm
   - Revit API Docs 2026 for `Edge.AsCurve()`: https://www.revitapidocs.com/2026/4d780a44-c713-036f-840f-605527bbb2ce.htm

## Decision
PASS. The collector now binds named `FamilyInstanceReferenceType.Left/Right` references to points derived from the same reference geometry, uses those points atomically in both caller paths, preserves fallback-only proxy behavior for `HostWallOpeningGeometry`, and builds successfully without touching BUG-10 ownership or audit semantics.

## Open questions
None.

## Handoff for downstream tasks
- Operator regression evidence is still needed on walls 379467, 379469, and 379470 to confirm BUG-11 is cleared under real smoke.
- BUG-10 remains intentionally untouched in this task.
- If downstream work inspects XML instrumentation, named refs should now log as `Reference-Owned (FamilyInstanceLeftRight)` and fallback refs as `Reference-Owned (HostWallOpeningGeometry)`.
