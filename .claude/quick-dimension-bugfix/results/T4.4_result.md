# T4.4 — Static regression review before secondary fixes

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T4.4_static_regression_review.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.2_result.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.3_result.md`, codebase-memory symbol discovery for `TryProjectOpeningOntoWallAxis` / `TryCollectOpeningCandidates` / `AddOpeningReferenceInfo` / `TryGetFamilyInstanceReferences` / `DeriveProvenance`, targeted reads of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:237-358,361-611,657-757`, Revit API web search results for `FamilyInstance.GetReferences(...)` and `Element.GetGeometryObjectFromReference(...)`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.4_result.md`

## Findings

1. Named-reference selection stayed unchanged, satisfying contract R4.
   - `TryGetFamilyInstanceReferences` still reads `instance.GetReferences(FamilyInstanceReferenceType.Left)` and takes `leftReferences[0]`, and likewise `Right` with `rightReferences[0]` (`ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:667-690`).
   - The patch only adds owned-point resolution after selection via `TryResolveReferenceOwnedPoint(...)`; it does not alter which named reference index is chosen (`...QuickDimensionDoorWindowCandidateCollector.cs:671-689`).
   - This matches the shared contract invariant that `GetReferences(Left)[0]` / `GetReferences(Right)[0]` must not change (`.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md:38-39`).
   - API evidence: the Revit 2026 `FamilyInstance` class page exposes `GetReferences`, and the method signature is documented as `IList<Reference> GetReferences(FamilyInstanceReferenceType referenceType)` on the Revit API Docs member page. URLs: `https://www.revitapidocs.com/2026/0d2231f8-91e6-794f-92ae-16aad8014b27.htm`, `https://www.revitapidocs.com/2025/a8a7dc74-db8e-a7b6-a9c8-869397cca6b4.htm`.

2. Fallback-only paths are still available in both the wall-axis and deprecated intersection collectors.
   - `TryProjectOpeningOntoWallAxis` still computes `fallbackReferences` behind `options.EnableHostWallOpeningFallback` and still routes both Left and Right through `AddOpeningReferenceInfo(...)` with both named and fallback inputs (`...QuickDimensionDoorWindowCandidateCollector.cs:260-277`).
   - `TryCollectOpeningCandidates` preserves the same fallback gate and call pattern (`...QuickDimensionDoorWindowCandidateCollector.cs:449-477`).
   - `AddOpeningReferenceInfo` keeps three distinct branches: forced fallback when validation says to use it, named reference when both `familyReference` and `familyPoint` exist, and safety fallback when named-reference stationing is unavailable but fallback is enabled (`...QuickDimensionDoorWindowCandidateCollector.cs:582-609`).
   - Therefore the BUG-11 patch did not remove the fallback escape hatch; it only stops the named-reference path from using an independently-derived proxy point.
   - `TryResolveReferenceOwnedPoint` returns `false` for non-stationable named references rather than inventing a proxy; those cases can still flow into fallback when enabled (`...QuickDimensionDoorWindowCandidateCollector.cs:704-757`).
   - API evidence: Autodesk 2026 documents `Element.GetGeometryObjectFromReference(Reference)` as the element-owned geometry lookup used by the new resolver. URL: `https://help.autodesk.com/cloudhelp/2026/ENU/Revit-API-MainReference/files/html/536b3d7a-ec8d-29f6-5957-751468c98dd0.htm`.

3. BUG-10 metadata scope remains separate.
   - In the main wall-axis path, candidate creation still uses `instance.Id` as `ElementId` and `hostWall.Id` as `HostElementId` (`...QuickDimensionDoorWindowCandidateCollector.cs:319-323`).
   - In the deprecated intersection path, candidate creation keeps the same ownership metadata pattern (`...QuickDimensionDoorWindowCandidateCollector.cs:534-544`).
   - `AddOpeningReferenceInfo` only chooses the live `Reference`, `ReferencePoint`, label, and `QuickDimensionReferenceStrategy`; it does not rewrite candidate ownership metadata (`...QuickDimensionDoorWindowCandidateCollector.cs:582-609`).
   - That preserves contract R5: BUG-10 remains a metadata-only defect, separate from BUG-11 stationing (`.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md:40-42`).
   - This also agrees with the upstream BUG-11 apply review, which recorded that candidate ownership and fallback pair selection were intentionally left untouched (`.claude/quick-dimension-bugfix/results/T4.2_result.md:35-37,50-58`).

4. No audit semantics were changed by the reviewed BUG-11 patch boundary.
   - Within the reviewed collector symbols, the only semantics change outside station derivation is `DeriveProvenance`, which now returns collector-side diagnostic strings `Reference-Owned (HostWallOpeningGeometry)` and `Reference-Owned (FamilyInstanceLeftRight)` (`ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:350-358`).
   - Those strings are only consumed by the PHASE2 instrumentation diagnostic inside `TryProjectOpeningOntoWallAxis`; they do not alter `ReferenceArray` construction, candidate ordering, or audit acceptance (`...QuickDimensionDoorWindowCandidateCollector.cs:304-317`).
   - Upstream task evidence shows the BUG-11 patch write scope touched only `QuickDimensionDoorWindowCandidateCollector.cs` plus its own result file (`.claude/quick-dimension-bugfix/results/T4.2_result.md:5-6`), and the follow-on build task touched no source code at all (`.claude/quick-dimension-bugfix/results/T4.3_result.md:5-6`).
   - Against contract R2 and R6, no reviewed code introduces local-pair-swap acceptance, unordered matching, wall-anchor changes, dedupe changes, or chain-creation changes (`.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md:32-45`).

5. Regression suspicion review: no NO_GO issue found, but one runtime watch item remains.
   - `TryResolveReferenceOwnedPoint` only materializes a named-reference point from two geometry cases: `Face.Project(openingCenterPoint)` and midpoint of a linear `Edge.AsCurve()` (`ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:714-748`).
   - If a family returns a named reference whose owned geometry is neither a face nor a linear edge, the named-reference path now declines to fabricate a station and relies on fallback when enabled (`...QuickDimensionDoorWindowCandidateCollector.cs:733-756`).
   - This does not break the agreed boundaries; it is consistent with contract R3 because it refuses to reintroduce independent proxy-point pairing (`.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md:35-37`).
   - It is a runtime watch item only: operator smokes should notice it as fallback usage or `MissingReference`, not as a static NO_GO.

## Decision
PASS. The BUG-11 patch stays inside the agreed boundaries: named-reference `[0]` selection is unchanged, fallback-only paths remain live, BUG-10 ownership metadata is still separate from reference/station derivation, and no audit semantics were modified in the reviewed patch boundary.

## Open questions
- Runtime only: do any production families expose named Left/Right references whose owned geometry is neither a face nor a linear edge, forcing fallback even after the BUG-11 fix?

## Handoff for downstream tasks
- Phase 5 can continue; there is no static boundary break that warrants NO_GO.
- Keep BUG-10 work scoped to metadata ownership only; this review found no justification to broaden into reference selection or audit logic.
- Operator smoke should watch for fallback activation or `MissingReference` on any family whose named references cannot be station-derived from owned geometry.