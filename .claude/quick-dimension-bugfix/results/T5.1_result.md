# T5.1 — Apply BUG-10 metadata fix

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T5.1_apply_bug10_metadata_fix.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T1.3_result.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.4_result.md`, codebase-memory symbol discovery for `TryProjectOpeningOntoWallAxis` / `TryCollectOpeningCandidates` / `AddOpeningReferenceInfo` / `QuickDimensionCandidate`, targeted reads of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:237-374,531-560`, Revit API Docs 2026 `Reference.ElementId` property page: https://www.revitapidocs.com/2026/909ec304-3c41-8319-4c80-efedce795d7f.htm, MSBuild verification of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/ArcTool.Core.csproj`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.1_result.md`

## Findings

1. BUG-10 fix landed only at candidate metadata assignment sites.
   - In the wall-axis path, candidate creation now resolves `candidateElementId` immediately before constructing `QuickDimensionCandidate`, instead of always passing `instance.Id` (`D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:319-324`).
   - In the deprecated intersection path, the same metadata-only substitution was applied at the candidate constructor site (`D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs:549-560`).
   - No geometry inputs changed: both paths still pass the pre-existing `projectedPoint` / `parameterOnDimensionLine` or `hitPoint` / `parameterOnDimensionLine`, and both still pass the same `referenceInfo.Reference` and `referenceInfo.ReferenceStrategy` values.

2. The new helper preserves the T1.3 ownership rule exactly: fallback candidates inherit the live reference owner, everything else stays on the opening instance.
   - New helper:
     - `ResolveCandidateElementId(ElementId instanceId, OpeningReferenceInfo referenceInfo)` returns `referenceInfo.Reference.ElementId` only when `referenceInfo.ReferenceStrategy == QuickDimensionReferenceStrategy.HostWallOpeningGeometry` and the owner is not `ElementId.InvalidElementId`; otherwise it returns the original `instanceId` (`...QuickDimensionDoorWindowCandidateCollector.cs:362-374`).
   - This matches the BUG-10 scope locked in T1.3: normalize fallback `ElementId` semantics only, preserve `HostElementId` as host-wall context, and do not touch attachment, pair selection, or geometry (`D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T1.3_result.md:83-91,114-117`).
   - Revit API evidence: `Reference.ElementId` is the API-owned element identifier exposed by the live `Reference`; Autodesk documents that `InvalidElementId` is returned when the reference does not point to a specific element. Source: https://www.revitapidocs.com/2026/909ec304-3c41-8319-4c80-efedce795d7f.htm.

3. `HostElementId` stays unchanged as required by the task contract.
   - Both candidate constructors still pass `hostWall.Id` in the `hostElementId` slot after the metadata fix (`...QuickDimensionDoorWindowCandidateCollector.cs:323-324` and `...QuickDimensionDoorWindowCandidateCollector.cs:557-558`).
   - This preserves the selected-host-wall context while aligning fallback `elementId` with the resolved reference owner invariant.

4. BUG-11 behavior and fallback selection logic were not widened.
   - `AddOpeningReferenceInfo` is unchanged: it still selects between the precomputed family reference path and fallback reference path exactly as before, and it still emits `QuickDimensionReferenceStrategy.HostWallOpeningGeometry` for fallback candidates (`...QuickDimensionDoorWindowCandidateCollector.cs:572-610`).
   - `TryProjectOpeningOntoWallAxis` and `TryCollectOpeningCandidates` still build candidates from the same `referenceInfos` list; the patch only changes which `ElementId` metadata value is copied into `QuickDimensionCandidate` at the final constructor site (`...QuickDimensionDoorWindowCandidateCollector.cs:265-277,290-325,454-477,496-560`).
   - This preserves shared-contract invariants R4-R6 and the static-review conclusion from T4.4 that BUG-10 must remain separate from BUG-11 stationing behavior (`D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md:38-45`; `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T4.4_result.md:24-35,49-52`).

5. Build verification passed after the metadata-only patch.
   - Command used: `"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" "D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/ArcTool.Core.csproj" -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo`.
   - Result: build succeeded and produced `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/bin/x64/Debug/net8.0-windows/ArcTool.Core.dll`.
   - Observed warning was pre-existing and outside write scope: `QuickDimensionReadOnlyXmlLogService.cs(77,32): warning CS8600`.

## Decision
The BUG-10 fix is complete and stays inside the approved boundary: fallback candidates now copy `elementId` from the resolved live `Reference.ElementId` when strategy is `HostWallOpeningGeometry`, while named-reference candidates, `hostElementId`, geometry, reference selection, attachment, and BUG-11 behavior remain unchanged.

## Open questions
None.

## Handoff for downstream tasks
- Downstream audit/logging work can now assume fallback `QuickDimensionCandidate.ElementId` matches the live reference owner when `ReferenceStrategy == HostWallOpeningGeometry`.
- No collector geometry or reference-order semantics changed in this patch; runtime regression should focus on confirming BUG-10 metadata closure without expecting any BUG-11 behavior change.
- Build remains green after this patch; the only observed compiler output was an unrelated existing nullable warning in `QuickDimensionReadOnlyXmlLogService.cs`.
