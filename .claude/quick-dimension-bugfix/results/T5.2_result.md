# T5.2 — Fix `actualSegmentCount` logging

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T5.2_fix_actual_segment_count.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T1.4_result.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.1_result.md`, codebase-memory symbol discovery attempts on project `D-Quang mini-OneDrive - MSFT-Plugin Revit-ArcTool`, targeted reads of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs:503-547`, `:665-699`, `:734-774`, `:921-936`, Revit API Docs 2026 pages for `Dimension`, `Dimension.Segments`, `DimensionSegment`, `DimensionSegmentArray.Size`, MSBuild verification of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/ArcTool.Core.csproj`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.2_result.md`

## Findings

1. The cosmetic defect was present exactly at the two sites locked by T1.4, and both now use the normalized measured-value count.
   - `BuildChainCreationAuditElement(...)` now computes `actualSegmentCount` from `segmentValuesInternal.Count` immediately after `ExtractSegmentValues(...)` at `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs:507-510`.
   - The top-level XML now emits `ChainCreationAudit/@actualSegmentCount` from that normalized count at `...QuickDimensionReadOnlyXmlLogService.cs:525-526`.
   - The nested builder call now passes the same normalized count into `BuildSegmentsAuditElement(...)` at `...QuickDimensionReadOnlyXmlLogService.cs:543`.
   - `BuildSegmentsAuditElement(...)` now receives `actualSegmentCount` and emits `Segments/@actualSegmentCount` from the same value at `...QuickDimensionReadOnlyXmlLogService.cs:665-699`.

2. The correct count source is the normalized measured-value collection, not the raw Revit segment properties.
   - `ExtractSegmentValues(...)` still normalizes both supported Revit shapes into one ordered measurement list: multi-segment dimensions read `DimensionSegment.Value` from `Segments`, while the single-segment shape reads `Dimension.Value` when `expectedReferenceCount == 2` and `Segments` is empty by design (`...QuickDimensionReadOnlyXmlLogService.cs:734-774`).
   - Therefore `segmentValuesInternal.Count` is the only count that always matches the number of measurement values actually collected for audit, which is the exact rule locked in T1.4.
   - Revit API basis:
     - `Dimension` exposes `NumberOfSegments`, `Segments`, and `Value`: https://www.revitapidocs.com/2026/210f88be-e3c5-26a4-7dd8-3296f6725cce.htm
     - `Dimension.Segments` returns `DimensionSegmentArray`: https://www.revitapidocs.com/2026/d7fcdab2-ca81-0ed1-4813-f7aa092430d7.htm
     - `DimensionSegment.Value` is the per-segment measurement value: https://www.revitapidocs.com/2026/36b254a0-3dc5-7bdc-d6b4-986e5d82ddbf.htm
     - `DimensionSegmentArray.Size` remains only the raw array size diagnostic: https://www.revitapidocs.com/2026/3aa3ea05-474f-f2d6-f8c9-dc821fcfc782.htm

3. Audit semantics stayed unchanged.
   - No edits were made to `referenceOrderRelation`, `referenceIdentityMatched`, `ReferenceOwnersMatch(...)`, `SegmentValuesMatch(...)`, tolerance handling, delta mapping, or XML append behavior.
   - `segmentArraySize` is still written separately as a raw diagnostic via `audit.Element("Segments")?.SetAttributeValue("segmentArraySize", segmentArraySize)` at `...QuickDimensionReadOnlyXmlLogService.cs:545`.
   - `MapSegmentIndexToExpectedDelta(...)` and segment matching logic remain unchanged; only the count source feeding the two `actualSegmentCount` attributes changed (`...QuickDimensionReadOnlyXmlLogService.cs:921-936`).

4. The change is surgical and build-clean within scope.
   - Source diff effect: remove the local `reportedSegmentCount = dimension?.NumberOfSegments ?? 0`, add `actualSegmentCount = segmentValuesInternal.Count`, rename the builder parameter, and swap both emitted attribute values to the normalized count.
   - MSBuild verification passed with the package-standard command on `ArcTool.Core.csproj`.
   - Observed compiler output was the same pre-existing nullable warning noted by T5.1: `QuickDimensionReadOnlyXmlLogService.cs(77,32): warning CS8600`; no new warning/error was introduced by this patch.

## Decision
The T5.2 cosmetic fix is complete: both emitted `actualSegmentCount` attributes now reflect the number of measured values actually collected from the normalized segment-value list, while all strict audit gates, owner checks, tolerance, and XML append behavior remain unchanged.

## Open questions
None.

## Handoff for downstream tasks
- T5.3 can assume `ChainCreationAudit/@actualSegmentCount` and `Segments/@actualSegmentCount` are now aligned and sourced from `segmentValuesInternal.Count`.
- `segmentArraySize` still exists as the separate raw diagnostic and must remain separate from measured-value count semantics.
- No audit gate behavior changed here; any downstream `valueSource` work should stay local to per-segment XML emission without touching order/owner/value matching logic.
