# T5.3 — Add `valueSource` to segment audit

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T5.3_add_value_source.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.2_result.md`, codebase-memory symbol discovery on project `D-Quang mini-OneDrive - MSFT-Plugin Revit-ArcTool`, targeted reads of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs:507-547`, `:666-699`, `:742-768`, Revit API 2026 doc URLs for `Dimension`, `Dimension.Segments`, `DimensionSegment`, `DimensionSegmentArray.Size`, MSBuild verification of `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/ArcTool.Core.csproj`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.3_result.md`

## Findings

1. The change stayed at the normalization point and per-segment XML builder, exactly as the task required.
   - `ExtractSegmentValues(...)` now returns `(ValueInternal, ValueSource)` tuples instead of bare values at `QuickDimensionReadOnlyXmlLogService.cs:742-768`.
   - Multi-segment measurements are tagged `"DimensionSegment.Value"` when collected from `Segments` at `...QuickDimensionReadOnlyXmlLogService.cs:755-760`.
   - The single-segment fallback path is tagged `"Dimension.Value"` when `expectedReferenceCount == 2` at `...QuickDimensionReadOnlyXmlLogService.cs:764-767`.

2. The new `valueSource` field is emitted on each `<Segment>` entry with no schema redesign.
   - `BuildSegmentsAuditElement(...)` now accepts the tuple list at `...QuickDimensionReadOnlyXmlLogService.cs:666-670`.
   - Each `<Segment>` gets one new attribute, `valueSource`, at `...QuickDimensionReadOnlyXmlLogService.cs:687-694`.
   - This is the smallest backward-readable shape change: existing attributes remain in place, and older readers that ignore unknown attributes can still parse the same `<Segments>/<Segment>` structure.

3. Existing segment values and gate logic were kept unchanged.
   - `BuildChainCreationAuditElement(...)` immediately projects the tuple list back to the pre-existing `List<double?> segmentValuesInternal` via `Select(measurement => measurement.ValueInternal)` at `...QuickDimensionReadOnlyXmlLogService.cs:507-515`.
   - `SegmentValuesMatch(...)`, `MapSegmentIndexToExpectedDelta(...)`, tolerance handling, order matching, owner matching, and append behavior were not edited.
   - The nested builder call now passes `segmentMeasurements`, while top-level gate inputs still consume the unchanged normalized values list at `...QuickDimensionReadOnlyXmlLogService.cs:509-545`.

4. The source distinction matches the documented Revit value shapes.
   - `Dimension.Segments` is the Revit API property for dimension segments: https://www.revitapidocs.com/2026/d7fcdab2-ca81-0ed1-4813-f7aa092430d7.htm
   - `DimensionSegment.Value` is the per-segment measurement value on the segment type page: https://www.revitapidocs.com/2026/36b254a0-3dc5-7bdc-d6b4-986e5d82ddbf.htm
   - `Dimension` exposes `Segments`, `NumberOfSegments`, and `Value` on the class page: https://www.revitapidocs.com/2026/210f88be-e3c5-26a4-7dd8-3296f6725cce.htm
   - `DimensionSegmentArray.Size` remains only a raw array-size diagnostic and was not repurposed: https://www.revitapidocs.com/2026/3aa3ea05-474f-f2d6-f8c9-dc821fcfc782.htm

5. The patch is compile-clean within scope.
   - MSBuild passed for `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/ArcTool.Core/ArcTool.Core.csproj`.
   - The only compiler output remained the pre-existing nullable warning at `QuickDimensionReadOnlyXmlLogService.cs(77,32): warning CS8600`; no new warning/error was introduced by this patch.

## Decision
The segment audit now records the exact measurement source per `<Segment>` using a single new `valueSource` attribute (`DimensionSegment.Value` or `Dimension.Value`), while preserving the existing normalized value flow and all strict audit semantics unchanged.

## Open questions
None.

## Handoff for downstream tasks
- Downstream XML consumers can now distinguish the two Revit measurement shapes per segment without inferring from counts or segment-array state.
- `actualSegmentCount`, order relation, owner checks, tolerance, and segment-value matching semantics are unchanged from T5.2.
- If operator evidence is reviewed later, expect `<Segment valueSource="DimensionSegment.Value" ...>` on multi-segment chains and `<Segment valueSource="Dimension.Value" ...>` on 2-reference single-segment chains.
