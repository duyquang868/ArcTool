# T6.1 — Operator runbook for regression matrix

- status: PASS
- date: 2026-08-03
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/04_EVIDENCE_QUEUE.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T6.1_operator_runbook_regression_matrix.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T5.5_result.md`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.1_result.md`

## Findings

1. EV-2 can be requested directly from this file because the command, fixed-build dependency, exact six runs, and exact return bundle are all fully specified here.

2. Operator runbook — run exactly this matrix on the fixed build prepared by T5.5.

| Run | Wall | Shell |
|---|---:|---|
| 1 | 379467 | Left/Exterior |
| 2 | 379467 | Right/Interior |
| 3 | 379469 | Left/Exterior |
| 4 | 379469 | Right/Interior |
| 5 | 379470 | Left/Exterior |
| 6 | 379470 | Right/Interior |

3. Command to run for every entry:
   - `QuickDimensionCreateChainSmokeCommand`

4. Execution-safe operator steps for each run:
   1. Use the fixed build from T5.5; do not change source, model content, or configuration between runs.
   2. Start `QuickDimensionCreateChainSmokeCommand`.
   3. Select the wall id listed for the run.
   4. Pick the shell listed for the run (`Left/Exterior` or `Right/Interior`).
   5. Let the command finish normally; do not rerun the same wall/shell before recording evidence.
   6. Immediately record the exact evidence listed below for that run before moving to the next run.

5. Exact evidence the operator must return for each run:
   - combined XML path for that run only (`one smoke run = one combined XML with <ChainCreationAudit> appended to the same XML`)
   - created dimension id (`none` only if the run rolled back and no dimension was created)
   - one annotated screenshot showing the displayed segment values for that dimension
   - one commit/rollback observation stating whether the command committed or rolled back
   - optional journal excerpt for that run

6. Return bundle format — one block per run, no interpretation required:

```markdown
### Run <1-6> — wall <id> — <shell>
- combined_xml_path: <full path>
- dimension_id: <element id | none>
- screenshot: <annotated image path>
- observation: <Committed | Rolled back> — <one short sentence>
- journal_excerpt: <optional; omit if none>
```

7. EV-2 request-ready text for the master:
   - Run `QuickDimensionCreateChainSmokeCommand` on walls `379467`, `379469`, and `379470`, both shells each (`Left/Exterior`, `Right/Interior`), using the fixed build from T5.5.
   - Return six evidence blocks, one per run, each containing: combined XML path, dimension id, annotated screenshot of segment values, commit/rollback observation, and optional journal excerpt.

## Decision

T6.1 passes because the regression-matrix operator runbook is now concise, execution-safe, and explicit enough for EV-2 to be requested straight from this file without extra interpretation.

## Open questions

None.

## Handoff for downstream tasks

- EV-2 should use this file as the direct operator request source.
- Required runtime is exactly six runs: wall `379467` both shells, wall `379469` both shells, wall `379470` both shells.
- Required return per run is exactly: combined XML path, dimension id, annotated screenshot, commit/rollback observation, optional journal excerpt.
