# T6.2 — Review wall 379467 regression evidence

- status: PASS
- date: 2026-08-04
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T6.2_review_wall_379467.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.1_result.md`, EV-2 evidence excerpt for wall `379467`
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.2_result.md`

## Findings

1. Both shell runs provide enough evidence to evaluate the wall-level acceptance gates from the shared contract.

| Shell | Commit | Ref count | Segment count | Order relation | Audit gates |
|---|---|---:|---:|---|---|
| Left/Exterior | Committed (`dimensionId=385355`) | `expectedReferenceCount=10`, `createdReferenceCount=10` | `expectedSegmentCount=9`, `actualSegmentCount=9` | `Exact` | `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true` |
| Right/Interior | Committed (`dimensionId=385356`) | `expectedReferenceCount=12`, `createdReferenceCount=12` | `expectedSegmentCount=11`, `actualSegmentCount=11` | `Exact` | `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true` |

2. BUG-11 is cleared on this fixture from the evidence provided. Historical context says wall `379467` previously exposed the local inversion on window `379477`. In EV-2, both shells now report `referenceOrderRelation=Exact` with `referenceIdentityMatched=true`, which is the strict audit outcome required by the shared contract. That is the expected post-fix signature for the atomic identity+station association.

3. BUG-10 is also cleared on this fixture from the evidence provided. Both shell excerpts state that every expected candidate has `elementIdMatchesReferenceOwner=true`, explicitly including fallback door `379481`. That removes the prior metadata-only divergence while staying separate from creation correctness.

4. The `actualSegmentCount` audit fix is confirmed on this fixture. Both shells show normalized counts matching expectation (`9/9` and `11/11`). The segment-value lists also reconcile exactly to the resolved line spans supplied in the excerpt:
   - Left/Exterior: listed segment values sum to `6452.495 mm`, matching `ResolvedDimensionLine spanMm=6452.495`.
   - Right/Interior: listed segment values sum to `6147.505 mm`, matching `ResolvedDimensionLine spanMm=6147.505`.

5. Geometry evidence is consistent with the audit output. The excerpt states the screenshot labels match the rounded audit segment values for both chains, and the coordinate screenshot confirms the resolved anchor and mid-run points align with the XML stations. On the Right/Interior shell, mid-run wall `379933` is accepted at stations `3804.878 mm` and `4006.38 mm`; the supplied screenshot evidence is reported as aligned with those XML stations.

6. No residual metadata-only issue is exposed for wall `379467` in this EV-2 excerpt. Creation correctness and metadata correctness both read clean on both shells.

7. One cosmetic enhancement remains unverified by this excerpt alone: the mission also added per-segment `valueSource` logging, but the excerpt does not quote those fields directly. This does not block a PASS for this task because the task gate is the wall-level regression review, and the supplied evidence is complete for the required acceptance checks.

## Decision

Wall `379467` is a PASS fixture for EV-2. Both shell runs committed successfully, matched expected reference and segment counts, audited `Exact` with all top-level gates true, and showed screenshot-consistent geometry. On this evidence, BUG-11 and BUG-10 both hold as fixed for wall `379467`, and the normalized `actualSegmentCount` audit fix also holds on this fixture.

## Open questions

- The excerpt does not independently show the per-segment `valueSource` fields, so that cosmetic enhancement is not directly re-verified here.

## Handoff for downstream tasks

- Treat wall `379467` as regression PASS on both shells for BUG-11, BUG-10, and strict chain-creation audit gates.
- No residual candidate-owner divergence is visible for fallback door `379481` on this wall.
- If a downstream task needs explicit proof of per-segment `valueSource`, it must inspect the raw combined XML rather than this excerpt.
