# T6.3 — Review wall 379469 regression evidence

- status: PASS
- date: 2026-08-04
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T6.3_review_wall_379469.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.1_result.md`, EV-2 wall `379469` evidence excerpt supplied in the task prompt
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.3_result.md`

## Findings

1. Both EV-2 shell runs for wall `379469` satisfy the strict acceptance gates from the shared contract.
   - Left/Exterior (`dimensionId=385632`): `attempted=true`, `succeeded=true`, `transactionStatus=Committed`, `expectedReferenceCount=10`, `createdReferenceCount=10`, `expectedSegmentCount=9`, `actualSegmentCount=9`, `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.
   - Right/Interior (`dimensionId=385584`): `attempted=true`, `succeeded=true`, `transactionStatus=Committed`, `expectedReferenceCount=12`, `createdReferenceCount=12`, `expectedSegmentCount=11`, `actualSegmentCount=11`, `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.

2. The historical BUG-11 diagnostic divergence on wall `379469` is clean in EV-2.
   - This fixture previously contained swapped cases `379475`, `379472`, and `379471`, plus same-type ordered control `379484`.
   - In EV-2, both shell audits are `Exact` with all top-level gates `true`, so the prior swapped-vs-ordered split is no longer visible at the chain-audit level.
   - Because the diagnostic fixture of record now passes on both shells under strict order semantics, the old invariant breach is not present in the supplied evidence.

3. Geometry evidence is consistent with the audit output for both shells.
   - Left/Exterior resolved dimension-line span is `9403.037 mm`; the nine displayed segment labels are reported as matching the rounded audit values: `1548.707 / 406 / 1139.5 / 915 / 977.838 / 406 / 601.162 / 915 / 2493.83`.
   - Right/Interior resolved dimension-line span is `8869.941 mm`; the eleven displayed segment labels are reported as matching the rounded audit values: `1243.398 / 406 / 1139.5 / 915 / 316.876 / 200.963 / 460 / 406 / 601.162 / 915 / 2266.043`.
   - The coordinate screenshot is reported to align the resolved anchors and the accepted mid-run points with the XML station values, so the screenshot evidence does not indicate a geometry regression.

4. The Interior/Right shell preserves the expected mid-run evidence without breaking audit order.
   - Mid-run wall `379933` is accepted at stations `4173.428 mm` and `4374.391 mm`.
   - Those accepted stations coexist with `referenceOrderRelation=Exact` and `segmentValuesMatched=true`, so the mid-run participation seen on this wall is consistent with the strict post-commit audit.

5. No remaining BUG-10 signal is present in the supplied wall `379469` evidence.
   - Both shell runs report that all `ExpectedCandidates` have `elementIdMatchesReferenceOwner=true`.
   - The task asked to note BUG-10 only if still visible; the supplied excerpt does not show any metadata-owner divergence on this wall.

## Decision

Wall `379469`, the BUG-11 diagnostic fixture of record, passes EV-2 on both shells with strict `Exact` audit ordering, matching geometry screenshots, correct segment counts, and no visible BUG-10 metadata drift, so this fixture is clean for downstream regression closure work.

## Open questions

None.

## Handoff for downstream tasks

- Treat wall `379469` as a confirmed clean EV-2 diagnostic fixture: both shells committed, counts matched, order relation stayed `Exact`, and geometry evidence matched the XML.
- Historical swapped instances on this wall no longer show divergence from the ordered control in the supplied evidence.
- No BUG-10 follow-up is indicated for wall `379469` from this excerpt.