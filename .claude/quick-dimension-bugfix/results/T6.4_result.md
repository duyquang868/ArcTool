# T6.4 — Review wall 379470 regression evidence

- status: PASS
- date: 2026-08-04
- inputs_read: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/01_SHARED_CONTRACT.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/tasks/T6.4_review_wall_379470.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/05_RESULT_SCHEMA.md`, `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.1_result.md`, EV-2 wall `379470` evidence excerpt supplied in the task prompt
- write_scope_touched: `D:/Quang mini/OneDrive - MSFT/Plugin Revit/ArcTool/.claude/quick-dimension-bugfix/results/T6.4_result.md`

## Findings

1. Both shell runs for wall `379470` satisfy the strict audit acceptance gates defined in the shared contract.

| Shell | XML | Commit | Ref count | Segment count | Order | Identity | Owners | Values |
|---|---|---|---:|---:|---|---|---|---|
| Left/Exterior | `ArcTool_QD_ReadOnlySummary_379470_Left_20260804_202239.xml` | `Committed` | `8/8` | `7/7` | `Exact` | `true` | `true` | `true` |
| Right/Interior | `ArcTool_QD_ReadOnlySummary_379470_Right_20260804_202244.xml` | `Committed` | `10/10` | `9/9` | `Exact` | `true` | `true` | `true` |

2. Left/Exterior evidence is internally consistent and clears the old smoke-#4 concern on the exterior shell.
   - `ChainCreationAudit` reports `attempted=true`, `succeeded=true`, `transactionStatus=Committed`, `dimensionId=385719`.
   - The resolved dimension-line span is `4439.267 mm`.
   - Segment values are `805.802 / 406 / 500 / 915 / 255.938 / 406 / 1150.527 mm`.
   - The excerpt states that screenshot labels match these rounded values and that the anchor coordinates align with the XML stations.
   - All `ExpectedCandidates` report `elementIdMatchesReferenceOwner=true`, including fallback door `379482`.

3. Right/Interior evidence is internally consistent and clears the old smoke-#4 concern on the interior shell.
   - `ChainCreationAudit` reports `attempted=true`, `succeeded=true`, `transactionStatus=Committed`, `dimensionId=385720`.
   - The resolved dimension-line span is `4071.55 mm`.
   - Segment values are `578.015 / 406 / 500 / 915 / 255.938 / 406 / 366.949 / 206.084 / 437.563 mm`.
   - Mid-run wall `380187` is accepted on the Interior/Right shell at stations `3541.796 mm` and `3747.881 mm`.
   - The excerpt states that screenshot labels match these rounded values and that the mid-run coordinates align with the XML stations.
   - All `ExpectedCandidates` report `elementIdMatchesReferenceOwner=true`, including fallback door `379482`.

4. BUG-10 is clean on wall `379470` in EV-2.
   - Historical divergence for fallback door `379482` is no longer present in either shell.
   - Because `elementIdMatchesReferenceOwner=true` is explicitly reported for that fallback door on both runs, the prior metadata-only mismatch is closed for this wall.

5. BUG-11 is closed for wall `379470` by current evidence.
   - Historical remaining-matrix evidence on windows `379479` and `379478` was specifically about local identity/station inversion surfacing as audit-order failure.
   - In EV-2, both shells now audit `Exact`, with `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, and `segmentValuesMatched=true`.
   - That combination means the previously problematic wall no longer shows the old creation/audit regression in either shell.

6. Closure scope must stay local to this task.
   - This evidence fully closes the old remaining-matrix concern for wall `379470` itself.
   - It does not, by itself, settle walls `379467` or `379469`; those remain separate regression-matrix review items outside T6.4 scope.

## Decision

Wall `379470` passes EV-2 on both shells: strict audit order/owner/value gates are all satisfied, screenshot geometry agrees with the rounded audit output, fallback door `379482` metadata is clean, and the old smoke-#4 remaining-matrix concern is fully closed for this wall.

## Open questions

None.

## Handoff for downstream tasks

- Treat wall `379470` as regression-cleared on both shells for BUG-10 and BUG-11 review purposes.
- Any matrix-level closure statement must combine this result with the separate reviews for walls `379467` and `379469`.
