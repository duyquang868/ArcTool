# T1.1 — Confirm source owners and no-touch boundaries

## Read first
- `../01_SHARED_CONTRACT.md`
- `../03_TASK_MANIFEST.md`

## Dependencies
- none

## Write scope
- `../results/T1.1_result.md` only

## Objective
Lock the exact source owners for BUG-11, BUG-10, and audit/logging work, and name the areas that must stay untouched.

## Do
1. Verify BUG-11 owner = `QuickDimensionDoorWindowCandidateCollector` main wall-axis flow plus the cross-association site.
2. Verify BUG-10 owner = the same collector, metadata only.
3. Verify `actualSegmentCount` and `valueSource` owner = `QuickDimensionReadOnlyXmlLogService`.
4. Verify `QuickDimensionChainCreationService` is reference-only, not an edit target.
5. Produce a concise no-touch list: wall anchors, mid-run aggregation, dedupe, chain creation transaction logic, strict audit semantics.

## PASS only if
- each change area has one clear owner file/symbol,
- the no-touch list is explicit,
- no scope conflict remains.

## BLOCKED if
- ownership cannot be stated precisely from the source map.