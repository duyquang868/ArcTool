# T1.3 — Lock BUG-10 metadata-only scope

## Read first
- `../01_SHARED_CONTRACT.md`
- `T1.1_confirm_source_owners.md`

## Dependencies
- `T1.1`

## Write scope
- `../results/T1.3_result.md` only

## Objective
Prove that BUG-10 is metadata-only and define exactly what may change later in `T5.1`.

## Do
1. Trace where fallback candidates are created and where `elementId` / `hostElementId` are assigned.
2. Confirm that creation/attachment uses `candidate.Reference`, not `candidate.ElementId`.
3. List the allowed BUG-10 edit boundary:
   - candidate ownership metadata only,
   - no geometry change,
   - no fallback pair-selection change,
   - no strategy enum change.
4. State the guardrail for future dedupe/grouping keyed on owner metadata.

## PASS only if
- the result clearly proves non-blocking status for `NewDimension`,
- the allowed edit boundary is explicit.

## BLOCKED if
- any attachment path still depends on `candidate.ElementId`.