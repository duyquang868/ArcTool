# T1.4 — Lock audit/logging fix scope

## Read first
- `../01_SHARED_CONTRACT.md`
- `T1.1_confirm_source_owners.md`

## Dependencies
- `T1.1`

## Write scope
- `../results/T1.4_result.md` only

## Objective
Define the exact edit boundary for the later logging fixes.

## Do
1. Confirm where `actualSegmentCount` is currently derived.
2. Confirm where segment audit entries are built.
3. State the allowed future edits:
   - use measured-value count for `actualSegmentCount`,
   - add `valueSource` per segment entry,
   - preserve strict `Exact` / full `Reversed` semantics,
   - preserve owner and segment-value gates.
4. Name what must not change: no local-pair-swap acceptance, no tolerance widening, no audit gate weakening.

## PASS only if
- the edit boundary is specific enough for `T5.2` and `T5.3`.

## BLOCKED if
- the logging ownership map is inconsistent.