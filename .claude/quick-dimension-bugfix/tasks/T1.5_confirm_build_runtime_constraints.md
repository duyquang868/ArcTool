# T1.5 — Confirm build/runtime constraints

## Read first
- `../01_SHARED_CONTRACT.md`
- `../04_EVIDENCE_QUEUE.md`

## Dependencies
- `T1.1`

## Write scope
- `../results/T1.5_result.md` only

## Objective
Lock the build path and the operator-owned runtime boundary before any source edit starts.

## Do
1. Confirm the Visual Studio MSBuild command that must be used.
2. Confirm why `dotnet build` is not the verification path here.
3. Confirm that no worker may launch Revit, use Revit MCP, or run smoke directly.
4. Confirm that runtime proof flows only through operator runbooks and returned evidence.
5. List the exact evidence artifacts expected from the operator.

## PASS only if
- build and runtime boundaries are both explicit,
- later build tasks can quote this result without re-deriving it.

## BLOCKED if
- build verification path is still ambiguous.