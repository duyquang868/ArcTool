# T2.2 — Prepare exact patch plan from T2.1

## Read first
- `../01_SHARED_CONTRACT.md`
- result file: `T2.1`

## Dependencies
- `T2.1`

## Write scope
- `../results/T2.2_result.md` only

## Objective
Turn the instrumentation design into an edit-ready patch plan with exact symbol targets.

## Do
1. Name every symbol/method that must change.
2. State the exact new fields, local variables, or diagnostics additions.
3. State what must not change semantically.
4. State how T2.4 will verify the instrumented build without Revit runtime.

## PASS only if
- `T2.3` can execute directly from this plan,
- the write scope stays inside `QuickDimensionDoorWindowCandidateCollector.cs`.

## BLOCKED if
- the design still leaves multiple valid edit interpretations.