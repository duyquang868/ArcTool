# T2.4 — Build instrumented debug candidate

## Read first
- `../01_SHARED_CONTRACT.md`
- result file: `T2.3`

## Dependencies
- `T2.3`

## Write scope
- `../results/T2.4_result.md` only

## Objective
Verify that the instrumented source compiles through the approved Visual Studio MSBuild path.

## Do
1. Run the locked MSBuild command from the shared contract.
2. Capture success/failure faithfully.
3. If build fails, isolate whether the failure is caused by T2.3 or by unrelated pre-existing state.
4. Do not run Revit.

## PASS only if
- the build succeeds.

## BLOCKED if
- the build cannot be executed in this environment.

## NO_GO if
- T2.3 introduced a compile failure that invalidates the instrumentation handoff.