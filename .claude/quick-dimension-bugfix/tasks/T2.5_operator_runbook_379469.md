# T2.5 — Operator runbook for wall 379469 (both shells)

## Read first
- `../01_SHARED_CONTRACT.md`
- `../04_EVIDENCE_QUEUE.md`
- result file: `T2.4`

## Dependencies
- `T2.4`

## Write scope
- `../results/T2.5_result.md` only

## Objective
Write the exact operator steps for the diagnostic instrumentation smoke on wall `379469`, both shells.

## Do
1. Specify the command to run: `QuickDimensionCreateChainSmokeCommand`.
2. Specify the target wall and the two shell selections.
3. Specify exactly what the operator must return:
   - 2 combined XML files,
   - 2 created dimension ids,
   - 2 screenshots showing segment values,
   - commit/rollback observation,
   - optional journal excerpt.
4. Keep it concise and execution-safe.

## PASS only if
- the runbook is precise enough for the master to ask EV-1 with no extra explanation.

## BLOCKED if
- a required runtime precondition is unknown.