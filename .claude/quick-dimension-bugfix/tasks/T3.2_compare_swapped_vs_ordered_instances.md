# T3.2 — Compare swapped vs ordered same-type instances

## Read first
- `../01_SHARED_CONTRACT.md`
- result file: `T3.1`

## Dependencies
- `T3.1`

## Write scope
- `../results/T3.2_result.md` only

## Objective
Use wall `379469` to compare swapped and ordered instances of the same family/type and isolate the invariant that distinguishes them.

## Do
1. Compare window `379475` vs window `379484` explicitly.
2. Compare door instances if EV-1 contains usable evidence.
3. State whether the data supports the reference-owned station hypothesis strongly enough for production.
4. Keep flip/mirror/orientation as hypothesis only unless EV-1 proves it.

## PASS only if
- the comparison sharpens the fix rule beyond a generic restatement.

## BLOCKED if
- EV-1 lacks the side-by-side detail needed for a same-type comparison.