# T4.1 — Design the production BUG-11 source change

## Read first
- `../01_SHARED_CONTRACT.md`
- result files: `T3.3`, `T3.4`

## Dependencies
- `T3.4`

## Write scope
- `../results/T4.1_result.md` only

## Objective
Translate the proven reference-owned station rule into an exact production edit plan for the collector.

## Do
1. Name the methods and DTOs that must change.
2. State how named references will carry their own physical projection input.
3. State what proxy-derived path remains allowed for fallback-only references.
4. Keep the design inside the collector owner file.
5. State how the plan preserves BUG-10's later edit boundary.

## PASS only if
- `T4.2` can implement directly from this result,
- the design does not widen into unrelated geometry logic.

## BLOCKED if
- the proven rule still leaves multiple materially different source edits.