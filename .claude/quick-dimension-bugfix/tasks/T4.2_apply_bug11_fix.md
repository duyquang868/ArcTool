# T4.2 — Apply BUG-11 production fix

## Read first
- `../01_SHARED_CONTRACT.md`
- result file: `T4.1`

## Dependencies
- `T4.1`

## Write scope
- `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`
- `../results/T4.2_result.md`

## Objective
Apply the production BUG-11 fix in the collector.

## Do
1. Edit only the authorized collector file.
2. Derive each named reference station from that reference's own geometry/projection path.
3. Keep named-reference selection unchanged.
4. Keep fallback-only behavior intact except where required to preserve the atomic identity+station association.
5. Remove only unused code created by your own patch.

## PASS only if
- the diff matches the T4.1 plan,
- local-pair-swap acceptance is not introduced,
- no unrelated behavior changes are added.

## BLOCKED if
- the implementation requires an unauthorized edit outside the collector.

## NO_GO if
- the T4.1 plan cannot be implemented without violating the shared contract.