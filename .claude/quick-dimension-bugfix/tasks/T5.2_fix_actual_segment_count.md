# T5.2 — Fix `actualSegmentCount` logging

## Read first
- `../01_SHARED_CONTRACT.md`
- result files: `T1.4`, `T5.1`

## Dependencies
- `T5.1`

## Write scope
- `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`
- `../results/T5.2_result.md`

## Objective
Make `actualSegmentCount` reflect the number of measurement values actually collected.

## Do
1. Edit only the XML log service.
2. Keep all audit gate semantics unchanged.
3. Use the measured-value collection count, not `Dimension.NumberOfSegments`, for the logged actual count.
4. Keep the change surgical.

## PASS only if
- the fix is cosmetic/logging-only,
- no audit gate logic changes.

## BLOCKED if
- the current code structure forces broader audit changes.