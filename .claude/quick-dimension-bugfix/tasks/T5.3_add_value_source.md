# T5.3 — Add `valueSource` to segment audit

## Read first
- `../01_SHARED_CONTRACT.md`
- result file: `T5.2`

## Dependencies
- `T5.2`

## Write scope
- `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`
- `../results/T5.3_result.md`

## Objective
Add a `valueSource` field to each segment audit entry.

## Do
1. Edit only the XML log service.
2. Keep existing segment values and gate logic unchanged.
3. Add the smallest field/attribute needed to tell whether the audit value came from `DimensionSegment.Value` or fallback `Dimension.Value`.
4. Keep the output backward-readable.

## PASS only if
- the new field is present without weakening strict audit behavior.

## BLOCKED if
- the change requires unrelated XML schema redesign.