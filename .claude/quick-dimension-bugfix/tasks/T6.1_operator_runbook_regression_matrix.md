# T6.1 — Operator runbook for regression matrix

## Read first
- `../01_SHARED_CONTRACT.md`
- `../04_EVIDENCE_QUEUE.md`
- result file: `T5.5`

## Dependencies
- `T5.5`

## Write scope
- `../results/T6.1_result.md` only

## Objective
Write the exact operator steps for the six-run regression matrix.

## Do
1. Specify the command to run: `QuickDimensionCreateChainSmokeCommand`.
2. Specify the required runs:
   - wall `379467` both shells,
   - wall `379469` both shells,
   - wall `379470` both shells.
3. Specify exactly what the operator must return for each run:
   - combined XML path,
   - created dimension id,
   - annotated screenshot of segment values,
   - commit/rollback observation,
   - optional journal excerpt.
4. Keep the runbook concise and execution-safe.

## PASS only if
- EV-2 can be requested directly from this file with no extra interpretation.

## BLOCKED if
- any required runtime step is still ambiguous.