# T6.5 — Operator runbook for reopen validation

## Read first
- `../01_SHARED_CONTRACT.md`
- `../04_EVIDENCE_QUEUE.md`
- result file: `T5.5`

## Dependencies
- `T5.5`

## Write scope
- `../results/T6.5_result.md` only

## Objective
Write the exact operator steps for reopen validation of previously committed valid dimensions.

## Do
1. Define the close/reopen check for the previously committed EV-2 dimensions.
2. Specify exactly what the operator must return:
   - reopen persistence observation,
   - post-reopen screenshot,
   - operator confirmation that displayed values stayed unchanged and dimensions still appear immediately at the picked side/position,
   - optional journal excerpt.
3. Record why forced rollback is deferred out of this mission without rewriting `QuickDimensionChainCreationService` or inventing a synthetic fixture.

## PASS only if
- EV-3 can be requested directly from this file as a reopen-only evidence request.

## BLOCKED if
- even the reopen validation procedure cannot be stated precisely.