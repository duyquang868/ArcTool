# T6.7 — Record the deferred rollback-validation track as a standalone future task

## Read first
- `../01_SHARED_CONTRACT.md` (section 8 scope narrowing is authoritative)
- `../results/T6.5_result.md` (the blocker analysis to preserve, not to repeat verbatim)

## Dependencies
- `T6.5`

## Write scope
- `../../../.Dossier/Quick Dimension - Deferred Rollback Validation Task.md` only

Create that file. Do not touch `CLAUDE.md`, `Memory/`, ADR, any other `.Dossier` file, any
package file, or any source file. `T7.1` owns all other durable records.

## Objective
Produce **one** self-contained dossier that a future session can pick up cold as an
independent task, so rollback validation is never re-litigated inside this mission again.

## Do
1. State the decision: forced-rollback validation is deferred out of the BUG-11/BUG-10
   mission (operator-approved 2026-08-04), and why deferral is technically sound —
   `QuickDimensionChainCreationService.cs` was never edited by this package, so rollback
   behavior is byte-identical to the pre-fix build.
2. Record the precise control-flow finding from `T6.5_result.md`: every operator-reachable
   invalid input returns `Result.Cancelled` **before** `Transaction.Start()`; the rollback
   branches execute only on internal post-start failures (`NewDimension` returns null,
   reference-count mismatch, non-`Committed` status, thrown exception). Cite the file/line
   anchors that result file already established. Do not re-derive them from source.
3. State the operator constraint plainly: the operator cannot manufacture such a failure by
   normal modelling actions, so no runtime request should ever be issued for it as-is.
4. Define the trigger conditions that would reopen this task — e.g. a field-observed
   rollback defect, a dirty-transaction report, or a corrupted read-only XML after a failed
   creation.
5. Define the entry approach for that future task: a fault-injection harness or debug-only
   switch that forces a post-`Transaction.Start()` failure without changing the production
   fix surface. Keep it as an approach sketch, not an implementation.
6. State what evidence that future task would need to close (clean rollback, no dirty
   transaction, original read-only XML preserved, no orphan `Dimension`).
7. English only. Keep it bounded and information-dense; no session diary, no duplicated
   EV-2 matrix tables.

## PASS only if
- the file exists, is self-contained, and requires no other package file to be actionable.

## BLOCKED if
- the shared contract's scope narrowing cannot be located or contradicts this task.
