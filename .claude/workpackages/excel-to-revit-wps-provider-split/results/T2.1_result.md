# T2.1 — Author `ISpreadsheetPdfExporter.cs`

- status: PASS
- date: 2026-08-09
- inputs_read: `01_SHARED_CONTRACT.md`, `tasks/T2.1.md`, `results/T1.1_result.md` (§1 locked interface,
  §5 decisions 1-5), `results/T1.3_result.md` (Q1/Q2 neutrality confirmation), `05_RESULT_SCHEMA.md`.
  Isolation check: `grep -n "namespace" ArcTool.Core/Services/ExcelInteropService.cs
  ArcTool.Core/Services/ExcelSyncEngine.cs` → both `namespace ArcTool.Core.Services`. Confirmed target
  folder `ArcTool.Core/Services/Excel/` did not exist before this task (`ls` → "No such file or
  directory").
- write_scope_touched: `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs`

## Findings

### Namespace convention

Sibling services (`ExcelInteropService.cs`, `ExcelSyncEngine.cs`) both use `namespace
ArcTool.Core.Services`. Per task instruction, the `Excel` subfolder gets the matching child
namespace: `ArcTool.Core.Services.Excel`.

### File content

Wrote exactly two declarations, no implementation:

1. `public enum SpreadsheetEngine { MsExcel, Wps }` — verbatim from T1.1 §1. XML doc on the enum and
   each member states "discriminator only", explicitly disclaims carrying any engine-specific type,
   COM interop reference, or ProgID, and states the name was chosen to avoid
   `Autodesk.Revit.DB` collision (contract §8).
2. `public interface ISpreadsheetPdfExporter : IDisposable` with the five members locked in T1.1 §1
   verbatim: `Engine`, `Open(string)`, `GetSheetNames()`, `GetNamedRanges(string)`,
   `ExportRegionToPdf(string, string, string)`. No signature deviation from T1.1.
3. XML doc on the interface and every member covers: what it does, what it returns on failure
   (`Open`/`ExportRegionToPdf` → `false`, never throws; `GetSheetNames`/`GetNamedRanges` → empty
   non-null list, never throws), and file ownership. `ExportRegionToPdf`'s doc comment carries the
   T1.1 decision-2 temp-PDF ownership rule verbatim in substance: "this method only writes the file
   ... it never deletes it ... The caller ... owns the file's entire lifetime ... This method must
   not be relied upon to clean up its own output."
4. `ExportRegionToPdf`'s doc comment also states the region-resolution fallback order (named range →
   print area → used range), matching T1.1 decision 3.
5. No implementation, no static helpers, no `#region` — file contains only `using System;`,
   `using System.Collections.Generic;`, the enum, and the interface (94 lines total).

### Isolation grep (acceptance condition)

Command:
```
grep -n -E "Interop|Xl[A-Z]|KET|ET\.Application|Worksheet|Workbook|Range" \
  ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs
```
Output:
```
14:        /// <summary>Microsoft Excel, accessed through early-bound Interop.</summary>
25:    /// Interop, WPS via late-bound COM only); neither implementation's engine-specific types may
68:        IReadOnlyList<string> GetNamedRanges(string sheetName);
```
All three are non-meaningful: lines 14 and 25 are prose in doc comments naming the word "Interop" to
describe which engine uses it (not a type reference — no `using Microsoft.Office.Interop.Excel;`,
no `Xl*` type, no COM type anywhere in the file); line 68 is the method name `GetNamedRanges`
matching the substring "Range" (not the COM `Range` type — the parameter/return types are `string`
and `IReadOnlyList<string>`). No `Xl*` enum, no `KET`/`ET.Application` ProgID, no `Worksheet`/
`Workbook`/`Range` COM type appears as a type reference anywhere. Gate passes.

## Decision

`ISpreadsheetPdfExporter.cs` authored exactly as locked by T1.1, confirmed engine-neutral by the
isolation grep. No deviation from the T1.1 signature list.

## Open questions

None.

## Handoff for downstream tasks

- `T3.1`/`T3.2` implement this interface for MS Excel and WPS respectively; the `ExportRegionToPdf`
  doc comment's temp-PDF-ownership and region-resolution-order text is caller-visible contract, not
  just internal comment — implementers must not have their concrete class delete the output PDF.
- `T3.3` (coordinator) is the party responsible for creating/deleting the temp PDF path per T1.1
  decision 2, restated in this file's doc comment.
- Namespace for all `Services/Excel/` files going forward: `ArcTool.Core.Services.Excel`.
