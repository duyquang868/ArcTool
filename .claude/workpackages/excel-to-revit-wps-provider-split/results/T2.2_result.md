# T2.2 — Author `PdfRasterImageService.cs`

- status: PASS
- date: 2026-08-09
- inputs_read: `01_SHARED_CONTRACT.md`, `tasks/T2.2.md`, `05_RESULT_SCHEMA.md`,
  `results/T3.7_result.md` (repair-pass bootstrap, per repair-task instructions).
- write_scope_touched: `ArcTool.Core/Services/Excel/PdfRasterImageService.cs` (verified only, no
  edit needed), this result file.

## Findings

Repair pass triggered by T3.7 BLOCKED audit check 4, which flagged `ExcelInteropService`
comment/doc references in three files including `PdfRasterImageService.cs` (lines 14, 20, 39 per
the T3.7-quoted grep at time of that audit).

Verified current file state before editing, per repair-task instruction to check for prior
out-of-scope cleanup. Current `PdfRasterImageService.cs` class-level and method-level doc comments
already read:
- line 14: "Ported unchanged from the legacy single-provider implementation (native-loader helpers, ..."
- line 20-22: "the coordinator, `SpreadsheetImageExportService`" (no `ExcelInteropService` literal)
- line 38-39: "Ported unchanged from the legacy render/crop routine: same DPI, same threshold, ..."

No literal `ExcelInteropService` string remains anywhere in the file. Someone (out-of-scope, per
the repair-task's own warning) already reworded these three doc comments before this pass started.
No source edit was made in this pass — the file was read in full and confirmed clean, not modified.

Confirmed no COM/spreadsheet markers present (file contains no
`Microsoft.Office.Interop`/`Worksheet`/`Workbook`/`GetTypeFromProgID`/`InvokeMember` — consistent
with the file's original PASS result and with T3.7 audit check 5, which already passed).

Verification command run against this file:
```bash
grep -nE "ExcelInteropService|Microsoft\.Office\.Interop|GetTypeFromProgID|InvokeMember|Worksheet|Workbook" ArcTool.Core/Services/Excel/PdfRasterImageService.cs
```
Output: no matches (exit 1, empty stdout) — zero hits confirmed.

Parity constants re-verified unchanged in the current file: `Dpi = 300`, `WithAnnotations = false`
(line ~70), `const byte WhiteThreshold = 240` (line ~79), four-direction crop scan order
(top → bottom → left → right, lines ~84-153), load-once native loader guards
(`EnsurePdfiumLoaded`/`EnsureSkiaSharpLoaded`, unchanged lock/guard structure). No executable logic
was touched in this pass.

## Decision

`PdfRasterImageService.cs` is confirmed, as of this repair pass, to contain zero literal
`ExcelInteropService` references and zero COM/spreadsheet markers, with all parity constants and
control flow intact and unmodified. The T3.7 audit-check-4 defect attributed to this file is
ratified closed under T2.2 ownership — no further edit is needed on this file. (The other two files
T3.7 flagged for the same check, `MsExcelWorkbookPdfExporter.cs` and `WpsWorkbookPdfExporter.cs`,
belong to `T3.1`/`T3.2` and are out of this task's write_scope.)

## Open questions

None.

## Handoff for downstream tasks

- `T3.7` (re-gate): for audit check 4, `PdfRasterImageService.cs` now returns zero hits on
  `grep -nE "ExcelInteropService|Microsoft\.Office\.Interop|GetTypeFromProgID|InvokeMember|Worksheet|Workbook" ArcTool.Core/Services/Excel/PdfRasterImageService.cs`.
  Re-running the check should attribute any remaining hits solely to
  `MsExcelWorkbookPdfExporter.cs` / `WpsWorkbookPdfExporter.cs` (owned by `T3.1`/`T3.2`), not to this
  file.
- No behavior, public API, or parity constant changed by this pass.
