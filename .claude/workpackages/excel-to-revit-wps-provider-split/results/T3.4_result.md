# T3.4 — Back up and delete the legacy `ExcelInteropService.cs`

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/01_SHARED_CONTRACT.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T3.4.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.1_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.2_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.3_result.md`; `ArcTool.Core/Services/ExcelInteropService.cs`; `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs`; `ArcTool.Core/Services/Excel/PdfRasterImageService.cs`; `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs`; `ArcTool.Core/ArcTool.Core.csproj`
- write_scope_touched: `ArcTool.Core/Services/_backup/ExcelInteropService.cs.bak`; `ArcTool.Core/Services/ExcelInteropService.cs`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.4_result.md`

## Findings
1. Pre-delete port verification passed before any destructive step.
   - `MsExcelWorkbookPdfExporter.cs` now owns the live COM open / enumerate / region-resolve / PageSetup / PDF export behaviors that used to live in `ExcelInteropService.cs` (`T3.1_result.md`, plus `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs:39-55,63-151,175-291,301-328`).
   - `PdfRasterImageService.cs` now owns the native loader + PDF render + white-margin crop stage formerly in `ExcelInteropService.cs:100-177` and `:231-348` (`ArcTool.Core/Services/Excel/PdfRasterImageService.cs:53-268`).
   - `SpreadsheetImageExportService.cs` now owns temp-PDF lifetime and the PDF→PNG handoff (`ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs:65-105`), matching the old `ExcelInteropService.cs:218-229` temp-path creation and `:355-357` cleanup split across coordinator+raster service.
   - The two non-ported methods are explicitly dead code by contract and upstream results: `GetActiveSheetName` (`ExcelInteropService.cs:54-66`) and `ExportPrintAreaAsHighResImage` (`ExcelInteropService.cs:68-98`).
   - No live behavior remained unaccounted for, so deletion was permitted.

2. Backup was created first and verified byte-identical.
   - Source: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\ExcelInteropService.cs`
   - Backup: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\_backup\ExcelInteropService.cs.bak`
   - Verification evidence:

     | File | Bytes | Lines | SHA-256 |
     |---|---:|---:|---|
     | `ExcelInteropService.cs` | 23198 | 585 | `ffd5005c21835c3f532ca2711a9612b76b5924fa8d3e0ab60c857acd78836897` |
     | `ExcelInteropService.cs.bak` | 23198 | 585 | `ffd5005c21835c3f532ca2711a9612b76b5924fa8d3e0ab60c857acd78836897` |

   - Outcome: byte-identical faithful backup confirmed before deletion.

3. Compile-safety check for `_backup/` passed without editing the project file.
   - `ArcTool.Core/ArcTool.Core.csproj:39-43` contains only `Compile Update="Properties\Resources.Designer.cs"`; there is no `Compile Include`, `Content Include`, `None Include`, or any `_backup/**` entry.
   - Because SDK-style default compile globs include only `**/*.cs`, the `.bak` file extension keeps the backup out of compilation as the task expected.
   - No `csproj` edit was required, so no BLOCKED condition was triggered.

4. Deletion completed.
   - Original file `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\ExcelInteropService.cs` was deleted after backup verification.
   - Post-delete existence check: original `exists=False`; backup `exists=True`.

5. Remaining live references match the expected three call sites exactly; no fourth live reference was found.
   - `ArcTool.Core\Services\ExcelSyncEngine.cs:160:                using (var svc = new ExcelInteropService())`
   - `ArcTool.Core\UI\ExcelToRevitWindow.xaml.cs:423:                using (var excelService = new ExcelInteropService())`
   - `ArcTool.Core\UI\ExcelToRevitWindow.xaml.cs:468:            using (var excelService = new ExcelInteropService())`
   - The broader grep also found stale copies under `.claude/worktrees/agent-*`; those were explicitly excluded per task instruction and are not live code.

## Decision
The legacy `ExcelInteropService.cs` was safely retired: all live behavior had already been ported or intentionally dropped per contract, a byte-identical backup now exists at `ArcTool.Core/Services/_backup/ExcelInteropService.cs.bak`, the project file does not compile the backup, the original source file was deleted, and the only remaining live references are the three expected call sites owned by the rewire tasks.

## Open questions
None.

## Handoff for downstream tasks
- Confirmed backup path for durable record / later recovery: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\_backup\ExcelInteropService.cs.bak`.
- `T3.5`/`T3.6` should rewire exactly these three live references and no others: `ArcTool.Core\Services\ExcelSyncEngine.cs:160`, `ArcTool.Core\UI\ExcelToRevitWindow.xaml.cs:423`, `ArcTool.Core\UI\ExcelToRevitWindow.xaml.cs:468`.
- Between this task and the rewire tasks, the build is knowingly broken because the deleted type is still referenced at those three sites.
