# T3.5 — Rewire `ExcelSyncEngine.cs` to the coordinator

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/01_SHARED_CONTRACT.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T3.5.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.3_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.4_result.md`; `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs`; `ArcTool.Core/Services/ExcelSyncEngine.cs:140-200`; `ArcTool.Core/Services/ExcelSyncEngine.cs:1-20`
- write_scope_touched: `ArcTool.Core/Services/ExcelSyncEngine.cs`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.5_result.md`

## Findings

1. The authorized rewire site in `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\ExcelSyncEngine.cs:160-179` now constructs `SpreadsheetImageExportService` instead of the deleted legacy type, with the `using` lifetime preserved exactly at the same scope boundary.

2. Both user-facing Vietnamese throws and both preceding `Debug.WriteLine` calls were preserved byte-for-byte at the call site.
   - Open failure throw still begins `Không mở được file Excel:` at `ExcelSyncEngine.cs:167-168`.
   - Export failure throw still begins `Không thể xuất hình từ Excel.` at `ExcelSyncEngine.cs:177-178`.
   - No engine-context text was appended, so downstream behavior remains unchanged.

3. The fallback comment remained intact exactly where required.
   - `// mapping.Region = null → ExportRegion tự fallback PrintArea → UsedRange` is still present at `ExcelSyncEngine.cs:171`.

4. The disposal comment remains semantically accurate after the coordinator swap.
   - Old wording implied Excel specifically closed at dispose.
   - New wording at `ExcelSyncEngine.cs:180` now says `// Dispose() gọi → phiên export spreadsheet đóng ngay`, which matches the coordinator contract from `T3.3_result.md`: the active provider session is disposed when the `using` block exits, regardless of whether the engine is MS Excel or WPS.

5. The only top-of-file adjustment was the needed namespace import.
   - Added `using ArcTool.Core.Services.Excel;` at `ExcelSyncEngine.cs:5` so the new coordinator type resolves.
   - No other using directive was removed because the deleted type had been in the same parent namespace; there was no legacy Excel-only import in the authorized header slice.

6. Acceptance check passed.
   - `grep -n "ExcelInteropService" ArcTool.Core/Services/ExcelSyncEngine.cs` now returns no matches.
   - Diff scope is confined to the header using list and the single rewire block.

### Before/after of the changed block

Before (`ExcelSyncEngine.cs:160-179` before edit):

```csharp
using (var svc = new ExcelInteropService())
{
    if (!svc.OpenFile(mapping.FilePath))
    {
        System.Diagnostics.Debug.WriteLine(
            $"[ExcelSyncEngine] Không mở được file: '{mapping.FilePath}'");
        throw new InvalidOperationException(
            $"Không mở được file Excel:\n{mapping.FilePath}\n\nFile có thể không tồn tại, đang bị khóa, hoặc Excel không đọc được file này.");
    }

    // mapping.Region = null → ExportRegion tự fallback PrintArea → UsedRange
    if (!svc.ExportRegion(mapping.WorkSheet, mapping.Region, tempPng))
    {
        System.Diagnostics.Debug.WriteLine(
            $"[ExcelSyncEngine] ExportRegion thất bại: " +
            $"sheet='{mapping.WorkSheet}', region='{mapping.Region ?? "(null)"}'");
        throw new InvalidOperationException(
            $"Không thể xuất hình từ Excel.\n\nSheet: {mapping.WorkSheet}\nRegion: {mapping.Region ?? "(PrintArea/UsedRange)"}\n\nNguyên nhân thường gặp: vùng dữ liệu rỗng, sheet/protected sheet không đọc được Print Area, hoặc pdfium.dll chưa được deploy đúng.");
    }
} // Dispose() gọi → Excel đóng ngay
```

After (`D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\ExcelSyncEngine.cs:161-180`):

```csharp
using (var svc = new SpreadsheetImageExportService())
{
    if (!svc.OpenFile(mapping.FilePath))
    {
        System.Diagnostics.Debug.WriteLine(
            $"[ExcelSyncEngine] Không mở được file: '{mapping.FilePath}'");
        throw new InvalidOperationException(
            $"Không mở được file Excel:\n{mapping.FilePath}\n\nFile có thể không tồn tại, đang bị khóa, hoặc Excel không đọc được file này.");
    }

    // mapping.Region = null → ExportRegion tự fallback PrintArea → UsedRange
    if (!svc.ExportRegion(mapping.WorkSheet, mapping.Region, tempPng))
    {
        System.Diagnostics.Debug.WriteLine(
            $"[ExcelSyncEngine] ExportRegion thất bại: " +
            $"sheet='{mapping.WorkSheet}', region='{mapping.Region ?? "(null)"}'");
        throw new InvalidOperationException(
            $"Không thể xuất hình từ Excel.\n\nSheet: {mapping.WorkSheet}\nRegion: {mapping.Region ?? "(PrintArea/UsedRange)"}\n\nNguyên nhân thường gặp: vùng dữ liệu rỗng, sheet/protected sheet không đọc được Print Area, hoặc pdfium.dll chưa được deploy đúng.");
    }
} // Dispose() gọi → phiên export spreadsheet đóng ngay
```

### Grep output

```text
grep -n "ExcelInteropService" ArcTool.Core/Services/ExcelSyncEngine.cs
No matches found
```

## Decision

`ExcelSyncEngine.cs` is now surgically rewired to the coordinator with the same call sequence, same exception types, same user-facing Vietnamese messages, same debug logging, and the same `using`-scoped session lifetime; the only behavioral change is the provider indirection now flowing through `SpreadsheetImageExportService`.

## Open questions

None.

## Handoff for downstream tasks

- `T3.7` can treat `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\Services\ExcelSyncEngine.cs` as migrated: this file no longer references `ExcelInteropService` and should compile against `ArcTool.Core.Services.Excel.SpreadsheetImageExportService`.
- `T4.1` can verify R11 export-failure parity against the unchanged throws/logging in `ExcelSyncEngine.cs:163-178`; this task intentionally did not alter the failure messages or exception type.
