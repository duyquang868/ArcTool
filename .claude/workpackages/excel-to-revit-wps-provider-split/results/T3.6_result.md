# T3.6 — Rewire `ExcelToRevitWindow.xaml.cs` to the coordinator

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/01_SHARED_CONTRACT.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T3.6.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.3_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.4_result.md`; `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs`; `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:1-15`; `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:380-519`
- write_scope_touched: `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.6_result.md`

## Findings
1. Rewired both authorized construction sites from the deleted legacy type to the coordinator and left all surrounding control flow intact.
   - `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:424` now constructs `SpreadsheetImageExportService` inside `LoadLookupData(...)`.
   - `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:469` now constructs `SpreadsheetImageExportService` inside `LoadRegionOptionsForRow(...)`.

2. The `_suppressRowEvents` discipline is unchanged.
   - Guard still starts at `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:413`.
   - `try`/`finally` shape is unchanged.
   - Unconditional reset still occurs at `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:452`.
   - The `using` block for the first call site remains inside the guarded span exactly where it was before; no statements were moved into or out of that span.
   - No `await`, dispatcher hop, dialog, or throw was introduced.

3. Failure behavior remained identical at both sites.
   - Both methods still short-circuit on `!excelService.OpenFile(...)` and return silently, preserving the existing empty-list-on-failure behavior required by contract R11.
   - No `TaskDialog`, `MessageBox`, or exception path was added.

4. User-visible selection behavior around row reload is unchanged.
   - In `LoadLookupData(...)`, the sequence after `GetSheetNames()` remains the same: populate sheet names, compute `shouldSelectFirst`, possibly restore/select first sheet, then resolve named ranges for the selected sheet.
   - In `LoadRegionOptionsForRow(...)`, the region-dropdown refresh flow remains the same: default `PrintArea`, silent open-fail return, named-range enumeration for the current `row.WorkSheet`, then option rebuild and selection sync.

5. Using directives were updated minimally.
   - Added `using ArcTool.Core.Services.Excel;` at `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:13` so the new coordinator type resolves.
   - Kept existing aliases unchanged; no new ambiguity was introduced.

6. Acceptance grep passed.
   - `grep` equivalent result for `ExcelInteropService` in `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs`: no matches.

7. Before/after snippets for the two authorized blocks:

   Call site 1 before (`ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:423`):
   ```csharp
   using (var excelService = new ExcelInteropService())
   ```
   Call site 1 after (`ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:424`):
   ```csharp
   using (var excelService = new SpreadsheetImageExportService())
   ```

   Call site 2 before (`ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:468`):
   ```csharp
   using (var excelService = new ExcelInteropService())
   ```
   Call site 2 after (`ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs:469`):
   ```csharp
   using (var excelService = new SpreadsheetImageExportService())
   ```

   Grep output after edit:
   ```text
   ExcelInteropService: no matches found
   ```

## Decision
`ExcelToRevitWindow.xaml.cs` is now surgically rewired to `SpreadsheetImageExportService` at the only two authorized UI call sites, with `_suppressRowEvents` span unchanged, silent empty-list failure behavior preserved, and no edits outside the using directives plus those two construction lines.

## Open questions
None.

## Handoff for downstream tasks
- UI interactions affected by this task for `EV-3`: initial sheet dropdown load during row load/reload, region dropdown load after selected worksheet changes, and row reload path that restores or defaults the selected worksheet before repopulating region options.
- Static verification passed for this file: no remaining `ExcelInteropService` reference in `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs`.
- Relevant files: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\UI\ExcelToRevitWindow.xaml.cs`, `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\excel-to-revit-wps-provider-split\results\T3.6_result.md`
