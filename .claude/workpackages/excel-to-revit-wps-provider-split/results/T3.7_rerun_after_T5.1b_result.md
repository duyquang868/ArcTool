# T3.7 — Build gate plus static isolation audit (post-T5.1b rerun)

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T3.7.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T5.1b_result.md`; `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs`; `ArcTool.Core/Services/Excel/PdfRasterImageService.cs`; `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs`; `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`; `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs`
- write_scope_touched: `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.7_rerun_after_T5.1b_result.md`

## Findings
This is the post-T5.1b rerun triggered after the `WpsWorkbookPdfExporter.cs` patch for the WPS `Workbooks.Open` defect.

### Build
Command:
```bash
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" ArcTool.Core/ArcTool.Core.csproj -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo
```
Verbatim output:
```text
  ArcTool.Core -> D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll
```
Exit code: `0`

Warnings/errors scan:
```text
(no warning or error lines)
```
Result: build clean; no new errors; no warnings attributable to any package-created or package-edited file.

### Audit check 1 — No Interop or `Xl*` enum in the WPS file (R9)
Command:
```bash
grep -nE "Microsoft\.Office\.Interop|XlFixedFormat|XlPaperSize|Xl[A-Z][A-Za-z]*\.|Excel\.Application" ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs
```
Verbatim output:
```text

```
Result: matched expectation.

### Audit check 2 — No WPS or late binding in the MS file (R8)
Command:
```bash
grep -nE "GetTypeFromProgID|InvokeMember|KET\.|ET\.Application|Kingsoft|KWPS|WPS\.Application" ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs
```
Verbatim output:
```text

```
Result: matched expectation.

### Audit check 3 — Only the coordinator may name both branches (R8)
Per-file scan command pattern used:
```bash
grep -nE 'Excel\.Application|Microsoft\.Office\.Interop|Xl[A-Z][A-Za-z]*\.|KET\.|ET\.Application|Kingsoft|KWPS|WPS\.Application|GetTypeFromProgID|InvokeMember' <file>
```
Verbatim output:
```text
CHECK3: ISpreadsheetPdfExporter.cs
17:        /// <summary>WPS Spreadsheet (Kingsoft WPS Office), accessed through late-bound COM only.</summary>
CHECK3: PdfRasterImageService.cs
CHECK3: MsExcelWorkbookPdfExporter.cs
4:using Microsoft.Office.Interop.Excel;
10:    /// (<c>Microsoft.Office.Interop.Excel</c>, embedded interop types via the project's
25:        private Microsoft.Office.Interop.Excel.Application _excelApp;
42:                _excelApp = new Microsoft.Office.Interop.Excel.Application
115:                        Microsoft.Office.Interop.Excel.Range r = namedRange.RefersToRange;
180:            Microsoft.Office.Interop.Excel.Range targetRange = null;
252:        private bool ExportRangeToPdf(Worksheet ws, Microsoft.Office.Interop.Excel.Range range, string outputPdfPath)
267:                try { pageSetup.PaperSize = XlPaperSize.xlPaperEsheet; }
270:                    try { pageSetup.PaperSize = XlPaperSize.xlPaperA3; }
275:                    XlFixedFormatType.xlTypePDF,
277:                    XlFixedFormatQuality.xlQualityStandard,
CHECK3: WpsWorkbookPdfExporter.cs
13:    /// through <see cref="Type.GetTypeFromProgID(string, bool)"/> +
14:    /// <see cref="Activator.CreateInstance(Type)"/> + <see cref="Type.InvokeMember"/>. It must never
35:        // Order matters: KET.Application is the only candidate confirmed to resolve on the
39:        private const string ProgIdKet         = "KET.Application";
40:        private const string ProgIdEt          = "ET.Application";
41:        private const string ProgIdKingsoftEt  = "Kingsoft.ET.Application";
43:        private static readonly string[] ProgIdCandidates = { ProgIdKet, ProgIdEt, ProgIdKingsoftEt };
69:            => target.GetType().InvokeMember(name, BindingFlags.GetProperty | PublicInstance, null, target, args);
72:            => target.GetType().InvokeMember(name, BindingFlags.SetProperty | PublicInstance, null, target, new object[] { value });
75:            => target.GetType().InvokeMember(name, BindingFlags.InvokeMethod | PublicInstance, null, target, args);
131:                try { t = Type.GetTypeFromProgID(progId, throwOnError: false); }
CHECK3: SpreadsheetImageExportService.cs
9:        private const string MsExcelProgId = "Excel.Application";
13:            "KET.Application",
14:            "ET.Application",
15:            "Kingsoft.ET.Application"
35:                    "SpreadsheetImageExportService.OpenFile: no spreadsheet engine detected. Microsoft Excel ProgID 'Excel.Application' not found; WPS Spreadsheet ProgIDs 'KET.Application', 'ET.Application', and 'Kingsoft.ET.Application' not found.");
136:            if (Type.GetTypeFromProgID(MsExcelProgId, false) != null)
145:                if (Type.GetTypeFromProgID(progId, false) != null)
```
Per-file result table:

| File | MS marker present | WPS marker present | Both present? | Expected |
|---|---:|---:|---:|---|
| `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs` | No | Yes | No | Yes |
| `ArcTool.Core/Services/Excel/PdfRasterImageService.cs` | No | No | No | Yes |
| `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs` | Yes | No | No | Yes |
| `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs` | No | Yes | No | Yes |
| `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs` | Yes | Yes | Yes | Allowed coordinator-only |

Result: matched expectation.

### Audit check 4 — Zero live references to the deleted type
Command:
```bash
grep -rn "ExcelInteropService" --include=*.cs --include=*.csproj --include=*.xaml . | grep -v "/.claude/worktrees/"
```
Verbatim output:
```text

```
Result: stronger than expected; no live references found at all, including outside `_backup`.

### Audit check 5 — No COM in the raster service
Command:
```bash
grep -nE "Microsoft\.Office\.Interop|GetTypeFromProgID|InvokeMember|Worksheet|Workbook" ArcTool.Core/Services/Excel/PdfRasterImageService.cs
```
Verbatim output:
```text

```
Result: matched expectation.

### Audit check 6 — The `COMReference` survived
Command:
```bash
grep -n "Microsoft.Office.Interop.Excel" ArcTool.Core/ArcTool.Core.csproj
```
Verbatim output:
```text
20:    <COMReference Include="Microsoft.Office.Interop.Excel">
```
Result: matched expectation.

### Audit check 7 — The csproj was not modified by this package
Command:
```bash
git diff --stat -- ArcTool.Core/ArcTool.Core.csproj
```
Verbatim output:
```text

```
Result: matched expectation.

### Audit check 8 — Backup exists (R12)
Command:
```bash
ls -la ArcTool.Core/Services/_backup/
```
Verbatim output:
```text
total 36
drwxr-xr-x 1 ADMIN 197121     0 Aug  9 09:01 .
drwxr-xr-x 1 ADMIN 197121     0 Aug  9 09:03 ..
-rw-r--r-- 1 ADMIN 197121 23198 Aug  9 09:01 ExcelInteropService.cs.bak
```
Result: matched expectation.

### Audit check 9 — Parity constants intact
Quoted constants from `ArcTool.Core/Services/Excel/PdfRasterImageService.cs`:
- `new RenderOptions { Dpi = 300, WithAnnotations = false }` at `PdfRasterImageService.cs:69`
- `const byte WhiteThreshold = 240;` at `PdfRasterImageService.cs:78`

Quoted `ExportAsFixedFormat` argument list from `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs:274-282`:
```text
ws.ExportAsFixedFormat(
    XlFixedFormatType.xlTypePDF,
    outputPdfPath,
    XlFixedFormatQuality.xlQualityStandard,
    false,
    false,
    1,
    1,
    false);
```
Result: matched expectation exactly (`xlTypePDF, path, xlQualityStandard, false, false, 1, 1, false`).

## Decision
Post-T5.1b rerun PASS: the project builds cleanly and all nine static isolation/build invariants still hold after the `WpsWorkbookPdfExporter.Open` fallback patch.

## Open questions
- Runtime EV-2 still must confirm whether one of the new WPS `Workbooks.Open` call shapes succeeds on the operator machine.
- Runtime EV-2 still must confirm downstream WPS member compatibility after workbook open succeeds.

## Handoff for downstream tasks
- `T5.3` may cite this rerun as the authoritative post-T5.1b build/static-isolation PASS.
- Parity constants remain intact: raster stage still uses `Dpi = 300` and `WhiteThreshold = 240`; MS provider `ExportAsFixedFormat` signature remains unchanged.