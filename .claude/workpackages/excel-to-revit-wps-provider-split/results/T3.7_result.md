# T3.7 — Build gate plus static isolation audit

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T3.7.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.1_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.2_result.md`; prior `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.7_result.md`
- write_scope_touched: `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.7_result.md`

## Findings

This file supersedes the earlier 2026-08-09 BLOCKED verdicts. Re-gate run executed after the updated owner repairs landed: T3.1 fully qualified all ambiguous `Application`/`Range` type sites in `MsExcelWorkbookPdfExporter.cs`; T3.2 renamed the four WPS local `Xl*` constants to provider-neutral names with numeric values unchanged; T2.2 remained clean.

Verdict on first line required by task: PASS

### Build

Command:
```bash
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" ArcTool.Core/ArcTool.Core.csproj -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo
```

Authoritative exit code: `0`

Errors:
```text
(none)
```

Warnings:
```text
(none)
```

Build output captured for the authoritative pass:
```text
  ArcTool.Core -> D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\ArcTool.Core\bin\x64\Debug\net8.0-windows\ArcTool.Core.dll
EXITCODE:0
```

Note: an earlier same-turn build invocation emitted a single nullable warning in `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs(77,32)` and still exited 0. That file is unrelated to this package and not one of the five new `Services/Excel/` files or the two rewired call-site files. The final authoritative gate rerun above, executed after the owner-fix confirmation and used for this result, emitted no warnings.

### Static isolation audit

Note: every repo-tree grep below explicitly excludes stale subagent worktrees with `| grep -v "/.claude/worktrees/"` where the task required repo-wide grep, because those checkouts are throwaway copies and not live source.

#### 1. No Interop or `Xl*` enum in the WPS file (R9)
Command:
```bash
grep -nE "Microsoft\.Office\.Interop|XlFixedFormat|XlPaperSize|Xl[A-Z][A-Za-z]*\.|Excel\.Application" ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs
```
Output:
```text
```
Result: matches expectation.

#### 2. No WPS or late binding in the MS file (R8)
Command:
```bash
grep -nE "GetTypeFromProgID|InvokeMember|KET\.|ET\.Application|Kingsoft|KWPS|WPS\.Application" ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs
```
Output:
```text
```
Result: matches expectation.

#### 3. No file except the coordinator names both branches (R8)
Command evidence:
```bash
for f in ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs ArcTool.Core/Services/Excel/PdfRasterImageService.cs ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs; do echo "=== $f ==="; echo "-- MS markers --"; grep -nE "Microsoft\.Office\.Interop|Excel\.Application|Xl[A-Z][A-Za-z]*|ExcelInteropService" "$f"; echo "-- WPS markers --"; grep -nE "GetTypeFromProgID|InvokeMember|KET\.|ET\.Application|Kingsoft|KWPS|WPS\.Application|SpreadsheetEngine\.Wps" "$f"; done
```
Verbatim output:
```text
=== ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs ===
-- MS markers --
-- WPS markers --
17:        /// <summary>WPS Spreadsheet (Kingsoft WPS Office), accessed through late-bound COM only.</summary>
=== ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs ===
-- MS markers --
4:using Microsoft.Office.Interop.Excel;
10:    /// (<c>Microsoft.Office.Interop.Excel</c>, embedded interop types via the project's
25:        private Microsoft.Office.Interop.Excel.Application _excelApp;
42:                _excelApp = new Microsoft.Office.Interop.Excel.Application
115:                        Microsoft.Office.Interop.Excel.Range r = namedRange.RefersToRange;
180:            Microsoft.Office.Interop.Excel.Range targetRange = null;
252:        private bool ExportRangeToPdf(Worksheet ws, Microsoft.Office.Interop.Excel.Range range, string outputPdfPath)
267:                try { pageSetup.PaperSize = XlPaperSize.xlPaperEsheet; }
270:                    try { pageSetup.PaperSize = XlPaperSize.xlPaperA3; }
275:                    XlFixedFormatType.xlTypePDF,
277:                    XlFixedFormatQuality.xlQualityStandard,
-- WPS markers --
=== ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs ===
-- MS markers --
-- WPS markers --
13:    /// through <see cref="Type.GetTypeFromProgID(string, bool)"/> +
14:    /// <see cref="Activator.CreateInstance(Type)"/> + <see cref="Type.InvokeMember"/>. It must never
35:        // Order matters: KET.Application is the only candidate confirmed to resolve on the
39:        private const string ProgIdKet         = "KET.Application";
40:        private const string ProgIdEt          = "ET.Application";
41:        private const string ProgIdKingsoftEt  = "Kingsoft.ET.Application";
43:        private static readonly string[] ProgIdCandidates = { ProgIdKet, ProgIdEt, ProgIdKingsoftEt };
65:        public SpreadsheetEngine Engine => SpreadsheetEngine.Wps;
69:            => target.GetType().InvokeMember(name, BindingFlags.GetProperty | PublicInstance, null, target, args);
72:            => target.GetType().InvokeMember(name, BindingFlags.SetProperty | PublicInstance, null, target, new object[] { value });
75:            => target.GetType().InvokeMember(name, BindingFlags.InvokeMethod | PublicInstance, null, target, args);
90:                try { t = Type.GetTypeFromProgID(progId, throwOnError: false); }
=== ArcTool.Core/Services/Excel/PdfRasterImageService.cs ===
-- MS markers --
-- WPS markers --
=== ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs ===
-- MS markers --
9:        private const string MsExcelProgId = "Excel.Application";
35:                    "SpreadsheetImageExportService.OpenFile: no spreadsheet engine detected. Microsoft Excel ProgID 'Excel.Application' not found; WPS Spreadsheet ProgIDs 'KET.Application', 'ET.Application', and 'Kingsoft.ET.Application' not found."
-- WPS markers --
13:            "KET.Application",
14:            "ET.Application",
15:            "Kingsoft.ET.Application"
35:                    "SpreadsheetImageExportService.OpenFile: no spreadsheet engine detected. Microsoft Excel ProgID 'Excel.Application' not found; WPS Spreadsheet ProgIDs 'KET.Application', 'ET.Application', and 'Kingsoft.ET.Application' not found."
136:            if (Type.GetTypeFromProgID(MsExcelProgId, false) != null)
145:                if (Type.GetTypeFromProgID(progId, false) != null)
```
Per-file result table:

| File | MS marker present | WPS marker present | Verdict |
|---|---:|---:|---|
| `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs` | no | yes | ok |
| `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs` | yes | no | ok |
| `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs` | no | yes | ok |
| `ArcTool.Core/Services/Excel/PdfRasterImageService.cs` | no | no | ok |
| `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs` | yes | yes | allowed coordinator |

Result: matches expectation.

#### 4. Zero live references to the deleted type
Command:
```bash
grep -rn "ExcelInteropService" --include=*.cs --include=*.csproj --include=*.xaml . | grep -v "/.claude/worktrees/"
```
Output:
```text
```
Result: matches expectation.

#### 5. No COM in the raster service
Command:
```bash
grep -nE "Microsoft\.Office\.Interop|GetTypeFromProgID|InvokeMember|Worksheet|Workbook" ArcTool.Core/Services/Excel/PdfRasterImageService.cs
```
Output:
```text
```
Result: matches expectation.

#### 6. The `COMReference` survived
Command:
```bash
grep -n "Microsoft.Office.Interop.Excel" ArcTool.Core/ArcTool.Core.csproj
```
Output:
```text
20:	  <COMReference Include="Microsoft.Office.Interop.Excel">
```
Result: matches expectation.

#### 7. The csproj was not modified by this package
Command:
```bash
git diff --stat -- ArcTool.Core/ArcTool.Core.csproj
```
Output:
```text
```
Result: matches expectation.

#### 8. Backup exists (R12)
Command:
```bash
ls -la ArcTool.Core/Services/_backup/
```
Output:
```text
total 36
drwxr-xr-x 1 ADMIN 197121     0 Aug  9 09:01 .
drwxr-xr-x 1 ADMIN 197121     0 Aug  9 09:03 ..
-rw-r--r-- 1 ADMIN 197121 23198 Aug  9 09:01 ExcelInteropService.cs.bak
```
Result: matches expectation.

#### 9. Parity constants intact
Command evidence:
```bash
echo "-- raster constants --" && grep -nE "Dpi\s*=|WhiteThreshold" ArcTool.Core/Services/Excel/PdfRasterImageService.cs && echo "-- MS ExportAsFixedFormat call --" && grep -n "ExportAsFixedFormat" -A 8 ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs
```
Verbatim output:
```text
-- raster constants --
69:                    new RenderOptions { Dpi = 300, WithAnnotations = false });
78:                const byte WhiteThreshold = 240;
89:                        if (IsNonWhitePixel(bitmap.GetPixel(x, top), WhiteThreshold))
109:                        if (IsNonWhitePixel(bitmap.GetPixel(x, bottom), WhiteThreshold))
126:                        if (IsNonWhitePixel(bitmap.GetPixel(left, y), WhiteThreshold))
143:                        if (IsNonWhitePixel(bitmap.GetPixel(right, y), WhiteThreshold))
-- MS ExportAsFixedFormat call --
155:        /// 512-582) plus the PageSetup normalization and <c>ExportAsFixedFormat</c> call from
156-        /// the legacy single-provider implementation's ExportRangeInternal member (old file lines 201-229). The region
157-        /// resolution order NamedRange → PrintArea → UsedRange is unchanged, as is every PageSetup
158:        /// field and every <c>ExportAsFixedFormat</c> positional argument.
159-        /// <para>
160-        /// Two deliberate, interface-mandated deviations from the old <c>ExportRegion</c>, both
161-        /// already locked by <c>results/T1.1_result.md</c> and by this interface's own documented
162-        /// contract — not new decisions made in this file:
163-        /// </para>
164-        /// <list type="number">
165-        /// <item>The legacy ExportRegion member re-threw on any exception (old line 574) so the caller
166-        /// could show a dialog. This method never throws; it swallows and returns <c>false</c>,
--
274:                ws.ExportAsFixedFormat(
275-                    XlFixedFormatType.xlTypePDF,
276-                    outputPdfPath,
277-                    XlFixedFormatQuality.xlQualityStandard,
278-                    false,
279-                    false,
280-                    1,
281-                    1,
282-                    false);
```
Quoted required parity evidence:
- `ArcTool.Core/Services/Excel/PdfRasterImageService.cs:69` → `new RenderOptions { Dpi = 300, WithAnnotations = false });`
- `ArcTool.Core/Services/Excel/PdfRasterImageService.cs:78` → `const byte WhiteThreshold = 240;`
- `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs:274-282` → `ws.ExportAsFixedFormat(XlFixedFormatType.xlTypePDF, outputPdfPath, XlFixedFormatQuality.xlQualityStandard, false, false, 1, 1, false);`

Result: parity constants and export-call shape are intact in current source text.

## Decision

PASS. The authoritative re-gate build exits `0` with no errors and no warnings, and all nine static isolation audits match the literal T3.7 expectations. Phase 4 may start.

## Open questions

None.

## Handoff for downstream tasks

- T4.1 may rely on this gate as cleared and may cite the confirmed parity evidence at `PdfRasterImageService.cs:69`, `PdfRasterImageService.cs:78`, and `MsExcelWorkbookPdfExporter.cs:274-282`.
- T5.3 may cite this file as the authoritative Phase 3 gate result.
- Relevant file: `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\excel-to-revit-wps-provider-split\results\T3.7_result.md`
