# T4.1 — MS Excel behavior-parity review against contract R11

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/01_SHARED_CONTRACT.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T4.1.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T2.1_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.7_result.md`; `ArcTool.Core/Services/_backup/ExcelInteropService.cs.bak`; `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs`; `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs`; `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`; `ArcTool.Core/Services/Excel/PdfRasterImageService.cs`; `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs`; `ArcTool.Core/Services/ExcelSyncEngine.cs` lines 140-200; `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs` lines 380-519; codebase-memory graph inbound traces for `GetActiveSheetName` and `ExportPrintAreaAsHighResImage`
- write_scope_touched: `.claude/workpackages/excel-to-revit-wps-provider-split/results/T4.1_result.md`

## Findings

Verdict on first line required by task: PASS

| # | R11 item | Old `file:line` | New `file:line` | Verdict | Evidence |
|---|---|---|---|---|---|
| 1 | PageSetup normalization | `_backup/ExcelInteropService.cs.bak:201-209` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:256-264` | RELOCATED | Old: `pageSetup.PrintArea = range.Address[false, false]; ... Zoom = false; FitToPagesWide = 1; FitToPagesTall = 1; TopMargin = 0; BottomMargin = 0; LeftMargin = 0; RightMargin = 0;` New: same statements in same order. |
| 2 | PaperSize fallback chain | `_backup/ExcelInteropService.cs.bak:210-216` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:266-272` | RELOCATED | Old: `try { pageSetup.PaperSize = XlPaperSize.xlPaperEsheet; } catch { try { pageSetup.PaperSize = XlPaperSize.xlPaperA3; } catch { /* Giữ paper size hiện tại — FitToPages vẫn handle được */ } }` New: same two-try silent chain with `xlPaperEsheet` then `xlPaperA3`. |
| 3 | `ExportAsFixedFormat` arguments | `_backup/ExcelInteropService.cs.bak:221-229` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:274-282` | RELOCATED | Old: `ws.ExportAsFixedFormat(XlFixedFormatType.xlTypePDF, tempPdf, XlFixedFormatQuality.xlQualityStandard, false, false, 1, 1, false);` New: `ws.ExportAsFixedFormat(XlFixedFormatType.xlTypePDF, outputPdfPath, XlFixedFormatQuality.xlQualityStandard, false, false, 1, 1, false);` Only path source moved to caller; positional argument list stays identical. |
| 4 | Render DPI and `SavePng` call | `_backup/ExcelInteropService.cs.bak:238-245` | `Services/Excel/PdfRasterImageService.cs:62-69` | RELOCATED | Old: `Conversion.SavePng(outputPath, pdfStream, 0, false, null, new RenderOptions { Dpi = 300, WithAnnotations = false });` New: `Conversion.SavePng(outputPngPath, pdfStream, 0, false, null, new RenderOptions { Dpi = 300, WithAnnotations = false });` |
| 5 | Crop threshold and pixel test | `_backup/ExcelInteropService.cs.bak:193-197, 254-344` | `Services/Excel/PdfRasterImageService.cs:78-187` | RELOCATED | Old: `const byte WhiteThreshold = 240;` and `color.Alpha < threshold || color.Red < threshold || color.Green < threshold || color.Blue < threshold;` with four scans top/bottom/left/right. New: same constant, same predicate, same four scan loops in same order. |
| 6 | PNG encode | `_backup/ExcelInteropService.cs.bak:341-347` | `Services/Excel/PdfRasterImageService.cs:165-171` | RELOCATED | Old: `using var data = image.Encode(SKEncodedImageFormat.Png, 100);` then `data.SaveTo(outputStream);` New: same encode format and quality, same save flow. |
| 7 | Region resolution order | `_backup/ExcelInteropService.cs.bak:531-567` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:192-230` | RELOCATED | Old: NamedRange `ws.Range[regionName]` → PrintArea `string printArea = ws.PageSetup.PrintArea; if (!string.IsNullOrEmpty(printArea)) targetRange = ws.Range[printArea];` → `targetRange = ws.UsedRange;` New: same fallback order and same branch conditions. |
| 8 | Named-range scope filtering | `_backup/ExcelInteropService.cs.bak:452-490` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:108-147` | RELOCATED | Old: `allNames = _workbook.Names; foreach (Name namedRange in allNames) { ... Range r = namedRange.RefersToRange; if (r?.Worksheet?.Name == sheetName) { result.Add(namedRange.Name); } ... }` New: same wrapper, same `RefersToRange`, same sheet test, same add. |
| 9 | COM release order in dispose | `_backup/ExcelInteropService.cs.bak:361-388` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:300-327` | RELOCATED | Old: `_workbook.Close(false)` → `ReleaseObject(_workbook)` → `_excelApp.Quit()` → `ReleaseObject(_excelApp)` → `GC.Collect(); GC.WaitForPendingFinalizers();` New: same sequence and same helper body `if (obj != null) Marshal.ReleaseComObject(obj);`. |
| 10 | Open-failure policy | `_backup/ExcelInteropService.cs.bak:32-48` + `Services/ExcelSyncEngine.cs:161-169` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:38-54` + `Services/Excel/SpreadsheetImageExportService.cs:43-48` + `Services/ExcelSyncEngine.cs:161-169` | RELOCATED | Old provider: `catch { return false; }`; old caller threw `InvalidOperationException("Không mở được file Excel:\n{mapping.FilePath}...`)`. New MS provider still `catch { return false; }`; coordinator preserves boolean contract: `if (_provider.Open(filePath)) return true; ... return false;`; caller still throws the same Vietnamese message at `ExcelSyncEngine.cs:167-168`. |
| 11 | Export-failure policy | `_backup/ExcelInteropService.cs.bak:571-574` + `Services/ExcelSyncEngine.cs:171-179` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:232-235, 286-289` + `Services/Excel/SpreadsheetImageExportService.cs:76-90` + `Services/ExcelSyncEngine.cs:171-179` | RELOCATED | Old provider rethrew: `catch (Exception ex) { ... throw; }`; caller threw `InvalidOperationException("Không thể xuất hình từ Excel...`)` when `ExportRegion(...)` returned false. New split changes the throw site but preserves observable caller behavior: MS provider returns `false` on export/PDF failure, coordinator returns `false` on provider failure or raster false, and `ExcelSyncEngine.cs:177-178` still throws the same Vietnamese `InvalidOperationException`. |
| 12 | UI enumeration failure policy | `_backup/ExcelInteropService.cs.bak:402-430, 443-494` | `Services/Excel/MsExcelWorkbookPdfExporter.cs:62-149` + `UI/ExcelToRevitWindow.xaml.cs:424-449, 469-480` | SAME | Old methods swallowed exceptions and returned empty lists. New MS provider does the same (`catch ...` only debug-log; returns list). UI still does `if (!excelService.OpenFile(row.FilePath)) return;`, `List<string> sheetNames = excelService.GetSheetNames();`, `List<string> namedRanges = ... ? new List<string>() : excelService.GetNamedRanges(...)`; no dialog, no throw, dropdowns simply receive empty lists. |
| 13 | Temp-PDF lifetime | `_backup/ExcelInteropService.cs.bak:218-229, 355-357` | `Services/Excel/SpreadsheetImageExportService.cs:70-104` + `Services/Excel/PdfRasterImageService.cs:17-21, 41-42` + `Services/Excel/MsExcelWorkbookPdfExporter.cs:169-171` | RELOCATED | Old created temp path internally then `finally { try { if (!string.IsNullOrEmpty(tempPdf) && File.Exists(tempPdf)) File.Delete(tempPdf); } catch { } }`. New coordinator creates temp path once, provider writes to caller-supplied path and `never deletes it`, raster service `never deletes the input PDF`, and coordinator `finally` does `if (File.Exists(tempPdfPath)) File.Delete(tempPdfPath);`. This `finally` runs after both success return and failure return; delete exceptions are swallowed in an inner `try/catch`, so they cannot mask the real export/raster outcome. |
| 14 | Native library candidate order | `_backup/ExcelInteropService.cs.bak:111-127, 141-147, 166-172` | `Services/Excel/PdfRasterImageService.cs:200-215, 230-236, 255-261` | RELOCATED | Old candidates: `Path.Combine(assemblyDir, libraryFileName)`, `Path.Combine(assemblyDir, "native", libraryFileName)`, `Path.Combine(assemblyDir, "runtimes", runtimeFolder, "native", libraryFileName)`; same foreach usage for `pdfium.dll` and `libSkiaSharp.dll`. New code is identical except `typeof(PdfRasterImageService).Assembly.Location` anchor. |
| 15 | `_suppressRowEvents` span | `UI/ExcelToRevitWindow.xaml.cs:414-454` | `UI/ExcelToRevitWindow.xaml.cs:414-454` | SAME | Current window code still wraps `LoadLookupData` with `_suppressRowEvents = true;` before any row mutation and `finally { _suppressRowEvents = false; }` after it. Phase-4 target area is unchanged. |
| 16 | Runtime folder anchor | `_backup/ExcelInteropService.cs.bak:100-127` | `Services/Excel/PdfRasterImageService.cs:189-215` | RELOCATED | Old: `string assemblyDir = Path.GetDirectoryName(typeof(ExcelInteropService).Assembly.Location);` New: `string assemblyDir = Path.GetDirectoryName(typeof(PdfRasterImageService).Assembly.Location);` Both types live in `ArcTool.Core.dll`, so `Assembly.Location` resolves to the same assembly path. The remainder of `GetRuntimeFolder` and `GetNativeLibraryCandidates` is identical. |

### Dropped legacy members: safe absence check

- `GetActiveSheetName` exists only in backup at `_backup/ExcelInteropService.cs.bak:54-66`; codebase-memory inbound trace returned `callers: []`.
- `ExportPrintAreaAsHighResImage` exists only in backup at `_backup/ExcelInteropService.cs.bak:68-98`; codebase-memory inbound trace returned `callers: []`.
- Repo grep over `ArcTool.Core` found those names only in the backup file, so their absence from the five new files is safe and non-observable.

## Decision

PASS. Every audited R11 item is either textually identical in place or relocated into the MS provider / shared raster service / coordinator without an observable behavior change on an MS Excel machine. The only structural shift is the temp-PDF owner, but the coordinator’s `finally` preserves create/delete lifetime and cannot mask a real export failure.

## Open questions

None.

## Handoff for downstream tasks

- `T4.2` can treat the MS Excel static baseline as preserved and focus EV-3 only on runtime confirmation, not on suspected static regressions.
- No `CONCERN` rows were found; if operator EV-3 fails, the defect is likely in runtime environment or a cross-file behavior outside R11 rather than in the audited static parity surface.
