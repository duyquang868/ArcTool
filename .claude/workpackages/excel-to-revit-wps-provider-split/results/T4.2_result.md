# T4.2 — Write the operator runbooks for EV-1, EV-2, EV-3

- status: PASS
- date: 2026-08-09
- inputs_read: `.claude/workpackages/excel-to-revit-wps-provider-split/01_SHARED_CONTRACT.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/05_RESULT_SCHEMA.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/tasks/T4.2.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/04_EVIDENCE_QUEUE.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T1.2_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.2_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.3_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/results/T3.6_result.md`
- write_scope_touched: `.claude/workpackages/excel-to-revit-wps-provider-split/results/T4.2_result.md`; `.claude/workpackages/excel-to-revit-wps-provider-split/04_EVIDENCE_QUEUE.md`

## Findings
- Wrote three operator-facing runbooks directly into `04_EVIDENCE_QUEUE.md`, replacing the prior `PENDING` stubs for `EV-1`, `EV-2`, and `EV-3` while preserving the file structure and the environment-constraint section unchanged.
- `EV-1` runbook targets the same machine with both engines installed and includes: close-WPS precondition, exact WPS version capture, one copy-paste PowerShell ProgID probe for `KET.Application`, `ET.Application`, `Kingsoft.ET.Application`, and `KWPS.Application`, one copy-paste PowerShell live-member probe for the assumed-member surface from T1.2/T3.2, and explicit orphan-process cleanup evidence via `tasklist`.
- `EV-1` member checklist written into the probe covers the assumptions that downstream analysis needs resolved: `Workbooks`, `Open`, `Worksheets`, `Names`, `RefersToRange`, `Worksheet`, `Range`, `UsedRange`, `Address`, `PageSetup`, `PrintArea`, `Zoom`, `FitToPagesWide`, `FitToPagesTall`, `TopMargin`, `BottomMargin`, `LeftMargin`, `RightMargin`, `PaperSize`, `ExportAsFixedFormat`, `Close`, and `Quit`.
- `EV-2` runbook explicitly uses the forcing mechanism documented by T3.3’s actual behavior: temporarily make the `Excel.Application` ProgID unresolved by unregistering or otherwise disabling Microsoft Excel COM registration for the duration of the run, because T3.3 documented no built-in override switch/log hook. The runbook marks this as scratch-only and BLOCKED if the operator cannot safely do it.
- `EV-2` covers the required WPS path proof sequence: verify the forced-precondition with the same ProgID probe, use a test workbook containing named-range / print-area / UsedRange fallback cases, exercise sheet dropdown, region dropdown, update/import, collect both WPS-produced PDF/PNG artifacts, then restore Excel COM registration and repeat the same export through the MS path for side-by-side fidelity comparison.
- `EV-3` runbook is the non-regression matrix for the MS path and includes the minimum required rows from T4.2 plus the UI interactions confirmed by T3.6: initial sheet dropdown load, region dropdown reload, and row reload/restoration behavior. It also covers missing file, wrong sheet, and locked/open-in-Excel failure paths and requires verbatim Vietnamese exception text capture.
- Step counts written into the runbooks: `EV-1` = 8 steps; `EV-2` = 13 steps; `EV-3` = 13 steps.
- Scratch-file requirement is explicit: `EV-1` is safe on the production machine but should use a non-critical workbook path for member probing; `EV-2` requires a scratch workbook and a safe way to disable/restore Excel COM detection; `EV-3` may run on the production machine for dropdown/load checks but any update/import or failure-path mutation should use a scratch copy of the workbook and a non-production Revit test context.
- Input note: `results/T4.1_result.md` was authorized by the task but absent on disk. The runbook still satisfies the acceptance condition using the surviving authorized inputs (`T1.2`, `T3.2`, `T3.3`, `T3.6`) and the minimum `EV-3` matrix required in `tasks/T4.2.md`, but no extra `CONCERN` rows could be expanded because that source file does not exist.

## Decision
Three complete operator runbooks now exist in `04_EVIDENCE_QUEUE.md` for `EV-1`, `EV-2`, and `EV-3`, with exact recordable outputs, explicit scratch-vs-production guidance, and a documented limitation that `T4.1_result.md` was missing so no additional concern-specific EV-3 rows could be derived.

## Open questions
1. `results/T4.1_result.md` is missing on disk; if it was expected to exist, downstream `T5.2` will need either that file restored or acceptance that EV-3 proceeds without concern-derived extra rows.
2. `EV-2` depends on the operator having a reversible, safe method to make `Excel.Application` temporarily unresolved on this machine; the codebase itself does not expose a force-WPS switch.

## Handoff for downstream tasks
- `T5.1` can now route the user to run `EV-1` first, then `EV-2`, and should expect pasted console tables, WPS/MS PDFs and PNGs, and any verbatim error text.
- `T5.2` can use the `EV-3` matrix as written for MS non-regression, but should note that no `T4.1` concern rows were available to expand because that result file was absent.
