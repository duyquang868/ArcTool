# T2.2 — Author `PdfRasterImageService.cs`

- depends on: `T1.3` (`GO`)
- write_scope: `ArcTool.Core/Services/Excel/PdfRasterImageService.cs`, result → `results/T2.2_result.md`
- model: sonnet

## Objective

Extract the engine-agnostic raster stage — PDF → PNG at 300 DPI → white-margin crop — into its own
service, with **behavior identical** to the current code. This file must contain zero COM, zero
spreadsheet knowledge, and zero Interop.

## Inputs you may read

- `01_SHARED_CONTRACT.md` (this package)
- `ArcTool.Core/Services/ExcelInteropService.cs` — lines 100-177 and 231-359 only. These are the exact
  regions you are porting.
- `results/T1.3_result.md`

Do not read anything else. Do not read `CLAUDE.md`. Do not run Revit, Excel, WPS, or any MCP tool (R1).

## What to port, and how faithfully

Port these, preserving behavior exactly (contract R11):

| Source lines | What it is | Porting note |
|---|---|---|
| 100-109 | `GetRuntimeFolder` | retarget the `typeof(...)` anchor to the new class; the assembly is the same, so the resolved folder must be unchanged |
| 111-127 | `GetNativeLibraryCandidates` | keep the full candidate list and its order: assembly dir, `native/`, `runtimes/win-x64/native/` |
| 129-152 | `EnsurePdfiumLoaded` | keep the load-once guard and the fallback walk for `pdfium.dll` |
| 154-177 | `EnsureSkiaSharpLoaded` | keep the load-once guard and the fallback walk for `libSkiaSharp.dll` |
| 231-~250 | PDF → PNG render | `Conversion.SavePng(outputPath, pdfStream, 0, false, null, new RenderOptions { Dpi = 300, WithAnnotations = false })` — keep every argument, keep 300 DPI |
| ~250-348 | white-margin crop | `const byte WhiteThreshold = 240`; four-directional scan for top / bottom / left / right; `SKRectI`; `bitmap.ExtractSubset`; `SKImage.Encode(SKEncodedImageFormat.Png, 100)`; write to `outputPath`; keep the local `IsNonWhitePixel` predicate logic exactly (`Alpha`, `Red`, `Green`, `Blue` each compared to the threshold) |

Preserve the existing edge-case handling as it stands today: an all-white page, a zero-size crop
rectangle, and a failed render must behave as they do now. If the current code has no guard for one of
these, **do not add one** — parity first; note the gap in your result file instead. Behavior changes,
even improvements, are out of scope for this task.

## Public shape

Expose the minimum useful surface. A single entry point is expected, along the lines of:

```csharp
bool RenderPdfToCroppedPng(string pdfPath, string outputPngPath);
```

Decide static-class versus instance-class and justify it in one sentence in your result file. Keep the
native-loader guards private and idempotent.

**Temp-PDF lifetime is not yours.** This service reads the PDF and writes the PNG; the coordinator
(`T3.3`) creates and deletes the temp PDF. Do not delete the input file, and say so in a doc comment —
the original code deleted it in `ExportRangeInternal`'s `finally` at line 357, so this relocation is
exactly the kind of thing a later reader will misread.

## Constraints

- No `using Microsoft.Office.Interop.Excel`, no `Xl*` enum, no COM, no WPS ProgID. This file must not
  know a spreadsheet exists.
- Do not create, modify, or delete any other file. If you believe `ArcTool.Core.csproj` needs a change,
  return `BLOCKED` — `PDFtoImage 5.2.1` and `SkiaSharp 3.119.2` are already referenced.
- Do not build; `T2.3` owns the build gate.

## Acceptance condition

The file exists; every row of the port table is accounted for; the threshold is `240`; the DPI is
`300`; the candidate-path order is unchanged. Your result file lists, per row, the old `file:line` and
the new member name, plus any parity gap you deliberately left alone.

## What downstream needs from you

`T3.3` calls your entry point. `T4.1` audits your port against R11 line by line — make that audit easy
by giving exact old→new line mappings.
