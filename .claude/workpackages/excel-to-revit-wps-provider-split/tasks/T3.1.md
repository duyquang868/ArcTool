# T3.1 — Author `MsExcelWorkbookPdfExporter.cs`

- depends on: `T2.3` (`PASS`)
- write_scope: `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs`, result → `results/T3.1_result.md`
- model: sonnet

## Objective

Move the MS Excel COM logic out of `ExcelInteropService.cs` into a provider that implements
`ISpreadsheetPdfExporter`, stopping at the PDF. Behavior on an MS Excel machine must be
indistinguishable from today (contract R11).

## Inputs you may read

- `01_SHARED_CONTRACT.md` (this package)
- `results/T1.1_result.md` — the member inventory and release-order rules
- `ArcTool.Core/Services/Excel/ISpreadsheetPdfExporter.cs` — the interface you implement
- `ArcTool.Core/Services/ExcelInteropService.cs` — lines 32-48, 188-230, 361-388, 402-494, 512-582.
  These are your source of truth for behavior.

Do not read anything else. Do not read `CLAUDE.md`. Do not run Revit, Excel, WPS, or any MCP tool (R1) —
do not launch Excel to "check" a member.

## What to port

| Source lines | Behavior | Requirement |
|---|---|---|
| 32-48 | `OpenFile` | `new Application { Visible = false, DisplayAlerts = false }`, then `Workbooks.Open(filePath)`. Keep the catch-all → `false`. `ExcelSyncEngine` converts `false` into a user-facing throw; do not throw here. |
| 402-430 | `GetSheetNames` | keep the enumeration order and the swallow-and-return-empty behavior at 419-422 |
| 443-494 | `GetNamedRanges` | keep the scope filtering logic exactly — which names are attributed to which sheet is load-bearing for the UI dropdown — and the swallow at 483-486 |
| 512-582 | `ExportRegion` region resolution | keep the order NamedRange → PrintArea → UsedRange and every fallback branch; this stays inside the provider |
| 201-229 | PageSetup normalization | `PrintArea = range.Address[false,false]`, `Zoom = false`, `FitToPagesWide = 1`, `FitToPagesTall = 1`, four margins `= 0`, then `PaperSize = xlPaperEsheet` with a nested try → `xlPaperA3` with a nested try → keep current size. Keep both fallbacks and keep them silent. |
| 220-228 | `ExportAsFixedFormat` | keep every positional argument exactly: `xlTypePDF`, path, `xlQualityStandard`, `false`, `false`, `1`, `1`, `false` |
| 361-388 | `Dispose` / `ReleaseObject` | keep the order `_workbook.Close(false)` → release workbook → `_excelApp.Quit()` → release app → `GC.Collect()` + `GC.WaitForPendingFinalizers()` |

**Do not port** `GetActiveSheetName` (54-66) or `ExportPrintAreaAsHighResImage` (68-98) — both are dead
code with zero callers (verified by graph `trace_path`).

**Do not port** the raster stage (231-348) or the native loaders (100-177) — `PdfRasterImageService`
owns them now. Your `ExportRegionToPdf` returns after the PDF exists on disk.

**Temp-PDF ownership moves out.** The old `finally` at line 357 deleted the temp PDF. You neither create
nor delete it: the coordinator supplies `outputPdfPath` and owns its lifetime. Preserve any other
cleanup that ran in that `finally` — read it and decide per statement, then record each decision.

## Constraints

- Keep the `Microsoft.Office.Interop.Excel` usage early-bound; the `COMReference` with
  `EmbedInteropTypes=true` stays as-is.
- **No WPS anything (R8).** No ProgID string, no `Type.GetTypeFromProgID`, no `InvokeMember`, no
  late-binding path, no comment describing the WPS branch. This file must not know WPS exists.
- Only this one file. If `ArcTool.Core.csproj` looks like it needs a change, return `BLOCKED`.
- Do not touch `ExcelInteropService.cs` — `T3.4` deletes it after backup. Both files coexisting and both
  compiling is the expected state at the end of your task.
- Do not build; `T3.7` owns the build gate.

## Acceptance condition

Every row of the port table is implemented; both dead methods are absent; the file implements
`ISpreadsheetPdfExporter` fully. Run and paste in your result file:

```bash
grep -nE "GetTypeFromProgID|InvokeMember|KET\.|ET\.Application|Kingsoft" ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs
```

Expected: no output.

## What downstream needs from you

`T3.3` constructs you. `T4.1` audits your port against R11 — provide an old `file:line` → new member
mapping for every row so that audit is mechanical.
