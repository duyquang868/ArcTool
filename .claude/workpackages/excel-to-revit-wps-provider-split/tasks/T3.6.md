# T3.6 — Rewire `ExcelToRevitWindow.xaml.cs` to the coordinator

- depends on: `T3.4`
- write_scope: `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs`, result → `results/T3.6_result.md`
- model: sonnet

## Objective

Replace the two `new ExcelInteropService()` usages at `:423` and `:468` with the coordinator, without
disturbing the window's event-suppression discipline.

## Inputs you may read

- `01_SHARED_CONTRACT.md` (this package)
- `results/T3.3_result.md` — the coordinator's public surface, quoted verbatim there. Code against it.
- `results/T3.4_result.md` — the confirmed reference list
- `ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs` — lines 380-519 only. Do not read or touch the rest.

Do not read anything else. Do not read `CLAUDE.md`. Do not run Revit, Excel, WPS, or any MCP tool (R1).

## Call site 1 — inside `LoadLookupData(row, defaultToFirstSheet)`, around line 423

Needs, in order: construct (423) → open (425) → `GetSheetNames()` (428) → `GetNamedRanges(sheetName)` (443).

**`_suppressRowEvents` is load-bearing.** The whole block runs inside `_suppressRowEvents = true` with a
`finally { _suppressRowEvents = false; }`. That guard exists because of BUG-P3-01: mutating the row's
`WorkSheet` / `Region` while data-binding events are live re-enters this method. Requirements:
- keep the flag set for exactly the same span, not one statement more or less
- keep the `finally` reset unconditional
- do not move the `using` block in or out of the guarded span
- do not add `await`, `Dispatcher.Invoke`, or any other continuation inside the guarded span

## Call site 2 — inside `LoadRegionOptionsForRow(row)`, around line 468

Needs: construct (468) → open (470) → `GetNamedRanges(row.WorkSheet)` (475). Same discipline.

## Requirements for both

1. Construct the coordinator; keep the `using` block so the COM session closes at the same point.
2. **Preserve the failure behavior exactly.** These sites populate dropdowns; today a failed open or a
   failed enumeration leaves the lists empty and does **not** throw a user-facing dialog. Do not introduce
   a `TaskDialog`, a `MessageBox`, or a throw here. The empty-list-on-failure behavior is the contract with
   the UI (contract R11).
3. Preserve the `defaultToFirstSheet` logic and the selected-value restoration around lines 428-460 —
   which sheet ends up selected after a reload is user-visible.
4. Update the `using` directives; drop any that only served `ExcelInteropService`.
5. If WinForms types are in scope in this file, respect the aliasing rule
   (`RevitTaskDialog = Autodesk.Revit.UI.TaskDialog`, `RevitView = Autodesk.Revit.DB.View`) — do not
   introduce a new ambiguity. Check before adding a directive.

## Do not touch

Any other member of this window: command handlers, browse logic, row collection management, settings
persistence. Settings still persist only via `ArcToolSettingsService.SaveMappings(...)` — not your concern
in this task, and not to be touched.

## Constraints

- Only this one file. `T3.5` is editing `ExcelSyncEngine.cs` concurrently — do not open it.
- `ArcTool.Core.csproj` needs no change; if you think it does, return `BLOCKED`.
- Do not build; `T3.7` owns the build gate.

## Acceptance condition

`grep -n "ExcelInteropService" ArcTool.Core/UI/ExcelToRevitWindow.xaml.cs` returns nothing. The diff is
confined to the using directives and the two blocks. Paste before/after for both blocks plus the grep
output, and state explicitly that the `_suppressRowEvents` span is unchanged.

## What downstream needs from you

`T3.7` builds and audits. `T4.2` needs to know which UI interactions to include in the `EV-3` matrix —
list the interactions your two call sites affect (sheet dropdown load, region dropdown load, row reload).
