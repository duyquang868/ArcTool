# T5.1b — Patch `WpsWorkbookPdfExporter.cs` for EV-1 `Workbooks.Open` defect

- depends on: `T5.1` (EV-1 defect confirmed)
- write_scope: `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`, result → `results/T5.1b_result.md`
- model: sonnet

## Objective

Patch the WPS provider so the late-bound workbook-open path tolerates the call-shape mismatch exposed by
EV-1 on this machine's WPS build.

## Trigger evidence

Operator/runtime evidence already captured at:
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1_output.txt`

Confirmed facts from EV-1:
- `KET.Application` resolves and instantiates.
- `Visible(set)` and `DisplayAlerts(set)` succeed.
- `Application.Version(get)` succeeds (`12.0`).
- `Workbooks` succeeds.
- `Workbooks.Open` fails with `DISP_E_TYPEMISMATCH` (`0x80020005`) on the current single-argument late-bound call.

Current code location:
- `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs:145-160`
- current call shape is `CallMethod(workbooks, "Open", filePath);`

## Inputs you may read

- `01_SHARED_CONTRACT.md`
- `03_TASK_MANIFEST.md` (only the `T5.1b` / `T3.7` routing you need)
- `05_RESULT_SCHEMA.md`
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1_output.txt`
- `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`
- `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs` (reference behavior only)
- `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs` (caller contract only)

Do not read unrelated package artifacts. Do not read full `CLAUDE.md`.

## Hard constraints

- Only this one source file: `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`.
- Keep the file late-bound only. No `Microsoft.Office.Interop.Excel`, no early-bound COM types, no `Xl*` enums.
- Preserve the existing swallow-and-return-`false` contract.
- Preserve MS-first / WPS-fallback architecture; do not touch provider selection.
- Do not modify export logic beyond what is needed for workbook open.

## Required implementation shape

Add a small private helper that tries a compact ordered set of `InvokeMember("Open", ...)` argument shapes
for WPS workbook open, instead of the current single direct call.

Requirements for the helper:
- First try the current shape: one positional `filePath` argument.
- Then try the smallest plausible optional-argument fallback(s) using `Type.Missing` / `Missing.Value`
  while staying entirely inside `Type.InvokeMember` late binding.
- Keep the number of variants small and intentional — this is not a brute-force matrix.
- Stop on the first non-null workbook.
- Keep concise debug output per failed attempt.
- If every shape fails, preserve the existing `EngineFoundOpenFailed` behavior.

## Verification you own

You may build locally to ensure the edited file compiles, but do not claim package closure. `T3.7` is re-run
separately after your task.

In your detailed result file include:
- the exact ordered `Open` call shapes you implemented
- why each was included
- whether the file compiled locally after the change
- a short grep proof that the WPS file still contains no Interop / `Xl*` references

## Return format

Write the full result file at:
- `.claude/workpackages/excel-to-revit-wps-provider-split/results/T5.1b_result.md`

Return ONLY the compact envelope defined by `05_RESULT_SCHEMA.md`.
