# T5.1c — Patch `WpsWorkbookPdfExporter.cs` for EV-1d/EV-1e WPS workbook-surface defect

- depends on: `T5.1` (WPS defect confirmed), `EV-1d`, `EV-1e`
- write_scope: `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`, result → `results/T5.1c_result.md`
- model: sonnet

## Objective

Patch the WPS provider so workbook acquisition uses the workbook/document surface actually exposed on this
machine's WPS build, and fail fast cleanly when that surface is absent or unusable.

## Trigger evidence

Operator/runtime evidence already captured at:
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1d_output.txt`
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1e_output.txt`
- `.claude/workpackages/excel-to-revit-wps-provider-split/results/EV-1d_root_cause_result.md`

Locked facts you must treat as authoritative:
- The real defect is **not** the old `Open(...)` argument-list direction. Do not widen or repeat that fix.
- `KET.Application` exposes Excel-shaped type info on this machine (`_Application`, GUID `000208d5-0000-0000-c000-000000000046`).
- `Workbooks` is a real non-null COM collection on this machine (DISPID `572`), and its own type info is
  `Workbooks`, GUID `000208db-0000-0000-c000-000000000046`.
- The discovered `Workbooks` collection exposes `Open memid=1923 cParams=15`, plus `_Open memid=682 cParams=13`.
- `Documents`, `ActiveDocument`, `Books`, `Workbook`, `Document`, and application-level `Open` / `OpenFile` /
  `OpenWorkbook` / `OpenDocument` are absent (`DISP_E_UNKNOWNNAME`) on this machine.
- `ActiveWorkbook` and `ActiveWindow` stay null until a real open succeeds.
- `Sheets` / `Worksheets` on the app object exist by name but currently return `E_FAIL` before a workbook is open.

## Current code location

- `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`
- You own the full file, but the source change should stay surgical and concentrated around workbook acquisition.

## Inputs you may read

- `01_SHARED_CONTRACT.md`
- `03_TASK_MANIFEST.md` (only the `T5.1c` / rerun routing you need)
- `05_RESULT_SCHEMA.md`
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1d_output.txt`
- `.claude/workpackages/excel-to-revit-wps-provider-split/evidence/EV-1e_output.txt`
- `.claude/workpackages/excel-to-revit-wps-provider-split/results/EV-1d_root_cause_result.md`
- `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`
- `ArcTool.Core/Services/Excel/MsExcelWorkbookPdfExporter.cs` (reference behavior only)
- `ArcTool.Core/Services/Excel/SpreadsheetImageExportService.cs` (caller contract only)

Do not read unrelated package artifacts. Do not read full `CLAUDE.md`.

## Hard constraints

- Only this one source file: `ArcTool.Core/Services/Excel/WpsWorkbookPdfExporter.cs`.
- Keep the file late-bound only. No `Microsoft.Office.Interop.Excel`, no early-bound COM types, no `Xl*` enums.
- Preserve the existing swallow-and-return-`false` contract.
- Preserve MS-first / WPS-fallback architecture; do not touch provider selection.
- Do not add probe-only code paths that depend on launching `et.exe` externally.
- If no usable workbook/document surface can be acquired, fail fast cleanly per contract; do not assume
  `Workbooks` is always usable just because the property is non-null.
- Do not implement more `Open(...)` permutations as the main fix direction. A tiny surface-specific fallback is
  acceptable only if directly justified by EV-1e-discovered members on the `Workbooks` collection itself.

## Required implementation shape

Use EV-1e as the binding authority for what to try, in this priority order:
1. `ActiveWorkbook` if already non-null.
2. `ActiveWindow` only insofar as it helps reach an already-open workbook surface; do not invent document aliases.
3. `Workbooks` / its real collection members (`Open`, `_Open`, `Item`, `Count`) — this is the only workbook/document
   surface actually discovered so far.
4. No `Documents`/`ActiveDocument` path unless you can justify it from the actual source file or evidence (current
   evidence says they are absent here).
5. No application-level `Open` path unless you can justify it from the actual source file or evidence (current
   evidence says those names are absent here).

Implementation intent:
- Add a small helper (or helpers) that acquires the workbook surface deterministically from the real app object.
- Null-check every surface before invoking deeper members.
- Prefer already-open state (`ActiveWorkbook`) before trying to open the target path.
- When opening is needed, bind from the `Workbooks` collection object itself, not from guessed application-level names.
- Consider whether the discovered `Workbooks` type info implies the current reflection flags are too narrow for WPS;
  adjust only if the change is directly tied to the evidence and stays surgical.
- Keep concise debug output that distinguishes: missing surface, null surface, bind failure, and open failure.

## Verification you own

You may build locally to ensure the edited file compiles, but do not claim package closure. The build/static gate is
rerun separately after your task.

In your detailed result file include:
- which workbook/document surfaces you now try, in order
- exactly how EV-1d and EV-1e justified each step
- whether you changed binding flags / open invocation shape, and why
- whether the file compiled locally after the change
- a short grep proof that the WPS file still contains no Interop / `Xl*` references
- whether the patch now fails fast cleanly when no usable workbook surface is available

## Return format

Write the full result file at:
- `.claude/workpackages/excel-to-revit-wps-provider-split/results/T5.1c_result.md`

Return ONLY the compact envelope defined by `05_RESULT_SCHEMA.md`.
