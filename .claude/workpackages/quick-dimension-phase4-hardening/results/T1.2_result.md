# T1.2 Result
Date: 2026-08-07
Task: T1.2 — Baseline lock

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\03_TASK_MANIFEST.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T1.2_baseline_lock.md`
5. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T1.1_result.md`

## 1. Locked baseline summary
1. BUG-10 is closed, not under review. The shared contract states this as a locked collector fix and says any future local swap must be treated as a fresh regression, not as a reason to relax the audit.
2. BUG-11 is closed, not under review. The shared contract states the named `FamilyInstanceReferenceType.Left/Right` station derivation fix is runtime-confirmed and must not be reopened by refactor or cleanup.
3. EV-2 six-run matrix passed. The mission baseline names the 2026-08-03 build as runtime-confirmed fixed with EV-2 six-run matrix `Exact`.
4. EV-3 reopen persistence passed. The mission baseline names EV-3 reopen PASS on the same fixed build.
5. Forced rollback validation is deferred by design. It is explicitly separated into `.Dossier/Quick Dimension - Deferred Rollback Validation Task.md` and is not an open blocker inside this package.
6. Grid is disabled in the wall-axis main flow as a present source fact. The domain model states wall-axis candidate collection emits a `Grid` disabled diagnostic before doing anything else.
7. This package starts from an already-working production feature. The mission says Phase 3 core creation works and Phase 4 is about hardening on real content, not reopening core creation defects.
8. The package baseline is evidence-backed and scope-locked. Later tasks may classify new findings only as fresh regressions or unsupported-by-design outcomes within the Phase 4 matrix, not as a silent reopening of BUG-10, BUG-11, or rollback scope.

## 2. Implementation boundaries vs evidence boundaries
### Implementation boundaries
These are present-source or locked-behavior facts that downstream tasks must treat as implementation scope boundaries.

- Grid is unconditionally excluded from the wall-axis flow. This is current-source behavior, not a tentative roadmap option.
- BUG-10 fix shape is locked: fallback candidate `elementId` aligns with the live reference owner while `hostElementId` remains the selected wall.
- BUG-11 fix shape is locked: named left/right opening stations derive from that same reference's own geometry.
- `QuickDimensionChainCreationService.cs` remains read-only package-wide, preserving rollback deferral safety while the file stays byte-identical.
- The audit strictness boundary remains locked: no weakening of `GetReferenceOrderRelation`, no `LocalPairSwap`, no unordered-set identity matching.

### Evidence boundaries
These are historical proof and mission-entry facts that define what the package is allowed to assume without re-litigating closed work.

- EV-2 six-run matrix passed on the 2026-08-03 fixed build.
- EV-3 reopen persistence passed on that fixed build.
- BUG-10 and BUG-11 are already runtime-confirmed fixed; they are not open hypotheses for this package.
- Forced rollback validation remains unproven inside this package by design, but that is tracked as a separate deferred task rather than as a Phase 4 blocker.
- The historical regression baseline is cited-only evidence, not a rerun target for this package.

## 3. Production starting state
This package begins from a working production feature, not from an active bugfix branch.

Basis:
- Shared contract mission line: harden the already-working Quick Dimension chain-creation feature on real project-like content, without reopening any closed defect.
- Shared contract section 5: Phase 3 core creation works. The open question is correctness envelope, safe failure envelope, and performance on broader content.
- Manifest sequencing: Phase 1 tasks lock baseline, scope, and acceptance language before any runtime evidence workflow, which is consistent with hardening a working feature rather than repairing an active break.

## 4. Baseline facts downstream tasks must not reinterpret
The following facts are locked and must not be silently reframed downstream.

1. BUG-10 is closed. A future mismatch is a fresh regression, not evidence that BUG-10 was still open.
2. BUG-11 is closed. A future mirror/flip anomaly is a fresh regression or a newly scoped unsupported case, not permission to re-open the original fix by implication.
3. EV-2 six-run matrix PASS and EV-3 reopen PASS are mission-entry evidence, not provisional claims awaiting reapproval.
4. Rollback deferral is a separate task boundary. No Phase 4 task may treat missing forced-rollback proof as an internal blocker or as justification to edit protected creation logic.
5. Grid exclusion in wall-axis mode is a present-source fact. Session 4.3 may judge only safe-failure behavior for Grid variants, never reinterpret the roadmap as a support-expansion mandate.
6. The package starts from a working production feature. Downstream tasks may test support limits and regressions, but may not narrate the package as if it were resuming unresolved BUG-10/BUG-11 repair work.
7. Implementation boundaries and evidence boundaries are distinct. Current source locks govern what may change; historical runtime passes govern what may be assumed closed.

## 5. Decision
PASS. The mission baseline is sufficiently established from the shared contract and T1.1 result to lock Phase 4 as hardening of an already-working production feature: BUG-10 and BUG-11 closed, EV-2 and EV-3 passed, rollback validation deferred outside the package, and Grid disabled in the wall-axis main flow as a current-source boundary.

## 6. Handoff note for T1.3
Use this baseline as fixed input when resolving the Session 4.3 contradiction.

- Do not reopen BUG-10 or BUG-11 while reasoning about Grid scope.
- Treat Grid exclusion in wall-axis mode as a current implementation boundary.
- Treat rollback deferral as out-of-package scope, not as a missing prerequisite.
- Resolve Session 4.3 only as a safe-failure interpretation, because the package starts from a working wall-axis production feature with closed collector defects and locked audit policy.
