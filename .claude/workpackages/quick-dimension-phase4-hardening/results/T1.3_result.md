# T1.3 Result
Date: 2026-08-07
Task: T1.3 — Resolve Grid scope contradiction

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T1.3_grid_scope_resolution.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T1.2_result.md`
5. Master-supplied verified source fact for `ArcTool.Core/Services/QuickDimensionReadOnlyEngine.CollectWallAxisCandidates`.

## 1. Contradiction, stated precisely
Roadmap Session 4.3 language asks to test Grid complexity variants — straight, cropped, hidden, and arc grids — while ensuring the MVP fails safely where support is not intended.

Present source behavior in the wall-axis main flow does not treat Grid as a variable under test. `CollectWallAxisCandidates` begins by unconditionally emitting a Grid-disabled diagnostic before wall resolution or any other work, and this happens regardless of `QuickDimensionOptions.IncludeGrids`. The message text says Grid collection is disabled by Quick Dimension options, but in this path the exclusion is unconditional; `IncludeGrids` gates only the deprecated two-point intersection path.

Therefore the first sentence of the roadmap text, if read as a support-expansion prompt, conflicts with the actual wall-axis flow and with the package baseline that Grid is excluded in wall-axis mode.

## 2. Derivation of the only package-safe interpretation
1. The package mission is hardening an already-working wall-axis chain-creation feature, not expanding scope.
2. T1.2 locked Grid exclusion in wall-axis mode as a present implementation boundary and explicitly said Session 4.3 may judge only safe-failure behavior for Grid variants.
3. The shared contract section 5 already resolves the contradiction by stating that reading Session 4.3 as “make grid dimensioning work” would contradict the locked scope.
4. The master-supplied source fact removes any remaining ambiguity: Grid exclusion occurs before selected-wall resolution, so Grid geometry cannot influence wall-axis candidate collection at all.
5. Because the exclusion is unconditional and structurally earlier than wall resolution, straight/cropped/hidden/arc Grid variants are not support cases, not conditional branches, and not partial inputs to the wall-axis flow. They are only negative controls for verifying that unsupported Grid content does not alter behavior, does not create dimensions from Grid references, and does not produce misleading output.

Only one materially safe interpretation remains: Session 4.3 is a Grid safe-failure matrix, not a Grid support matrix.

## 3. ALLOWED — what Session 4.3 may prove
Session 4.3 may prove only the following:

- The wall-axis mode remains behaviorally unchanged in the presence of Grid variants because Grid is excluded before wall resolution.
- Unsupported Grid content fails safely rather than silently affecting candidate collection.
- Operator-facing outcome is honest: no crash, no misleading apparent Grid support, no silently wrong dimension value attributable to Grid content.
- Any Grid variant near or around the selected wall does not change the wall-axis result relative to the same wall-axis case without that Grid variant.
- If a reduced or no-dimension outcome occurs, it is accompanied by an honest unsupported/disabled signal rather than by false success.

## 4. FORBIDDEN — what Session 4.3 may not attempt
Session 4.3 may not attempt any of the following:

- Widening source support.
- Enabling Grid candidates.
- Editing `QuickDimensionGridCandidateCollector.cs`.
- Reinterpreting the roadmap as a requirement to make straight/cropped/hidden/arc grids participate in wall-axis candidate collection.
- Using Grid-variant runs to reopen BUG-10 or BUG-11.
- Treating `QuickDimensionOptions.IncludeGrids` as relevant to wall-axis mode.
- Judging Grid variant differences as expected feature behavior when the flow has no structural path for Grid input.

## 5. EV-3 / Session 4.3 runbook guidance with zero ambiguity
### Structural rule the runbook author must respect
Because Grid is excluded before wall resolution, a Grid variant cannot change wall-axis behavior at all. This means Session 4.3 must be designed and judged as an A/B negative-control exercise:

- A case with a valid selected straight host wall and the normal wall-axis content under test.
- The same case plus one Grid variant nearby or otherwise visible/relevant in the view.
- The operator compares outcomes to confirm the Grid variant did not enter the flow.

The runbook must not ask the operator to “see whether the Grid is dimensioned,” “see whether arc grids partly work,” or “observe how cropped vs hidden grids change candidate picking,” because there is no path for Grid shape or visibility to influence wall-axis collection.

### What safe failure looks like concretely for a Grid variant in wall-axis mode
For any straight/cropped/hidden/arc Grid variant in a wall-axis run, safe failure means:

- The operator still works through the normal wall-axis command path using one selected straight host wall and one picked side.
- The resulting dimension outcome is exactly the normal wall-axis outcome for that wall case, unaffected by Grid presence or Grid type; or, if the wall case itself is otherwise unsupported, the unsupported outcome remains the same with or without the Grid.
- The operator does not observe Grid references being dimensioned, Grid-driven witness placement, Grid-driven station changes, or Grid-specific partial participation.
- Any diagnostic/log/dialog evidence that mentions Grid should be consistent with unsupported/disabled status, not with conditional support.
- There is no crash, hang, or misleading success narrative.

### What would instead constitute a Defect
For Session 4.3, any of the following is a Defect:

- A Grid variant changes the wall-axis dimension result, candidate span, station set, or witness/reference composition.
- A Grid reference appears to participate in the created dimension.
- Straight/cropped/hidden/arc Grid variants yield materially different wall-axis behavior from each other.
- The product implies Grid support in wall-axis mode when the flow actually excludes Grid unconditionally.
- The command crashes, hangs, or produces a silently wrong value in the presence of Grid content.
- The only explanation offered is “disabled by options” in a way that misleads the operator into thinking toggling options should enable Grid support in wall-axis mode.

## 6. Canonical quotable sentence
**CANONICAL RULE:** Session 4.3 is a Grid safe-failure matrix only: in wall-axis mode every Grid variant is outside the candidate flow by unconditional pre-wall-resolution exclusion, so downstream tasks may verify only unchanged behavior and honest unsupported outcomes, never Grid support expansion.

## 7. Final decision
PASS. The contradiction resolves to a single package-safe interpretation: Session 4.3 is strictly a Grid safe-failure matrix, and all downstream work must treat Grid variants as negative controls that cannot affect wall-axis behavior because Grid is excluded before wall resolution.

## 8. Handoff note for T1.4
Use the canonical rule verbatim when writing acceptance language for Session 4.3 and any downstream EV-3-style runbook text.

Operationally:
- Define Grid cases as unsupported-by-design unless a defect is observed.
- Write the runbook as A/B negative-control observation, not as support probing.
- Ask the operator to observe unchanged wall-axis behavior, absence of Grid participation, and honest unsupported/disabled signaling.
- Classify any Grid-caused behavioral difference as Defect, not as partial support or an invitation to widen scope.
