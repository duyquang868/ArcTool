# T2.3 Result — Operator Runbook for EV-1 Clean Fixture
Date: 2026-08-07
Task: T2.3 — Operator runbook for EV-1 clean fixture

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T2.3_operator_runbook_clean_fixture.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T2.1_result.md`
5. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T2.2_result.md`
6. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\04_EVIDENCE_QUEUE.md`

No source file read, no build, no runtime, no Revit/MCP call.

---

## 1. Exact operator runbook for EV-1

### 1.1 Scope
EV-1 is one clean-fixture acceptance on one wall, run twice on that same wall:
1. Left/Exterior shell
2. Right/Interior shell

Command for both runs: `QuickDimensionCreateChainSmokeCommand`

Do not add extra scenarios. Do not use grids, joins, mirrored openings, arc walls, curtain walls, linked models, or additional test walls.

### 1.2 Build the fixture exactly once
Create a new clean plan-view fixture with these exact properties:

#### Wall
- One straight non-curtain host wall
- Axis length: exactly 8000 mm
- Horizontal placement preferred for readability
- Axis start: `(0, 0, 0)`
- Axis end: `(8000, 0, 0)`
- No wall joins at either end
- No mid-run T-junctions
- No intersecting walls

#### Openings hosted in that wall
Use temporary labels in the view so the evidence is unambiguous:
- `O1` = Door, width `900 mm`, left jamb station `1000 mm`
- `O2` = Window, width `1200 mm`, left jamb station `3500 mm`
- `O3` = Door, width `800 mm`, left jamb station `6000 mm`

Derived right jamb stations:
- `O1` right jamb = `1900 mm`
- `O2` right jamb = `4700 mm`
- `O3` right jamb = `6800 mm`

Required clearances:
- Wall start to O1 left = `1000 mm`
- O1 right to O2 left = `1600 mm`
- O2 right to O3 left = `1300 mm`
- O3 right to wall end = `1200 mm`

#### View constraints
- Plan view
- No grids visible
- No linked models
- Wall fully visible
- View scale large enough that all seven final segment values will be readable in one screenshot

### 1.3 Pre-check before any smoke run
Before running the command, confirm all of the following:
- The fixture contains exactly one test wall for EV-1.
- The wall is straight, non-curtain, and hosted openings belong to that same wall.
- Wall axis length reads `8000 mm`.
- O1/O2/O3 widths read `900 / 1200 / 800 mm`.
- O1/O2/O3 left jamb stations match `1000 / 3500 / 6000 mm`.
- No wall joins at either end.
- No mid-run intersections or T-walls.
- No grids are visible in the view.
- The view can show one full dimension chain with all `7` segment texts readable.

If any item above is false, fix the fixture before running EV-1.

### 1.4 Pre-run evidence capture
Capture one annotated pre-run screenshot of the fixture showing all of the following:
- The full wall
- Labels `O1`, `O2`, `O3`
- Visible confirmation of wall length `8000 mm`
- Visible confirmation of opening widths `900 mm`, `1200 mm`, `800 mm`
- Clear visual state showing no joins, no grids, and no mid-run intersections

### 1.5 Run order
Use the same wall for both runs, in this exact order:
1. Run A — `Left/Exterior`
2. Run B — `Right/Interior`

### 1.6 Run A — Left/Exterior
1. Start from the clean fixture above.
2. Invoke `QuickDimensionCreateChainSmokeCommand`.
3. Select the EV-1 wall.
4. Choose `Left/Exterior` when the shell/side choice is requested.
5. Let the command finish normally unless Revit shows a dialog or cancel outcome.
6. Record the exact outcome for Run A.

### 1.7 Run B — Right/Interior
1. Stay on the same clean fixture wall.
2. Invoke `QuickDimensionCreateChainSmokeCommand` again.
3. Select that same EV-1 wall.
4. Choose `Right/Interior` when the shell/side choice is requested.
5. Let the command finish normally unless Revit shows a dialog or cancel outcome.
6. Record the exact outcome for Run B.

### 1.8 Exact evidence to return per run
Return one evidence bundle for Run A and one for Run B. Each bundle must contain:

1. **Combined XML path**
   - The path to the single combined Quick Dimension XML produced by that run.
   - One run must correspond to one combined XML.

2. **Created dimension id or explicit no-dimension outcome**
   - If a dimension is created, return the created dimension ElementId.
   - If no dimension is created, state that explicitly as `no dimension created`.

3. **Annotated screenshot with all 7 readable segments**
   - One screenshot per run is preferred if all seven segment texts are readable.
   - The screenshot must show:
     - the created dimension,
     - all `7` visible segment values readable,
     - witnesses visibly attached,
     - labels `O1`, `O2`, `O3`.

4. **Any dialog/cancel outcome**
   - If any warning, failure, cancel, or informational dialog appears, return the visible dialog text.
   - If the operator cancels at any point, state exactly where the cancel happened.
   - If no dialog appeared, say `no dialog`.

5. **Optional journal excerpt**
   - Optional only.
   - Include only if it helps explain a non-standard outcome.

### 1.9 Minimum manual quality gate before sending evidence to T2.4
Before returning the evidence, verify:
- Each run produced either a created dimension id or an explicit `no dimension created` outcome.
- Each run has one combined XML path.
- Each run has a screenshot where all `7` segment values are readable.
- The screenshot is annotated so O1/O2/O3 are identifiable.
- Any dialog/cancel outcome is written down exactly.

If the seven segment values are not readable, the evidence is not sufficient for T2.4 and must be re-captured.

### 1.10 What T2.4 will compare against
T2.4 will compare each run against this fixed seven-segment oracle:
- `1000 mm`
- `900 mm`
- `1600 mm`
- `1200 mm`
- `1300 mm`
- `800 mm`
- `1200 mm`

Expected total: `8000 mm`

Expected numeric result is the same for both shells.

---

## 2. EV-1 wording alignment for 04_EVIDENCE_QUEUE.md

The prior EV-1 wording was slightly too loose in two places:
- it said “shell(s)” instead of the exact two-run order,
- it requested annotated screenshots without explicitly requiring all seven readable segment values per run.

Updated EV-1 wording now matches the runbook exactly:
- same wall,
- Left/Exterior then Right/Interior,
- `QuickDimensionCreateChainSmokeCommand`,
- exact return payload per run,
- single clean-fixture acceptance only.

---

## 3. Final decision

PASS. T2.1 and T2.2 provide enough information to write an execution-safe EV-1 runbook with no unresolved operator choices. The runbook is concrete, uses one fixture built once, runs the exact two shells in fixed order on the same wall, names the exact command, and requests only the minimum evidence T2.4 needs for oracle comparison.

---

## 4. Handoff note for T2.4

Use the evidence in two per-run bundles, in this exact order:
1. Left/Exterior on the EV-1 wall
2. Right/Interior on that same wall

For each run, expect:
- one combined XML path,
- one created dimension id or explicit no-dimension outcome,
- one annotated screenshot with all seven readable segment values,
- one dialog/cancel statement,
- optional journal excerpt.

Reject evidence as insufficient if any run lacks readable values for all seven segments or if the fixture pre-checks were not satisfied.