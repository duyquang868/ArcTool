# T3.1 Result — Wall/opening case matrix
Date: 2026-08-07
Task: T3.1 — Wall/opening case matrix

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T3.1_case_matrix_wall_opening.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T2.4_result.md`
5. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\06_EXECUTION_STATE.md`
6. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\03_TASK_MANIFEST.md`
7. Previous contents of `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.1_result.md`

No source file read, no build, no runtime, no Revit/MCP call.

---

## 1. Upstream gate used

The user instruction for this continuation states that `06_EXECUTION_STATE.md` is the authoritative upstream verdict because `T2.5_result.md` is missing but execution state records:
- `T2.5` = `PASS`
- Note: `Session 4.1 clean-fixture gate passed on superseding rerun; Session 4.2 authorized`

That authoritative package state supersedes the absence of `results/T2.5_result.md` for this dispatch. Therefore T3.1 is not blocked by the missing detailed T2.5 file.

`T2.4_result.md` remains consistent with the execution-state note that a superseding rerun replaced the earlier failed EV-1 judgment.

---

## 2. Scope boundaries applied

Included Session 4.2 scope:
- one straight selected host wall;
- wall-axis MVP candidate sources only: resolved wall end anchors, hosted Door/Window jambs, and mid-run wall-joint stations;
- the unresolved risks and mandatory coverage points named by the shared contract and task file.

Explicitly excluded from this matrix:
- Grid cases;
- linked content;
- arc walls;
- rollback / dirty-transaction validation;
- any scope expansion beyond wall-axis MVP.

This matches both the task file and shared-contract sections 3, 5, and 6.

---

## 3. Final Session 4.2 case matrix

### T3.1-C01 — Empty wall
- Geometry intent: one straight non-curtain host wall, no hosted Door/Window instances, no mid-run joins.
- Why it belongs: the shared contract lists empty-wall behaviour as still unproven; this is the minimum two-anchor chain-readiness case.
- Expected classification going in: Supported.
- Evidence that must be visible:
  - one created dimension or equivalent explicit success outcome;
  - exactly 2 expected/created references and 1 segment in the combined XML audit;
  - audit gates all `true` and `referenceOrderRelation` = `Exact` or full `Reversed`;
  - screenshot showing a single two-anchor dimension with no extra stations.

### T3.1-C02 — Single opening
- Geometry intent: one straight host wall with exactly one hosted Door or Window, clear of both wall ends, no mid-run joins.
- Why it belongs: proves the simplest nontrivial production opening case: 2 wall-end anchors + 2 opening jambs.
- Expected classification going in: Supported.
- Evidence that must be visible:
  - exactly 4 expected/created references and 3 segments;
  - candidate station list showing ordered wall-end and jamb stations;
  - audit gates all `true` and `referenceOrderRelation` = `Exact` or full `Reversed`;
  - screenshot showing all three readable segments.

### T3.1-C03 — Several openings
- Geometry intent: one straight host wall with multiple hosted Door/Window instances, each comfortably spaced from one another and from both wall ends, no mid-run joins.
- Why it belongs: exercises the ordinary dense project-like supported case the feature is meant to handle.
- Expected classification going in: Supported.
- Evidence that must be visible:
  - reference count consistent with 2 anchors + 2 jambs per opening;
  - segment count = reference count minus 1;
  - ordered candidate station list spanning all openings without silent drops;
  - audit gates all `true` and `referenceOrderRelation` = `Exact` or full `Reversed`;
  - screenshot showing the complete chain across the wall span.

### T3.1-C04 — Close-spaced openings
- Geometry intent: one straight host wall with two adjacent openings whose nearest jamb stations are close enough to test station-collision / duplicate-removal behaviour, no mid-run joins.
- Why it belongs: the shared contract explicitly lists the close-spaced-opening tolerance case as unproven.
- Expected classification going in: Unsupported-by-design unless the resulting chain remains explicitly correct without ambiguity.
- Evidence that must be visible:
  - whether a dimension is created, reduced, or refused;
  - any `DuplicateStation` or equivalent honest diagnostic explaining dropped/merged stations;
  - reference/candidate counts sufficient to prove the reduction path;
  - screenshot and dialog text proving no crash and no silently wrong extra segment.

### T3.1-C05 — Opening flush with a resolved end anchor
- Geometry intent: one straight host wall with one opening whose jamb is coincident with, or within end-station tolerance of, a resolved wall end anchor, no mid-run joins.
- Why it belongs: the shared contract explicitly lists the flush-with-end-anchor condition as unproven and it directly tests anchor/opening collision handling.
- Expected classification going in: Unsupported-by-design unless the reduction is explicit and numerically honest.
- Evidence that must be visible:
  - whether the coincident wall-end/jamb pair is collapsed, reduced, or refused;
  - any duplicate/end-tolerance diagnostic explaining the outcome;
  - resulting reference count and segment count after collision handling;
  - screenshot proving the created dimension, if any, matches the reduced/retained station set without silent wrong values.

### T3.1-C06 — Mirrored/flipped family instances
- Geometry intent: one straight host wall with hosted Door/Window instances covering flip/mirror orientation variants while staying clear of end collisions and without mid-run joins.
- Why it belongs: the shared contract identifies residual dependence on mirror/flip/orientation as BUG-11’s still-unproven follow-up question.
- Expected classification going in: Supported.
- Evidence that must be visible:
  - created dimension with expected reference and segment counts for the chosen opening count;
  - candidate stations that remain geometrically correct despite orientation variants;
  - audit gates all `true` and `referenceOrderRelation` = `Exact` or full `Reversed`;
  - screenshot showing the complete chain and operator note identifying which instances were flipped/mirrored.

### T3.1-C07 — Mid-run T-junction host condition
- Geometry intent: one straight selected host wall with at least one mid-run intersecting wall forming a T-junction on the selected side, plus hosted openings so both opening and mid-run-joint collection paths are exercised together.
- Why it belongs: the task file requires this case explicitly, and the shared contract names mid-run joint behaviour as part of the real-model hardening target.
- Expected classification going in: Supported.
- Evidence that must be visible:
  - candidate station list showing both opening jambs and at least one mid-run joint station;
  - created dimension spanning the resolved final candidate range;
  - audit gates all `true` and `referenceOrderRelation` = `Exact` or full `Reversed`;
  - screenshot showing the chain across the jointed region without omitted or misordered stations.

---

## 4. Coverage check against required unresolved risks

Coverage required by task file and shared contract:
- empty wall → `T3.1-C01`
- single opening → `T3.1-C02`
- several openings → `T3.1-C03`
- close-spaced openings → `T3.1-C04`
- opening flush with resolved end anchor → `T3.1-C05`
- mirrored/flipped instances → `T3.1-C06`
- mid-run T-junction host condition → `T3.1-C07`

Out-of-scope items intentionally omitted:
- Grid variants → Session 4.3 safe-failure only
- linked content → outside wall-axis MVP
- arc host walls → outside straight-wall-only MVP
- rollback / transaction cleanliness → deferred standalone validation task

This is the minimum nonredundant case set that still covers every unresolved Session 4.2 wall/opening risk named in the shared contract.

---

## 5. Downstream handoff

### For T3.2 — prediction authoring
Use these final case ids and geometry intents exactly:
- `T3.1-C01` empty wall
- `T3.1-C02` single opening
- `T3.1-C03` several openings
- `T3.1-C04` close-spaced openings
- `T3.1-C05` opening flush with a resolved end anchor
- `T3.1-C06` mirrored/flipped family instances
- `T3.1-C07` mid-run T-junction host condition

T3.2 should preserve the going-in classifications above and convert each case into explicit predicted candidate counts, station-shape expectations, and supported-vs-unsupported acceptance language before EV-2 is read.

### For T3.4 — runtime run list
Run EV-2 exactly against the seven cases above. Operator evidence must make the visible conditions named under each case’s evidence checklist auditable.

---

## 6. Final decision

Final decision: PASS.

Reason: the authoritative execution-state file explicitly authorizes Session 4.2 despite the missing `T2.5_result.md`, and the authored matrix covers every unresolved wall/opening MVP risk named in the shared contract without widening scope beyond the wall-axis feature boundary.
