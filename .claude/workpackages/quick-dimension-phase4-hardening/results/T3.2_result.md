# T3.2 Result — Static predictions for wall/opening matrix
Date: 2026-08-07
Task: T3.2 — Static predictions for wall/opening matrix

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T3.2_case_predictions_wall_opening.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.1_result.md`

No source file read, no build, no runtime, no Revit/MCP call.

---

## 1. Prediction basis locked before EV-2

The shared contract fixes the wall-axis domain model used for every prediction:
- one straight non-curtain selected host wall;
- candidate sources are exactly wall end anchors, mid-run wall-joint stations, and hosted Door/Window jambs;
- chain readiness requires distinct projected stations after duplicate removal;
- accepted supported outcomes require all audit gates true and `referenceOrderRelation` = `Exact` or complete `Reversed`;
- unsupported outcomes are acceptable only when they fail honestly: no crash, no silent wrong value, and an explicit reduced or refused result.

Per T3.1, the Session 4.2 case order is fixed as `T3.1-C01` through `T3.1-C07`.

---

## 2. Case-by-case predictions

### T3.1-C01 — Empty wall
**Prediction:** Supported.

**Expected visible outcome**
- A dimension is created using only the two resolved wall end anchors.
- The visible chain contains exactly one segment spanning anchor-to-anchor.

**Expected audit outcome**
- Expected/created reference count = 2.
- Segment count = 1.
- No opening or mid-run candidates appear.
- All audit gates are `true`.
- `referenceOrderRelation` is `Exact` or complete `Reversed`.

**What would still be acceptable**
- Full reversal only; no other reduction path is expected because only two distinct anchors should exist.

**Key falsifier**
- Any no-dimension outcome, any extra station, or any audit mismatch with only an empty wall fixture present.

### T3.1-C02 — Single opening
**Prediction:** Supported.

**Expected visible outcome**
- A dimension is created across the wall span including both wall end anchors and both jambs of the single hosted opening.
- The visible chain contains three segments.

**Expected audit outcome**
- Expected/created reference count = 4.
- Segment count = 3.
- Candidate stations appear as two anchors plus two distinct jamb stations.
- All audit gates are `true`.
- `referenceOrderRelation` is `Exact` or complete `Reversed`.

**What would still be acceptable**
- Full reversal only. No reduction is expected because the opening is clear of both ends.

**Key falsifier**
- Missing one jamb, collapsed jambs without a duplicate diagnostic, or any wrong segment count/value.

### T3.1-C03 — Several openings
**Prediction:** Supported.

**Expected visible outcome**
- A full chain dimension is created across the wall span.
- Every hosted opening contributes both jamb stations, and the chain spans from first resolved anchor to last resolved anchor.

**Expected audit outcome**
- Expected/created reference count = `2 + (2 × opening count)`.
- Segment count = `reference count - 1`.
- Candidate stations are strictly ordered after projection and duplicate removal, with no silent drops.
- All audit gates are `true`.
- `referenceOrderRelation` is `Exact` or complete `Reversed`.

**What would still be acceptable**
- Full reversal only. No reduction is expected when openings are comfortably spaced from each other and from both wall ends.

**Key falsifier**
- Any omitted opening jamb without an explicit duplicate/collision explanation, or any mismatch between predicted count and actual chain size.

### T3.1-C04 — Close-spaced openings
**Prediction:** Unsupported-by-design unless the resulting station set remains explicitly reduced and numerically honest.

**Expected honest failure or reduced behavior**
- If nearest jamb stations collide within duplicate tolerance, the implementation may drop one or more colliding stations and either:
  - create a reduced dimension using the surviving distinct stations, or
  - refuse creation if too few distinct stations remain for a meaningful chain.
- Any reduction must be explained by `DuplicateStation` or equivalent explicit diagnostics.

**Expected audit / evidence shape if acceptable**
- The resulting reference count is lower than the naive `2 + (2 × opening count)` only when the dropped stations are explicitly diagnosed.
- No crash, no silently wrong extra short segment, and no misleading "success" with an unexplainable missing jamb.
- If a dimension is created, every retained segment still matches the reduced station set.

**Defect risk worth watching**
- A silent drop that leaves a plausible-looking but incorrect chain is the main risk.

**Key falsifier**
- Missing or merged stations without duplicate diagnostics, or a created dimension that keeps a numerically wrong short segment between near-coincident jambs.

### T3.1-C05 — Opening flush with a resolved end anchor
**Prediction:** Unsupported-by-design unless anchor/jamb coincidence is reduced explicitly and honestly.

**Expected honest failure or reduced behavior**
- If one opening jamb is coincident with, or within tolerance of, a resolved wall end anchor, the implementation may collapse that pair into one surviving station and either:
  - create a reduced dimension with one fewer distinct station than the naive anchor-plus-jamb count, or
  - refuse creation if the remaining station set is insufficient.
- The reduction must be explicit through duplicate/end-tolerance diagnostics rather than a silent omission.

**Expected audit / evidence shape if acceptable**
- Resulting reference count is reduced exactly by the coincident pair, not by unrelated stations.
- Any created dimension remains numerically consistent with the surviving stations.
- No crash, no silent wrong zero-length or near-zero segment.

**Defect risk worth watching**
- End-anchor resolution and opening jamb projection may fight over ownership/ordering at the wall boundary.

**Key falsifier**
- A silent missing end or jamb reference, or a created dimension showing a wrong boundary segment with no duplicate explanation.

### T3.1-C06 — Mirrored/flipped family instances
**Prediction:** Supported, but this remains the explicit unresolved probe from BUG-11 follow-up.

**Expected visible outcome**
- A dimension is created with counts matching the chosen opening count: two anchors plus two jambs per hosted opening.
- Flip/mirror state does not alter the physical station values of the jamb references.

**Expected audit outcome**
- Expected/created reference count = `2 + (2 × opening count)`.
- Segment count = `reference count - 1`.
- Candidate stations remain geometrically correct and ordered despite orientation variants.
- All audit gates are `true`.
- `referenceOrderRelation` is `Exact` or complete `Reversed`.

**Unresolved probe to preserve**
- Mirror/flip is not considered "solved" by assumption. The runtime evidence must still prove that reference-owned station derivation is orientation-neutral in practice.

**Defect risk worth watching**
- A flipped or mirrored family might still surface a left/right reference whose derived station is swapped, duplicated, or displaced.

**Key falsifier**
- Any orientation-correlated missing jamb, swapped station order beyond full reversal, or wrong segment value that appears only on flipped/mirrored instances.

### T3.1-C07 — Mid-run T-junction host condition
**Prediction:** Supported.

**Expected visible outcome**
- A dimension is created across the wall span including end anchors, opening jambs, and at least one mid-run joint station from the selected side.
- The chain spans the resolved final candidate range rather than raw wall endpoints.

**Expected audit outcome**
- Expected/created reference count = `2 + (2 × opening count) + (mid-run joint station count)`.
- Segment count = `reference count - 1`.
- Candidate station list includes at least one non-opening, non-end-anchor mid-run station.
- All audit gates are `true`.
- `referenceOrderRelation` is `Exact` or complete `Reversed`.

**What would still be acceptable**
- Full reversal only. No reduction is expected unless the mid-run joint station collides with another legitimate station and the collision is explicitly diagnosed.

**Key falsifier**
- Missing the mid-run joint station entirely, or creating a chain that includes openings and anchors but silently omits the T-junction evidence.

---

## 3. Cross-case prediction summary

### Supported cases expected to pass cleanly
- `T3.1-C01` Empty wall
- `T3.1-C02` Single opening
- `T3.1-C03` Several openings
- `T3.1-C06` Mirrored/flipped family instances
- `T3.1-C07` Mid-run T-junction host condition

These should produce dimensions with all expected surviving stations represented, all audit gates true, and `referenceOrderRelation` equal to `Exact` or full `Reversed`.

### Unsupported-by-design cases expected to fail honestly or reduce explicitly
- `T3.1-C04` Close-spaced openings
- `T3.1-C05` Opening flush with a resolved end anchor

These do not require "no dimension" specifically. They require an honest outcome: explicit duplicate/end-collision diagnostics, no crash, and no silent wrong values.

### Primary defect risks to watch in EV-2
- Silent station drops that are not explained by duplicate/end-collision diagnostics.
- Wrong short segments caused by near-coincident stations being retained incorrectly.
- Orientation-correlated station swaps or displaced jambs in mirrored/flipped instances.
- Mid-run joint omission when mixed with ordinary opening collection.
- Any `Mismatch` `referenceOrderRelation`, which is always a defect.

---

## 4. Downstream handoff

### For T3.3
Preserve this explicit unresolved hypothesis:
- Despite BUG-11’s reference-owned station fix, mirrored/flipped family orientation could still expose a residual left/right station inversion, duplication, or displacement at runtime.

T3.3 should turn that into the focused probe language and evidence questions for orientation-specific confirmation or falsification.

### For T3.4
Run EV-2 in this exact case order and judge each case against these predictions:
1. `T3.1-C01` Empty wall — Supported
2. `T3.1-C02` Single opening — Supported
3. `T3.1-C03` Several openings — Supported
4. `T3.1-C04` Close-spaced openings — Unsupported-by-design unless explicitly reduced honestly
5. `T3.1-C05` Opening flush with a resolved end anchor — Unsupported-by-design unless explicitly reduced honestly
6. `T3.1-C06` Mirrored/flipped family instances — Supported, but explicit orientation probe
7. `T3.1-C07` Mid-run T-junction host condition — Supported

Evidence review must treat the listed key falsifier for each case as the fail trigger.

---

## 5. Final decision

Final decision: PASS.

Reason: all seven Session 4.2 cases inherited from T3.1 now have pre-runtime predictions with explicit classification, expected visible/audit behavior, and concrete falsifiers, while preserving mirror/flip as an unresolved probe rather than assuming it is settled.
