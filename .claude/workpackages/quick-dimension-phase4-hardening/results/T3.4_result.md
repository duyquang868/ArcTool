# T3.4 Result — Operator runbook for EV-2 wall/opening matrix
Date: 2026-08-07
Task: T3.4 — Operator runbook for EV-2 wall/opening matrix

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\01_SHARED_CONTRACT.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T3.4_operator_runbook_wall_opening.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
4. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\04_EVIDENCE_QUEUE.md`
5. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.1_result.md`
6. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.2_result.md`
7. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\ArcTool\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.3_result.md`

No source file read, no build, no runtime, no Revit/MCP call.

---

## 1. Scope and run-count summary

The Session 4.2 case matrix (`T3.1`) has 7 cases. `T3.1-C06` (mirror/flip) is expanded here into
its own sub-runs per the `T3.3` probe design, since a single run cannot discriminate orientation
sensitivity. Every other case is a single run.

- Mandatory runs: **8** — `R1`..`R5` (C01–C05, one run each), `R6`/`R7` (C06 baseline + one
  orientation variant), `R8` (C07).
- Optional runs: **2** — `R6b`/`R6c` (C06 stronger 4-subcase set), only if the operator's chosen
  Door/Window family exposes both Hand/Facing-flip and Mirror independently without changing
  placement geometry.
- Every run uses the same command: `QuickDimensionCreateChainSmokeCommand`.
- Every run returns exactly one combined Quick Dimension XML (per the shared-contract invariant:
  "one smoke run produces ONE combined XML").

This matches the case order fixed by `T3.1`/`T3.2` and does not add any off-matrix experiment
(no Grid, no linked content, no arc wall, no rollback probe).

---

## 2. Run order and per-case runbook

### R1 — T3.1-C01 — Empty wall
**Prediction (T3.2): Supported.**

- Fixture/setup prerequisite: one straight non-curtain host wall, no hosted Door/Window instances,
  no mid-run joins/T-junctions, no other wall touching it along its run. Length is not critical;
  reuse or rebuild an isolated wall (e.g. 6000 mm) with clean, unjoined ends.
- Shell(s) to run: **Left/Exterior** only (symmetric case; one shell is sufficient — the point is
  the two-anchor chain, not side parity).
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] expected/created reference count (predicted 2) and segment count (predicted 1) read from
        the XML audit
  - [ ] `referenceOrderRelation` value
  - [ ] annotated screenshot showing the single two-anchor segment
  - [ ] visible dialog / cancel text, or "no dialog"

### R2 — T3.1-C02 — Single opening
**Prediction (T3.2): Supported.**

- Fixture/setup prerequisite: one straight host wall (e.g. 6000 mm), one hosted Door or Window
  clear of both wall ends by a comfortable margin (recommend ≥ 500 mm from each jamb to the
  nearest wall end — well above the ~1 mm `WallEndStationTolerance`), no mid-run joins.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] expected/created reference count (predicted 4) and segment count (predicted 3)
  - [ ] candidate station list (2 anchors + 2 jambs) as reported in the XML
  - [ ] `referenceOrderRelation` value
  - [ ] annotated screenshot showing all 3 segments readable
  - [ ] visible dialog / cancel text, or "no dialog"

### R3 — T3.1-C03 — Several openings
**Prediction (T3.2): Supported.**

- Fixture/setup prerequisite: one straight host wall long enough to host 3+ Door/Window instances,
  each comfortably spaced from its neighbors and from both wall ends (recommend ≥ 500 mm jamb-to-
  jamb and jamb-to-end clearance), no mid-run joins.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] expected/created reference count (predicted `2 + 2×opening count`) and segment count
        (`reference count - 1`)
  - [ ] full ordered candidate station list confirming no silent drops
  - [ ] `referenceOrderRelation` value
  - [ ] annotated screenshot showing the complete chain
  - [ ] visible dialog / cancel text, or "no dialog"

### R4 — T3.1-C04 — Close-spaced openings
**Prediction (T3.2): Unsupported-by-design unless the reduction is explicit and numerically honest.**

- Fixture/setup prerequisite: one straight host wall with two openings placed so their nearest
  jambs land as close together as the operator can practically achieve (target ≤ 1 mm gap; the
  engine's `DuplicateTolerance` is `1e-4` ft ≈ 0.03 mm, which may be finer than manual Revit
  placement allows). Record the actual measured gap between the two nearest jambs in the evidence
  so the judge can classify the outcome against the real geometry, not the target. No mid-run
  joins.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension/reduced-dimension outcome
  - [ ] the actual measured jamb-to-jamb gap for the close-spaced pair (from Revit properties or
        model measurement, not just design intent)
  - [ ] any `DuplicateStation` or equivalent diagnostic entry from the XML
  - [ ] resulting reference/segment counts after any reduction
  - [ ] annotated screenshot proving no silently wrong short segment
  - [ ] visible dialog / cancel / diagnostic text

### R5 — T3.1-C05 — Opening flush with a resolved end anchor
**Prediction (T3.2): Unsupported-by-design unless the reduction is explicit and numerically honest.**

- Fixture/setup prerequisite: one straight host wall with one opening inserted so its outer jamb
  lands as close as practically achievable to one resolved wall end anchor (target ≤ 1 mm, at or
  inside the ~1 mm `WallEndStationTolerance` band — e.g. insert the opening flush against the wall
  end with no return). No mid-run joins. Record the actual measured jamb-to-end-anchor gap.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension/reduced-dimension outcome
  - [ ] the actual measured jamb-to-end-anchor gap
  - [ ] any duplicate/end-tolerance diagnostic entry from the XML
  - [ ] resulting reference/segment counts after any collapse
  - [ ] annotated screenshot proving no silent wrong zero-length/near-zero segment
  - [ ] visible dialog / cancel / diagnostic text

### R6 — T3.1-C06A — Mirrored/flipped family instances, baseline orientation
**Prediction (T3.2/T3.3): Supported baseline for the orientation probe.**

- Fixture/setup prerequisite: one straight host wall with one hosted Door or Window clear of both
  wall ends and with no mid-run joins. Keep the same wall, family/type, width, insertion station,
  side pick, and surrounding geometry for both `R6` and `R7`. This baseline instance should be the
  ordinary non-mirrored, non-flipped state when the family exposes those flags.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] orientation-state evidence for the tested opening: `Mirrored`, `HandFlipped`,
        `FacingFlipped` when exposed, or a note that a flag is unavailable for that family
  - [ ] expected/created reference count and segment count for the whole run
  - [ ] `referenceOrderRelation`
  - [ ] created segment values
  - [ ] the two jamb stations for the tested opening from the XML
  - [ ] annotated screenshot showing the selected opening, visible chain, and orientation-state UI
  - [ ] short operator note: `no visible inversion` / `visible inversion but numerically correct` /
        `visible inversion with wrong numeric segments`
  - [ ] visible dialog / cancel text, or `no dialog`

### R7 — T3.1-C06B — Mirrored/flipped family instances, orientation variant
**Prediction (T3.2/T3.3): Supported if orientation is operationally irrelevant.**

- Fixture/setup prerequisite: duplicate `R6` exactly except change only the opening's available
  orientation state so at least one of `Mirrored` / `HandFlipped` / `FacingFlipped` differs from
  `R6`. Do not change wall, family/type, width, insertion station, shell pick, or physical opening
  location. If the family cannot expose any changed orientation state without moving the opening,
  mark the probe inconclusive and stop after capturing that limitation.
- Shell(s) to run: **Left/Exterior**.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] orientation-state evidence proving at least one flag changed from `R6`, or explicit note
        that no flag could be changed for this family
  - [ ] expected/created reference count and segment count for the whole run
  - [ ] `referenceOrderRelation`
  - [ ] created segment values
  - [ ] the two jamb stations for the tested opening from the XML
  - [ ] annotated screenshot showing the same opening location, visible chain, and orientation-state
        UI
  - [ ] short operator note: `no visible inversion` / `visible inversion but numerically correct` /
        `visible inversion with wrong numeric segments`
  - [ ] visible dialog / cancel text, or `no dialog`

Optional stronger probe only when it does not change placement geometry:
- `R6b` — flip-only variant
- `R6c` — mirror+flip or mirror-only variant

These optional sub-runs are not required for EV-2 PASS. They are only extra attribution evidence
when the family supports multiple independent orientation controls cleanly.

### R8 — T3.1-C07 — Mid-run T-junction host condition
**Prediction (T3.2): Supported.**

- Fixture/setup prerequisite: one straight selected host wall with at least one intersecting wall
  forming a clear mid-run T-junction on the selected side, plus at least one hosted Door/Window in
  the selected host wall so both opening-jamb and mid-run-joint collection paths are exercised in
  the same run. Keep the host wall straight and non-curtain.
- Shell(s) to run: **the shell on the side where the T-junction exists**. If the T-junction is on
  the wall's Left/Exterior side, run Left/Exterior; if it is on the Right/Interior side, run
  Right/Interior. Do not run the opposite shell for this case.
- Command: `QuickDimensionCreateChainSmokeCommand`.
- Evidence to return:
  - [ ] combined XML path
  - [ ] created dimension id, or explicit no-dimension outcome
  - [ ] expected/created reference count and segment count
  - [ ] candidate station list showing both opening jambs and at least one mid-run joint station
  - [ ] `referenceOrderRelation`
  - [ ] annotated screenshot showing the T-junction region and the complete chain
  - [ ] visible dialog / cancel text, or `no dialog`

---

## 3. Operator execution notes

1. Execute runs in this exact order: `R1`, `R2`, `R3`, `R4`, `R5`, `R6`, `R7`, `R8`.
2. Do not batch multiple cases into one run. One run = one picked wall + one side pick + one
   combined XML.
3. Use stable case/run labels in every returned bundle: `R1_C01`, `R2_C02`, `R3_C03`, `R4_C04`,
   `R5_C05`, `R6_C06A`, `R7_C06B`, `R8_C07`.
4. If `R7` cannot change any orientation flag while keeping placement geometry fixed, return the
   baseline bundle plus an explicit `orientation probe inconclusive for this family` note. That is
   still usable evidence for `T3.5`–`T3.8`; it is not a license to improvise a different family
   unless the operator chooses to restart `R6`/`R7` on a better-controlled family.
5. For `R4` and `R5`, include the actual measured near-collision gap because the supported/unsupported
   judgment depends on the real geometry reached, not only the intended setup.
6. Keep screenshots annotated only enough to identify wall/opening case, visible segments, and the
   relevant probe note. No extra experiments or alternate shell reruns are part of EV-2.

---

## 4. Minimum evidence bundle checklist for EV-2 handoff

For each mandatory run, return one bundle containing only:
- run label and case id;
- combined XML path;
- created dimension id or explicit no-dimension/reduced-dimension outcome;
- the case-specific required counts/stations/diagnostics listed above;
- one annotated screenshot;
- visible dialog/cancel text or `no dialog`;
- optional journal excerpt only if it adds a concrete diagnostic not already visible in the XML/UI.

This is the smallest evidence set that still allows `T3.5` to map evidence to cases, `T3.6` to
check supported vs unsupported behavior, `T3.7` to judge the orientation probe, and `T3.8` to issue
the Session 4.2 verdict.

---

## 5. EV-2 wording alignment for `04_EVIDENCE_QUEUE.md`

The EV-2 entry should match this runbook more precisely in three ways:
- it should request the exact mandatory run order and labels;
- it should call out that `C06` requires two mandatory sub-runs (`baseline` + `orientation variant`)
  with optional stronger sub-runs only when cleanly available;
- it should request the orientation-state evidence and actual measured near-collision gaps needed by
  `T3.7` and the unsupported-case judges.

---

## 6. Final decision

Final decision: PASS.

Reason: the EV-2 operator runbook now fixes run order, fixture/setup prerequisites, shell choice,
orientation-probe capture requirements, and the minimum per-run evidence bundle needed by
`T3.5`–`T3.8`, without adding off-matrix runtime work.
