# T3.6 Result — EV-2 review: close-spaced and end-flush edge cases
Date: 2026-08-08
Task: T3.6 — Judge EV-2 cases that stress station distinctness and wall-end anchor interaction

## Inputs read
1. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\.claude\workpackages\quick-dimension-phase4-hardening\tasks\T3.6_ev2_edge_cases_review.md`
2. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\.claude\workpackages\quick-dimension-phase4-hardening\05_RESULT_SCHEMA.md`
3. `D:\Quang mini\OneDrive - MSFT\Plugin Revit\.claude\workpackages\quick-dimension-phase4-hardening\results\T3.2_result.md`
4. `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_386116_Left_20260807_182248.xml`
5. `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_387179_Left_20260807_183426.xml`

No source file read, no build, no runtime, no Revit/MCP call.

---

## 1. Scope and evidence mapping

This task owns only the EV-2 edge cases:
- `T3.1-C04` — close-spaced openings
- `T3.1-C05` — opening flush with a resolved end anchor

The supplied XMLs map cleanly to those cases:
- `...386116_Left_20260807_182248.xml` is the close-spaced run because it preserves the same 8000 mm wall and opening set as the supported dense fixture, but compresses one door/window pair until one station is removed under an explicit `DuplicateStation` diagnostic.
- `...387179_Left_20260807_183426.xml` is the end-flush run already logged as `R5_C05`; it drops the opening entirely under `MissingReference` and leaves an anchor-only chain.

---

## 2. Case verdicts

### T3.1-C04 — Close-spaced openings
**Static prediction from `T3.2`:** Unsupported-by-design unless the reduction is explicit and numerically honest.

**Observed evidence**
- Final candidate count = `7`, not the naive `8`.
- The surviving ordered stations are `0 / 2550 / 3450 / 3500 / 4700 / 5500 / 8000`.
- The chain is numerically honest after reduction: segment values are `2550 / 900 / 50 / 1200 / 800 / 2500` and every segment matches expected delta.
- Audit result is internally clean:
  - `expectedReferenceCount="7"`
  - `createdReferenceCount="7"`
  - `expectedSegmentCount="6"`
  - `actualSegmentCount="6"`
  - `referenceOrderRelation="Exact"`
  - `referenceIdentityMatched="true"`
  - `referenceOwnersMatched="true"`
  - `segmentValuesMatched="true"`
- The reduction is explicit, not silent. Diagnostic 11 states:
  - `reason="DuplicateStation"`
  - `Removed Door candidate 'Door: M_Single-Flush - 800x2000 (386190) [Left]' at station 4700 mm because it shares that station with 'Window: M_Fixed - 1200x1800 (386233) [Right]' (Window). Chain dimensions cannot use zero-length segments.`

**Judgment**
- Classification: **Unsupported-by-design**
- MVP acceptability: **Acceptable**

**Reasoning**
This is the exact honest-reduction pattern `T3.2` allowed. One colliding station is removed with an explicit duplicate-station diagnostic, and the created dimension remains fully auditable and numerically correct. The retained 50 mm segment between `3450` and `3500` is not a wrong-value defect; it reflects two distinct surviving stations after the diagnosed collision was removed.

### T3.1-C05 — Opening flush with a resolved end anchor
**Static prediction from `T3.2`:** Unsupported-by-design only if anchor/jamb coincidence is reduced explicitly and honestly.

**Observed evidence**
- Final candidate count collapses to `2` with only wall start/end anchors retained.
- The opening is not reduced as a coincident jamb/anchor pair; it is dropped completely.
- Diagnostic trail shows an opening-reference acquisition failure, not an explicit anchor/jamb coincidence reduction:
  - `Opening geometry check for Window: M_Fixed - 1200x1800 (387213): expected span 1200 mm ... raw padded-bbox extrema 0 mm, selected jamb pair n/a`
  - `reason="MissingReference"`
  - `Skipped Window: M_Fixed - 1200x1800 (387213) because no valid opening-edge reference was available. Host Wall: 387179.`
- The created dimension then audits cleanly only against the reduced anchor-only set:
  - `expectedReferenceCount="2"`
  - `actualSegmentCount="1"`
  - one segment = `5500`
  - `referenceOrderRelation="Exact"`
  - audit booleans all `true`

**Judgment**
- Classification: **Defect**
- MVP acceptability: **Not acceptable**

**Reasoning**
This is not the explicit unsupported-by-design reduction allowed by `T3.2`. The evidence does not show a diagnosed end-anchor/jamb coincidence with one intentionally surviving boundary station. Instead, the hosted opening vanishes because the engine cannot obtain a valid opening-edge reference at all, and the output silently degrades to an anchor-only dimension that masks the missing opening. The audit passes only because it audits the already-reduced wrong candidate set.

---

## 3. Edge-case decision summary

- `T3.1-C04` is a valid unsupported-by-design case with explicit `DuplicateStation` handling and honest numeric output.
- `T3.1-C05` is a concrete defect, not an acceptable unsupported-by-design reduction, because the opening is lost through `MissingReference` and the result collapses to a misleading anchor-only chain.

This means the EV-2 edge-case bucket is not uniformly safe for MVP hardening: one case is acceptable as an explicit limitation, while the end-flush boundary case remains a real defect signal.

---

## 4. Final decision

Final decision: PASS.

Reason: both assigned cases received explicit verdicts, the result cleanly distinguishes honest duplicate-station reduction from defect-like hidden loss, and the task stayed inside the `C04`/`C05` subset.

---

## 5. MICRO_RESULT

```text
<MICRO_RESULT>
task: T3.6
status: PASS
confirmed:
- `...386116_Left_20260807_182248.xml` is the close-spaced case and records an explicit `DuplicateStation` exclusion with a numerically correct reduced 7-reference chain.
- `T3.1-C04` is classified Unsupported-by-design and acceptable for MVP because the reduction is explicit and the audit is fully clean.
- `...387179_Left_20260807_183426.xml` is the end-flush case and drops the opening under `MissingReference`, leaving an anchor-only 2-reference chain.
- `T3.1-C05` is classified Defect because the opening disappears through reference failure rather than an explicit anchor/jamb coincidence reduction.
unknown:
- none
decision: Publish `C04` as an acceptable explicit limitation and escalate `C05` as the edge-case defect statement for Session 4.2.
files_read: tasks/T3.6_ev2_edge_cases_review.md; 05_RESULT_SCHEMA.md; results/T3.2_result.md; 386116...182248.xml; 387179...183426.xml
files_changed: results/T3.6_result.md
result_file: .claude/workpackages/quick-dimension-phase4-hardening/results/T3.6_result.md
next_input: `T3.8` should classify `C04` as acceptable unsupported-by-design behavior and `C05` as a concrete defect while preserving the locked `T3.7` language for `C06/C07`.
blocker: none
</MICRO_RESULT>
```