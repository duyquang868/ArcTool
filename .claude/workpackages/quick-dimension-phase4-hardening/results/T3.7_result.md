# T3.7 result — EV-2 mirror/flip plus mid-run review (R6_C06A baseline, R7_C06B flip, R7_C06B mirror, R8_C07 mid-run)

Date: 2026-08-08
Task: T3.7
Scope note: this result now covers all supplied EV-2 runs that matter to T3.7: the mirror/flip probe (baseline, flip sub-case, mirror sub-case) plus the mid-run T-junction case (`R8_C07`).

## Exact inputs read
- Task file: `tasks/T3.7_ev2_mirror_midrun_review.md`
- Probe design: `results/T3.3_result.md`
- Pre-EV-2 prediction: `results/T3.2_result.md`
- Result schema: `05_RESULT_SCHEMA.md`
- Prior result state: `results/T3.7_result.md` (R6_C06A baseline intake only)
- Evidence XML 1 (baseline, R6_C06A): `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_387179_Left_20260807_185428.xml`
- Evidence XML 2 (flip sub-case, R7_C06B): `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_387179_Left_20260807_190513.xml`
- Evidence XML 3 (mirror sub-case, R7_C06B): `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_387562_Left_20260807_190738.xml`
- Evidence XML 4 (mid-run case, R8_C07): `C:\Users\ADMIN\Desktop\PA4\ArcTool_QD_ReadOnlySummary_387562_Left_20260807_202458.xml`
- Operator statements: screenshots showing segment values 1450/900/3150 for the supplied mirror/flip runs; baseline/flip screenshots show the door swing arc on opposite sides; mirror screenshot shows the door highlighted on the opposite face of the wall.

CLAUDE.md was not read in full, per task constraint. No source file read, no Revit/MCP call, no smoke test.

---

## A. Three-way comparison table

| Field | Baseline — R6_C06A | Flip sub-case — R7_C06B | Mirror sub-case — R7_C06B |
|---|---|---|---|
| XML path | `...185428.xml` | `...190513.xml` | `...190738.xml` |
| selectedWallId | `387179` | `387179` | `387562` |
| selectedShellLayer | `Exterior` | `Exterior` | `Interior` |
| sideLabel / sideSign | `Left` / `1` | `Left` / `1` | `Left` / `1` |
| axisLengthMm | `5500` | `5500` | `5500` |
| AxisDirection | `(0.8910065, 0.4539905, 0)` | `(0.8910065, 0.4539905, 0)` | `(0.8910065, -0.4539905, 0)` |
| SideNormal | `(-0.4539905, 0.8910065, 0)` | `(-0.4539905, 0.8910065, 0)` | `(0.4539905, 0.8910065, 0)` |
| hosted door instance id | `387482` | `387482` | `387563` |
| hosted door family/type | `M_Single-Flush / 900x2000` | `M_Single-Flush / 900x2000` | `M_Single-Flush / 900x2000` |
| FinalCandidates count | `4` | `4` | `4` |
| Candidate 1 | `0` mm, `Wall`, `WallSideFace`, face token `20` | `0` mm, `Wall`, `WallSideFace`, face token `20` | `0` mm, `Wall`, `WallSideFace`, face token `20` |
| Candidate 2 | `1450` mm, `Door`, `HostWallOpeningGeometry`, face token `60` | `1450` mm, `Door`, `HostWallOpeningGeometry`, face token `60` | `1450` mm, `Door`, `HostWallOpeningGeometry`, face token `60` |
| Candidate 3 | `2350` mm, `Door`, `HostWallOpeningGeometry`, face token `52` | `2350` mm, `Door`, `HostWallOpeningGeometry`, face token `52` | `2350` mm, `Door`, `HostWallOpeningGeometry`, face token `51` |
| Candidate 4 | `5500` mm, `Wall`, `WallSideFace`, face token `12` | `5500` mm, `Wall`, `WallSideFace`, face token `12` | `5500` mm, `Wall`, `WallSideFace`, face token `12` |
| ChainCreationAudit dimensionId | `387510` | `387543` | `387564` |
| expected / created reference counts | `4 / 4` | `4 / 4` | `4 / 4` |
| expected / actual segment counts | `3 / 3` | `3 / 3` | `3 / 3` |
| referenceOrderRelation | `Exact` | `Exact` | `Exact` |
| referenceIdentityMatched | `true` | `true` | `true` |
| referenceOwnersMatched | `true` | `true` | `true` |
| segmentValuesMatched | `true` | `true` | `true` |
| ordered segment values | `1450 / 900 / 3150` | `1450 / 900 / 3150` | `1450 / 900 / 3150` |
| ResolvedDimensionLine sideOffsetMm | `1210.526` | `640.186` | `576.722` |

### Candidate ordering detail
All three runs produce the same index pattern by station and source type:
1. start anchor at 0 mm (or `-0` in the baseline XML lexical form)
2. door left jamb at 1450 mm
3. door right jamb at 2350 mm
4. finish anchor at 5500 mm

No run shows any extra candidate, missing jamb, duplicate collapse, or local swap in the final ordered candidate list.

---

## B. Probe verdicts against T3.3 criteria and T3.2 prediction

### Prediction preserved from T3.2
`T3.1-C06` pre-EV-2 prediction in `T3.2_result.md`: supported, with the explicit unresolved probe that flip/mirror state must not alter the physical station values of the jamb references; falsifier = any orientation-correlated missing jamb, swapped station order beyond full reversal, or wrong segment value.

### B1. FLIP sub-case verdict

#### Controlled-comparison status
This is the only controlled comparison supplied.
- Same wall id as baseline: `387179`
- Same shell layer as baseline: `Exterior`
- Same door instance id as baseline: `387482`
- Same family/type: `M_Single-Flush / 900x2000`
- Same side pick: `Left`
- Same axis length: `5500` mm
- Same candidate stations: `0, 1450, 2350, 5500`

This satisfies T3.3’s required geometry controls as far as the XML can verify them.

#### Did station ownership change?
No.

Confirming signal:
- Candidate 2 remains the left jamb at `1450` mm with `referenceStrategy="HostWallOpeningGeometry"` and stable-reference face token `60`.
- Candidate 3 remains the right jamb at `2350` mm with `referenceStrategy="HostWallOpeningGeometry"` and stable-reference face token `52`.
- `referenceOwnersMatched="true"` in both baseline and flip.
- No jamb station drift, local jamb swap, or owner mismatch appears anywhere in the audit.

#### Did output order change?
No.

Confirming signal:
- `referenceOrderRelation="Exact"` in both runs.
- Candidate index order remains anchor → left jamb → right jamb → anchor.
- Created references match expected candidate indices 1/2/3/4 in both runs.

#### Did segment values change?
No.

Confirming signal:
- Ordered segments remain `1450 / 900 / 3150` mm.
- `segmentValuesMatched="true"` in both runs.
- Operator screenshot values for baseline and flip match the XML exactly.

#### Flip verdict
Observed result matches the T3.2 supported prediction and confirms the T3.3 hypothesis for the controlled flip pair: no station-ownership change, no output-order change, and no segment-value change.

### B2. MIRROR sub-case verdict

#### Controlled-comparison status
This is not a controlled orientation-toggle comparison under T3.3.
- Wall id changed from `387179` to `387562`.
- Shell layer changed from `Exterior` to `Interior`.
- Door instance id changed from `387482` to `387563`.
- AxisDirection and SideNormal changed as well.

So this run cannot by itself isolate mirror state from changed host/wall-side fixture.

#### Did station ownership change?
No regression is visible in the supplied output, but the verdict strength is downgraded because the fixture changed.

Observed signal:
- The mirror run still shows candidate 2 at `1450` mm and candidate 3 at `2350` mm.
- `referenceOwnersMatched="true"` and `referenceIdentityMatched="true"`.
- No missing jamb, duplicate collapse, local swap, or displaced station appears.

Limitation:
- Because the wall and door instance changed, this does not prove that mirroring alone preserved station ownership; it only proves that this different mirrored-looking fixture also produced a numerically correct 4-reference chain.

#### Did output order change?
No regression is visible, but again not at full probe strength.

Observed signal:
- `referenceOrderRelation="Exact"`.
- Candidate order remains anchor → left jamb → right jamb → anchor.

Limitation:
- Since the comparison is not controlled, this is compatibility evidence, not a clean orientation-toggle proof.

#### Did segment values change?
No.

Observed signal:
- Ordered segments remain `1450 / 900 / 3150` mm.
- `segmentValuesMatched="true"`.
- Operator screenshot values match the XML.

#### Mirror verdict
The mirror run does not show a fresh regression, but it does not satisfy T3.3’s control condition for a full-strength mirror-only conclusion. The strongest honest reading is: no mirror-correlated failure is observed in this supplied run, but the evidence is fixture-confounded and therefore only supports a qualified non-regression verdict, not a clean mirror-toggle closure.

---

## C. Critical fixture-control check

T3.3 requires the operator to keep wall, family/type, width, insertion station, and shell pick fixed, changing only orientation flag(s).

### Flip run vs baseline
- Same wall id? Yes — `387179` in both.
- Same shell layer? Yes — `Exterior` in both.
- Same door instance id? Yes — `387482` in both.
- Same family/type? Yes — `M_Single-Flush / 900x2000`.
- Same station set? Yes — `0, 1450, 2350, 5500`.

Verdict: yes, the flip run is a controlled orientation-toggle comparison to the degree the XML can verify.

### Mirror run vs baseline
- Same wall id? No — baseline `387179`, mirror `387562`.
- Same shell layer? No — baseline `Exterior`, mirror `Interior`.
- Same door instance id? No — baseline `387482`, mirror `387563`.
- Same family/type? Yes — `M_Single-Flush / 900x2000`.
- Same station set? Yes numerically, but on a different wall/instance fixture.

Verdict: no, the mirror run is not a controlled orientation toggle.

### Exact confounds in the mirror run
1. Different host wall (`387562` instead of `387179`).
2. Different shell pick (`Interior` instead of `Exterior`).
3. Different hosted door instance (`387563` instead of `387482`).
4. Different wall axis/sign convention in the XML geometry (`AxisDirection` and `SideNormal` differ).

Therefore the mirror-run verdict strength must be downgraded. It can support “no observed regression on this alternate mirrored-looking fixture,” but it cannot by itself close the narrow T3.3 mirror-only causality question at full strength.

---

## D. Stable-reference token differences

### Baseline vs flip
No candidate-level stable-reference face-index token differences:
- Candidate 1: `20` vs `20`
- Candidate 2: `60` vs `60`
- Candidate 3: `52` vs `52`
- Candidate 4: `12` vs `12`

What this proves:
- In the controlled flip pair, the exact stable reference identities stayed the same for all four created references.
- This strongly supports the conclusion that the observed flip state did not change candidate identity, owner mapping, or ordering for this fixture.

### Baseline vs mirror
The common token pattern changes only at candidate 3:
- Candidate 1: `20` vs `20`
- Candidate 2: `60` vs `60`
- Candidate 3: `52` vs `51`
- Candidate 4: `12` vs `12`

Important interpretation:
- This token difference is exact and real: the mirror run’s right-jamb stable reference token is `51`, not `52`.
- But because the wall id and door instance id both changed, this does not prove a mirror-caused reference remap. Stable references are owner-specific; a different host wall/instance can legitimately yield different token numbering while still projecting the same physical station and producing the same chain values.
- So the `52` → `51` change is evidence of a different underlying reference identity on the different fixture, not evidence of a BUG-11 regression by itself.

---

## E. Instrumentation-gap reconfirmation

Reconfirmed across all three XMLs:
- No field records `mirrored`.
- No field records `handFlipped`.
- No field records `facingFlipped`.
- No field records a generic orientation-state property for the door instance.

So the flip-vs-mirror distinction is not machine-verifiable from current XML instrumentation.
It remains operator-asserted via screenshots/statement.

This limits closure strength in two separate ways:
1. The controlled flip case can prove “the two supplied runs behaved identically while the operator says the swing/flip state changed,” but the XML itself does not prove which orientation flag changed.
2. The mirror case cannot be closed strongly because the XML both lacks orientation-state fields and shows that the fixture changed (different wall, shell, and door instance).

---

## F. BUG-11 status

Keep BUG-11 closed.

Reason:
- No supplied run shows a fresh concrete regression.
- The controlled flip pair is fully clean: same station ownership, same candidate order, same segment values, all audit gates true.
- The mirror run is not controlled, but it also does not show any failure signature: no missing jamb, no local swap, no `Mismatch`, no segment drift, no owner mismatch.

The remaining issue is evidence strength for mirror-only closure, not a demonstrated functional regression.

---

## G. Mid-run joint case status

### Prediction preserved from T3.2
`T3.1-C07` pre-EV-2 prediction in `T3.2_result.md`: supported, with at least one non-opening, non-end-anchor mid-run station included in the final candidate list; falsifier = missing the mid-run joint station entirely, or creating a chain that includes openings and anchors but silently omits the T-junction evidence.

### Observed outcome in `R8_C07`
The supplied XML proves that the wall mid-run aggregator executed, but it does not prove a supported mid-run output.

Observed signal:
- `WallMidRunAggregation supported="true" ... candidateCount="24" ... message="Mid-run aggregator ran."`
- The selected wall is `387562`, shell `Interior`, side `Left`, axis length `5500` mm.
- All 24 scanned candidate walls ended with `acceptedMidRunStationCount="0"` and rejection text `accepted distinct station count 0 < 2`.
- The nearest-looking candidate in the excerpt is wall `387655` with `fallbackStationMm="3475.517"` and `fallbackDistanceToSideLineMm="100"`, but it still contributes zero accepted mid-run stations.
- `FinalCandidates count="4"` contains only:
  1. start anchor at `0`
  2. door left jamb at `1450`
  3. door right jamb at `2350`
  4. finish anchor at `5500`
- No non-opening, non-end-anchor mid-run station survives into `FinalCandidates`.
- `ChainCreationAudit` still succeeds exactly (`dimensionId="387683"`, `referenceOrderRelation="Exact"`, `referenceIdentityMatched="true"`, `referenceOwnersMatched="true"`, `segmentValuesMatched="true"`) with ordered segments `1450 / 900 / 3150`.

### Mid-run verdict
`R8_C07` falsifies the T3.2 supported prediction for `T3.1-C07`.

Reason:
- The command succeeds only as a normal anchor-plus-door chain.
- The mid-run aggregator runs, but no candidate wall contributes an accepted mid-run station.
- Therefore this evidence does not show supported mid-run T-junction collection; it shows omission of the T-junction contribution in this scenario.

### Exact confirming/falsifying signal
The decisive falsifier is the combination of:
- aggregator executed (`supported="true"`, `candidateCount="24"`), yet
- every scanned candidate reports `acceptedMidRunStationCount="0"`, and
- the final ordered candidate list contains only anchors plus door jambs, with no mid-run wall station.

This is not a vague residual-risk note. It is a concrete negative verdict on the supplied case: mid-run T-junction support is NOT demonstrated here, and the supplied `R8_C07` run behaves as a silent omission of the expected T-junction station.

---

## Final decision

Final decision: PASS, with a qualified mirror conclusion and a negative mid-run verdict.

Reason:
- The supplied evidence is enough to answer the flip question explicitly and to show no fresh regression in the supplied mirror run.
- The flip sub-case confirms T3.2/T3.3 expectations at full strength because it is controlled.
- The mirror sub-case does not meet T3.3’s fixture-control rule, so the honest verdict is qualified: no observed regression, but not full-strength mirror-only proof.
- `R8_C07` now supplies the missing mid-run evidence and it is a concrete falsifier for the predicted supported behavior: the mid-run aggregator runs, but no mid-run station survives into the final chain.
- BUG-11 remains closed.

## Downstream handoff for T3.8

Use the following closure language:
- Flip: confirmed orientation-neutral on the supplied controlled pair; no station-ownership change, no output-order change, no segment-value change.
- Mirror: no regression observed, but the run is confounded by changed wall/shell/door instance, so mirror-only residual risk is reduced but not fully eliminated by this evidence.
- Mid-run: not supported on the supplied `R8_C07` evidence. The aggregator executed, but every scanned candidate produced zero accepted mid-run stations and the final chain remained anchor + door jambs only (`0 / 1450 / 2350 / 5500`, dimension `387683`).

If T3.8 needs a binary residual-risk statement, the strongest accurate form is:
- residual risk from flip = cleared by controlled evidence;
- residual risk from mirror = not reproduced, but not closed at full causal strength because the operator did not hold the fixture constant and the XML lacks machine-readable orientation flags;
- residual risk from mid-run T-junction support = realized as a concrete negative case in `R8_C07`, not merely theoretical.
