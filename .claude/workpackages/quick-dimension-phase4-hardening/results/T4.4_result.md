# T4.4 Result — EV-3 Grid safe-failure evidence judgment
Date: 2026-08-08
Task: T4.4 — Judge the Grid safe-failure evidence variant by variant.

## Inputs read
1. `.claude/workpackages/quick-dimension-phase4-hardening/01_SHARED_CONTRACT.md`
2. `.claude/workpackages/quick-dimension-phase4-hardening/tasks/T4.4_ev3_grid_evidence_judgment.md`
3. `.claude/workpackages/quick-dimension-phase4-hardening/results/T4.2_result.md`
4. EV-3 evidence excerpt supplied by the master only:
   - Operator says the four XML files correspond in order to V1, V2, V3, V4.
   - Common across all four runs: selectedWallId=387562, wall type Generic - 200mm, activeViewName=Level 1, sideLabel=Left, axisLengthMm=5500.
   - Common across all four runs: FinalCandidates count=4 with ordered stations 0 / 1450 / 2350 / 5500.
   - Common across all four runs: diagnostic present exactly as `Grid collection is disabled by Quick Dimension options.`
   - Common across all four runs: ChainCreationAudit attempted=true, succeeded=true, referenceOrderRelation=Exact, referenceIdentityMatched=true, referenceOwnersMatched=true, segmentValuesMatched=true.
   - Common across all four runs: segment values 1450 / 900 / 3150.
   - V1: WallMidRunAggregation candidateCount=1.
   - V2: WallMidRunAggregation candidateCount=24; scanned candidates all ended with referenceHitCount=0 and acceptedMidRunStationCount=0.
   - V3: WallMidRunAggregation candidateCount=1.
   - V4: WallMidRunAggregation candidateCount=1.

No build, no runtime, no Revit/MCP call, no reads outside the allowed inputs.

---

## 1. Prediction baseline used for judgment

Per `T4.2_result.md`, every Grid variant (V1..V4) was predicted to be acceptable only if it showed all of the following:
- the exact Grid disabled diagnostic: `Grid collection is disabled by Quick Dimension options.`
- no grid-attributable dimension behavior,
- no crash,
- unchanged wall/opening/mid-run chain relative to a no-grid-equivalent wall-axis run.

Per the shared contract acceptance vocabulary:
- **Unsupported-by-design** = no grid support, but honest behavior with no crash and no silently wrong value.
- **Defect** = wrong value, silent drop, crash, misleading diagnostic, or `Mismatch` order relation.

Because the EV-3 runs all created the same clean 4-station chain with exact audit pass and the Grid diagnostic present, the judgment question is whether any variant shows accidental Grid participation, misleading success, or corrupted chain. The supplied excerpt does not show any such contradiction.

---

## 2. Variant-by-variant judgment

### GRID-V1 — straight, normal view (control)
**Observed**
- Grid disabled diagnostic present with the exact predicted literal text.
- FinalCandidates count=4 with stations 0 / 1450 / 2350 / 5500.
- ChainCreationAudit succeeded with `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.
- Segment values = 1450 / 900 / 3150.
- `WallMidRunAggregation candidateCount=1`.

**Judgment**
- **Unsupported-by-design**.

**Why**
- The run explicitly announces Grid exclusion using the expected diagnostic.
- The produced chain is internally consistent and audit-clean.
- Nothing in the supplied evidence suggests Grid references entered the created dimension or corrupted the result.
- No crash, misleading success, or silent wrong behavior is visible.

### GRID-V2 — straight, cropped view
**Observed**
- Grid disabled diagnostic present with the exact predicted literal text.
- FinalCandidates count=4 with stations 0 / 1450 / 2350 / 5500.
- ChainCreationAudit succeeded with `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.
- Segment values = 1450 / 900 / 3150.
- `WallMidRunAggregation candidateCount=24`, but all scanned candidates ended with `referenceHitCount=0` and `acceptedMidRunStationCount=0`.

**Judgment**
- **Unsupported-by-design**.

**Why**
- The run still announces Grid exclusion with the exact expected diagnostic.
- Despite higher wall mid-run scan volume, the accepted result is unchanged from the control: same final stations, same exact audit pass, same segment values.
- The excerpt shows no accidental dimension creation from Grid, no wrong values, and no crash.
- The elevated scan count is a reportable behavior difference in wall-side scanning, but not a defect here because it ends in zero accepted mid-run stations and leaves the final chain unchanged.

### GRID-V3 — straight, hidden view
**Observed**
- Grid disabled diagnostic present with the exact predicted literal text.
- FinalCandidates count=4 with stations 0 / 1450 / 2350 / 5500.
- ChainCreationAudit succeeded with `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.
- Segment values = 1450 / 900 / 3150.
- `WallMidRunAggregation candidateCount=1`.

**Judgment**
- **Unsupported-by-design**.

**Why**
- The run preserves the exact same disabled diagnostic and the exact same clean chain/audit result as the control.
- No evidence shows hidden-grid handling caused a crash, suppressed the diagnostic, or altered the created dimension.
- This is honest no-support behavior, not silent wrong behavior.

### GRID-V4 — arc, normal view
**Observed**
- Grid disabled diagnostic present with the exact predicted literal text.
- FinalCandidates count=4 with stations 0 / 1450 / 2350 / 5500.
- ChainCreationAudit succeeded with `referenceOrderRelation=Exact`, `referenceIdentityMatched=true`, `referenceOwnersMatched=true`, `segmentValuesMatched=true`.
- Segment values = 1450 / 900 / 3150.
- `WallMidRunAggregation candidateCount=1`.

**Judgment**
- **Unsupported-by-design**.

**Why**
- The run announces Grid exclusion exactly as predicted.
- The created chain is identical to the control and audit-clean.
- No evidence shows arc-grid presence caused accidental support, misleading success, wrong values, or crash.

---

## 3. Cross-variant conclusion

All four supplied variants are judged **Unsupported-by-design**, not Defect.

What is positively established from the excerpt:
- Every run visibly reports the exact Grid disabled diagnostic.
- Every run ends with the same final 4-station candidate set and the same segment values.
- Every run has a successful chain-creation audit with exact order and identity/owner/value matching.
- No run shows a crash, a `Mismatch` order relation, or any visible accidental Grid-derived dimension outcome.

What did **not** happen in the supplied evidence:
- No accidental dimension creation attributable to Grid.
- No misleading success state where Grid appears supported.
- No silently wrong numeric result.
- No crash.

The only variant-specific deviation in the excerpt is V2's higher `WallMidRunAggregation candidateCount=24`, but because every scanned candidate ended with `referenceHitCount=0` and `acceptedMidRunStationCount=0`, and because the final chain/audit remain identical to the other variants, this does not cross the shared-contract threshold for Defect.

---

## 4. Decision and handoff

Decision: PASS.

The task objective is met because each supplied variant received an explicit judgment, the result distinguishes honest no-support behavior from silent wrong behavior, and it does not invent a defect from missing evidence. The EV-3 excerpt supports a clean safe-failure verdict across V1..V4.

Handoff to `T4.5`: use the per-variant verdict table below directly.

| Variant | Verdict | NO_GO trigger seen? | Notes |
|---|---|---:|---|
| V1 straight normal | Unsupported-by-design | No | Control-safe failure; exact diagnostic; exact audit pass |
| V2 straight cropped | Unsupported-by-design | No | Higher wall mid-run scan count, but zero accepted stations and unchanged final chain |
| V3 straight hidden | Unsupported-by-design | No | Hidden-state variant still yields exact diagnostic and unchanged clean chain |
| V4 arc normal | Unsupported-by-design | No | Arc-grid presence does not alter result and does not crash |

Package implication: EV-3 evidence, as supplied, supports Session 4.3 PASS rather than NO_GO.
