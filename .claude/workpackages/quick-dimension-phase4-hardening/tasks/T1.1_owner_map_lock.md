# T1.1 — Owner map and no-touch lock

## Read first
- `../01_SHARED_CONTRACT.md` — sections 2, 4, 5, 8
- `../03_TASK_MANIFEST.md` — Phase 1 row for `T1.1`
- `../05_RESULT_SCHEMA.md`

## Dependencies
- None

## Write scope
- `results/T1.1_result.md`

## Objective
Confirm that the shared contract's source ownership map, line ranges, and no-touch list are internally coherent and sufficient for downstream Phase 4 work.

## Do
1. Re-state the verified production owners for:
   - `QuickDimensionCreateChainSmokeCommand.Execute`
   - `QuickDimensionReadOnlyEngine.CollectCandidates`
   - `QuickDimensionReadOnlyEngine.CollectWallAxisCandidates`
   - `QuickDimensionDoorWindowCandidateCollector.TryProjectOpeningOntoWallAxis`
   - `QuickDimensionDoorWindowCandidateCollector.TryResolveReferenceOwnedPoint`
   - `QuickDimensionWallCandidateCollector.TryCollectWallEndAnchors`
   - `QuickDimensionWallAxisAggregatorService.CollectMidRunCandidates`
   - `QuickDimensionReadOnlyXmlLogService.GetReferenceOrderRelation`
   - `QuickDimensionLineContext.CreateFromWallAxis`
   - `QuickDimensionCandidate`
2. Verify the line ranges in the shared contract are non-overlapping and credible for those owners.
3. Re-state the no-touch list in compact form and call out any item that is critical to mission safety.
4. Confirm whether any other source file must be added to the no-touch set for this package. Add none unless you can justify it from the contract alone.

## PASS only if
- The result states the owner map is usable as-is or names the exact contradiction.
- It explicitly confirms `QuickDimensionChainCreationService.cs` and `GetReferenceOrderRelation` are protected.
- It does not request runtime evidence.

## BLOCKED if
- The shared contract lacks enough owner information to route later write scopes safely.

## Downstream needs
- `T1.2` needs a compact, trustworthy baseline of what code is in scope and what is untouchable.
