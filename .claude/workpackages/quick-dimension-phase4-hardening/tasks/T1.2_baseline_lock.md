# T1.2 — Baseline lock

## Read first
- `../01_SHARED_CONTRACT.md` — sections 1, 2, 3, 5, 6, 8
- `../03_TASK_MANIFEST.md` — Phase 1 row for `T1.2`
- `../05_RESULT_SCHEMA.md`

## Dependencies
- `T1.1`

## Write scope
- `results/T1.2_result.md`

## Objective
Lock the mission baseline so later workers cannot silently reopen closed defects, widen scope, or pull rollback work back into this package.

## Do
1. Summarize the already-closed baseline in 6–10 lines:
   - BUG-10 closed
   - BUG-11 closed
   - EV-2 six-run matrix passed
   - EV-3 reopen persistence passed
   - forced rollback deferred by design
   - Grid disabled in the wall-axis main flow
2. State which of those facts are implementation boundaries versus evidence boundaries.
3. Confirm that this package starts from a working production feature, not from an active bugfix branch.
4. Name any baseline fact that downstream tasks must not reinterpret.

## PASS only if
- BUG-10 and BUG-11 are explicitly marked closed, not "under review".
- Rollback deferral is treated as a separate task boundary, not an open blocker inside this package.
- Grid exclusion is stated as a present-source fact, not a future option.

## BLOCKED if
- The closed-state baseline cannot be established from the shared contract.

## Downstream needs
- `T1.3` needs the locked baseline to resolve the Grid scope contradiction without re-opening support questions.
