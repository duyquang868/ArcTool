# T1.5 — Build path lock

## Read first
- `../01_SHARED_CONTRACT.md` — section 7
- `../03_TASK_MANIFEST.md` — Phase 1 row for `T1.5`
- `../05_RESULT_SCHEMA.md`

## Dependencies
- `T1.4`

## Write scope
- `results/T1.5_result.md`

## Objective
Verify the approved build path and the known-benign build noise policy so Phase 5/6 write tasks do not drift into the wrong build tool or chase irrelevant warnings.

## Do
1. Re-state the exact approved Visual Studio MSBuild command.
2. Re-state why `dotnet build` is not approved here.
3. List the known-benign build noise already acknowledged by the contract.
4. State what counts as a real build failure for this package.

## PASS only if
- The result includes the exact MSBuild command string.
- It explicitly rejects `dotnet build` for this repo.
- It distinguishes warnings from errors cleanly.

## BLOCKED if
- The build command or noise policy is ambiguous.

## Downstream needs
- `T5.3`, `T5.9`, and `T6.6` rely on this build policy without re-litigating it.
