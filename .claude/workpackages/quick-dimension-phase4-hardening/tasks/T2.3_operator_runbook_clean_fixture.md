# T2.3 — Operator runbook for EV-1 clean fixture

## Read first
- `../01_SHARED_CONTRACT.md` — sections 1, 2, 6, 8
- `../03_TASK_MANIFEST.md` — Phase 2 row for `T2.3`
- `../04_EVIDENCE_QUEUE.md`
- `../05_RESULT_SCHEMA.md`
- `../results/T2.1_result.md`
- `../results/T2.2_result.md`

## Dependencies
- `T2.2`

## Write scope
- `../04_EVIDENCE_QUEUE.md`
- `results/T2.3_result.md`

## Objective
Write the exact operator steps for EV-1 so the clean fixture can be built and run once with the minimum evidence needed for oracle comparison.

## Do
1. Turn `T2.1` + `T2.2` into concise operator instructions:
   - fixture build steps,
   - wall id naming convention or temporary labels if needed,
   - shell(s) to run,
   - command to run: `QuickDimensionCreateChainSmokeCommand`.
2. State exactly what the operator must return:
   - combined XML path,
   - created dimension id or explicit no-dimension outcome,
   - annotated screenshot of visible segment values,
   - visible dialog/cancel outcome,
   - optional journal excerpt.
3. Update `04_EVIDENCE_QUEUE.md` only if needed to make EV-1 wording match the runbook precisely.
4. Keep the runbook execution-safe: no Revit MCP, no code changes, no extra scenarios.

## PASS only if
- A human can execute EV-1 from the runbook without asking clarifying questions.
- The requested evidence is exactly what `T2.4` needs and nothing more.
- EV-1 remains a single clean-fixture acceptance check, not a matrix.

## BLOCKED if
- The upstream fixture or oracle leaves the operator with unresolved choices.

## Downstream needs
- `T2.4` needs the final requested evidence list and the exact shell/run order.
