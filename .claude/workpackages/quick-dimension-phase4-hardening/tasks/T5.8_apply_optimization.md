# T5.8 — Apply the single allowed optimization

## Read first
- `../01_SHARED_CONTRACT.md` — sections 2, 4, 5
- `../03_TASK_MANIFEST.md` — Phase 5 row for `T5.8` and the lock summary
- `../05_RESULT_SCHEMA.md`
- `../results/T5.7_result.md`
- Only the one collector file named by `T5.7`

## Dependencies
- `T5.7`

## Write scope
- Exactly one of:
  - `ArcTool.Core/Services/QuickDimensionDoorWindowCandidateCollector.cs`
  - `ArcTool.Core/Services/QuickDimensionWallAxisAggregatorService.cs`
  - `ArcTool.Core/Services/QuickDimensionWallCandidateCollector.cs`
- `results/T5.8_result.md`

## Objective
Apply the approved optimization to exactly one collector file, or record an explicit no-change outcome.

## Do

### If `T5.7` designed a patch
1. Apply only that patch.
2. Preserve the BUG-10 and BUG-11 fix shapes exactly.
3. Preserve station derivation, reference ownership, candidate ordering, and diagnostics.
4. Read back your own diff and confirm no observable behavior changed.
5. Record the touched methods and the neutrality argument.

### If `T5.7` rejected optimization
1. Change no source file.
2. Record `files_changed: none` and state that Phase 5 proceeds on measured baseline only.

## PASS only if
- At most one collector file was modified.
- No protected fix shape was altered.
- The result states clearly whether source changed.

## BLOCKED if
- The patch cannot be applied without touching a second file or protected logic.

## Downstream needs
- `T5.9` needs to know whether a build is required or whether no-change should be recorded.
