# T6.5 — Apply instrumentation disposition

## Read first
- `../01_SHARED_CONTRACT.md` — sections 2, 4, 5
- `../03_TASK_MANIFEST.md` — Phase 6 row for `T6.5` and the lock summary
- `../05_RESULT_SCHEMA.md`
- `../results/T6.4_result.md`
- `ArcTool.Core/Services/QuickDimensionReadOnlyEngine.cs`
- `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`

## Dependencies
- `T6.4`

## Write scope
- `ArcTool.Core/Services/QuickDimensionReadOnlyEngine.cs`
- `ArcTool.Core/Services/QuickDimensionReadOnlyXmlLogService.cs`
- `results/T6.5_result.md`

## Objective
Apply the final `KEEP` or `REMOVE` decision for the Phase 5 timing instrumentation.

## Do

### If `T6.4` = `REMOVE`
1. Strip the temporary timing instrumentation cleanly from the two allowed files only.
2. Remove only code that exists solely to support the timing output.
3. Leave every candidate, station, diagnostic, and audit path unchanged.

### If `T6.4` = `KEEP`
1. Keep the instrumentation in place.
2. Apply only minimal cleanup needed to make the final code coherent, if any.
3. If no cleanup is needed, change no source file and record that explicitly.

### In both cases
1. Do not touch any collector file.
2. Do not touch `QuickDimensionChainCreationService.cs`.
3. Verify from your own diff that no user-visible Quick Dimension behavior changed.
4. Record the touched methods and the neutrality argument.

## PASS only if
- Only the two allowed files were considered for source edits.
- A `REMOVE` path fully strips temporary timing code.
- A `KEEP` path does not silently broaden instrumentation scope.

## BLOCKED if
- The disposition would require changes outside the allowed write scope.

## Downstream needs
- `T6.6` needs to know whether a rebuild is required and which files changed.
