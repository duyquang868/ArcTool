# T1.1 — Confirm active QD footprint and boundaries

- status: PASS
- date: 2026-08-10
- inputs_read: `.claude/workpackages/retire-quick-dimension/01_SHARED_CONTRACT.md`; `.claude/workpackages/retire-quick-dimension/03_TASK_MANIFEST.md`; `.claude/workpackages/retire-quick-dimension/05_RESULT_SCHEMA.md`; `ArcTool.Core/App.cs` (`App.OnStartup` excerpt, lines 24-273); `ArcTool.Core/Commands/ArrangeDimensionCommand.cs` (active neighbor confirmation)
- write_scope_touched: `.claude/workpackages/retire-quick-dimension/results/T1.1_result.md`

## Findings

1. Active-source Quick Dimension product surface in `App.OnStartup` is confined to seven ribbon button registrations inside the `Annotation Tools` panel.
   - `ArcTool.Core/App.cs:113-121` registers `btnQuickDimensionGridReferenceSpike` -> `ArcTool.Core.Commands.QuickDimensionGridReferenceSpikeCommand`.
   - `ArcTool.Core/App.cs:123-131` registers `btnQuickDimensionWallReferenceSpike` -> `ArcTool.Core.Commands.QuickDimensionWallReferenceSpikeCommand`.
   - `ArcTool.Core/App.cs:133-141` registers `btnQuickDimensionMixedReferenceSpike` -> `ArcTool.Core.Commands.QuickDimensionMixedReferenceSpikeCommand`.
   - `ArcTool.Core/App.cs:143-151` registers `btnQuickDimensionDoorWindowReferenceSpike` -> `ArcTool.Core.Commands.QuickDimensionDoorWindowReferenceSpikeCommand`.
   - `ArcTool.Core/App.cs:153-161` registers `btnQuickDimensionFullMixedReferenceSpike` -> `ArcTool.Core.Commands.QuickDimensionFullMixedReferenceSpikeCommand`.
   - `ArcTool.Core/App.cs:163-171` registers `btnQuickDimensionReadOnlySummary` -> `ArcTool.Core.Commands.QuickDimensionReadOnlySummaryCommand`.
   - `ArcTool.Core/App.cs:173-181` registers `btnQuickDimensionCreateChainSmoke` -> `ArcTool.Core.Commands.QuickDimensionCreateChainSmokeCommand`.

2. No extra QD startup hooks were found in the allowed `App.OnStartup` excerpt.
   - Outside the seven push buttons above, the method continues with active Excel and Coordinate ribbon setup at `ArcTool.Core/App.cs:183-266` and document event wiring at `ArcTool.Core/App.cs:268-270`.
   - No QD-specific event subscriptions, startup services, or panel creation logic exist outside the Annotation Tools button additions in the inspected scope.

3. Archive candidates confirmed by authoritative ownership map in the shared contract.
   - Commands to retire/archive: the seven `ArcTool.Core/Commands/QuickDimension*.cs` files named in `01_SHARED_CONTRACT.md:52-62`.
   - Models to retire/archive: eight `ArcTool.Core/Models/QuickDimension*.cs` files named in `01_SHARED_CONTRACT.md:63-74`.
   - Services to retire/archive: fourteen `ArcTool.Core/Services/QuickDimension*.cs` files named in `01_SHARED_CONTRACT.md:75-93`.
   - Total active-source QD file family confirmed for downstream retirement planning: 29 source files across Commands/Models/Services, plus the live registration footprint in `App.cs`.

4. No-touch active neighbor boundary confirmed.
   - `ArcTool.Core/App.cs:95-111` registers `ArrangeDimensionCommand` immediately before the QD block and is active non-QD surface that must remain untouched.
   - `ArcTool.Core/Commands/ArrangeDimensionCommand.cs:11-80` is a standalone active annotation command with no visible Quick Dimension dependency in the inspected excerpt.
   - `ArcTool.Core/App.cs:183-266` registers Excel-to-Revit and Coordinate tools after the QD block; these are active neighbors and should remain intact.

5. Surprise blocker check within allowed scope: none found.
   - In the inspected startup code, every live QD dependency is string-based command registration only; no additional variables, shared helpers, or startup-time service calls appear to couple QD to Arrange Dimension / Excel / Coordinate features.
   - This means retirement at the product surface should be cleanly localized to the seven `PushButtonData` blocks in `App.OnStartup`, with downstream source-move/project cleanup handling compile-time fallout.

## Decision

The active Quick Dimension footprint is exactly seven ribbon button registrations in `ArcTool.Core/App.cs` under `App.OnStartup`; active adjacent features are Arrange Dimension before the QD block and Excel/Coordinate tooling after it, with no startup-time non-QD dependency surprise detected in the allowed scope.

## Open questions

- None within this task scope.

## Handoff for downstream tasks

- `T1.2` and `T2.2` can treat `ArcTool.Core/App.cs:113-181` as the exact QD live-UI removal slice.
- Preserve `ArcTool.Core/App.cs:95-111` (Arrange Dimension) and `ArcTool.Core/App.cs:183-266` (Excel/Coordinate) as no-touch active neighbors.
- Downstream archive/design tasks should use the contract ownership map as the authoritative QD source inventory: 7 command files, 8 model files, 14 service files.