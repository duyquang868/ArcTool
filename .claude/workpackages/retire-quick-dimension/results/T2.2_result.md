# T2.2 — Remove live Quick Dimension ribbon registrations

- status: PASS
- date: 2026-08-10
- inputs_read: `.claude/workpackages/retire-quick-dimension/01_SHARED_CONTRACT.md`; `.claude/workpackages/retire-quick-dimension/results/T2.1_result.md`; `ArcTool.Core/App.cs`
- write_scope_touched: `ArcTool.Core/App.cs`; `.claude/workpackages/retire-quick-dimension/results/T2.2_result.md`

## Findings

1. Removed all seven live Quick Dimension `PushButtonData` registration blocks from `App.OnStartup` in `ArcTool.Core/App.cs`.
   - Removed ribbon entries:
     - `btnQuickDimensionGridReferenceSpike`
     - `btnQuickDimensionWallReferenceSpike`
     - `btnQuickDimensionMixedReferenceSpike`
     - `btnQuickDimensionDoorWindowReferenceSpike`
     - `btnQuickDimensionFullMixedReferenceSpike`
     - `btnQuickDimensionReadOnlySummary`
     - `btnQuickDimensionCreateChainSmoke`

2. Preserved non-QD ribbon surface exactly as requested.
   - `Void Manager` split button unchanged.
   - `Arrange Dimensions` button unchanged.
   - `Excel to Revit` button unchanged.
   - All `Coordinate Tools` buttons unchanged.
   - Document event wiring unchanged.

3. No additional local-variable cleanup was required.
   - Every removed variable was scoped to its own removed QD block.
   - `annotationPanel` and surrounding startup variables remain used by the retained `Arrange Dimensions` button and later panel creation logic.

## Verification

- Static source verification only, per task instructions.
- `App.OnStartup` now transitions directly from `annotationPanel.AddItem(arrangeDimBtnData);` to the Excel Tools panel setup with no remaining QD button registrations.
- No Revit runtime, smoke, or build execution performed.

## Notes for downstream tasks

- T2.3/T2.4 can now archive QD command/model/service files without any live ribbon entry points remaining in `App.OnStartup`.
