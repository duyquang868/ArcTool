# T2.5 — csproj cleanup after Quick Dimension archive move

- status: PASS
- date: 2026-08-10
- inputs_read: `.claude/workpackages/retire-quick-dimension/01_SHARED_CONTRACT.md`; `.claude/workpackages/retire-quick-dimension/results/T2.1_result.md`; `.claude/workpackages/retire-quick-dimension/results/T2.4_result.md`; `ArcTool.Core/ArcTool.Core.csproj`; Revit API reference root `https://www.revitapidocs.com/2026/` lookup attempted but unavailable due tool auth failure; no Revit API surface was needed for this csproj-only task
- write_scope_touched: `ArcTool.Core/ArcTool.Core.csproj`; `.claude/workpackages/retire-quick-dimension/results/T2.5_result.md`

## Findings

1. `ArcTool.Core/ArcTool.Core.csproj` is SDK-style and has no pre-existing explicit Quick Dimension source entries.
   - No `<Compile Include=...>` entries for Quick Dimension files exist.
   - No `<Compile Remove=...>` entries existed before this task.
   - No explicit `<EmbeddedResource ...>` or other resource items reference Quick Dimension paths.

2. Applied the minimal build-surface cleanup required by T2.1/T2.4.
   - Added a dedicated `ItemGroup` with:
     - `<Compile Remove="Archive\QuickDimension\**\*.cs" />`
   - This excludes the archived retired subtree from compilation while preserving the files in-repo under `ArcTool.Core/Archive/QuickDimension/...`.

3. Scope remained intentionally narrow.
   - No package references, COM references, WPF/WinForms settings, resource settings, or non-QD project items were changed.
   - No source `.cs` files were touched.
   - No extra cleanup was needed because the csproj contained no explicit Quick Dimension file or resource references to remove.

## Expected verification surface for T2.6

Approved static/build verification should confirm exactly:

1. MSBuild no longer compiles files under `ArcTool.Core/Archive/QuickDimension/**` because of the new `Compile Remove` item.
2. The active build has no dangling references to retired Quick Dimension types after T2.2/T2.3/T2.4.
3. Any compile failure, if present, should be documented precisely as either:
   - unrelated to Quick Dimension retirement, or
   - a remaining active-source reference to retired QD types outside this task’s write scope.

Approved command from shared contract:

```bash
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" ArcTool.Core/ArcTool.Core.csproj -property:Configuration=Debug -property:Platform=x64 -verbosity:minimal -nologo
```

## Decision

T2.5 is complete. The archived Quick Dimension subtree is now preserved in-repo but excluded from the active SDK-style compilation surface, with no other csproj cleanup required.
