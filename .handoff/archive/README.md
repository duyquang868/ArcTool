# Handoff archive

Historical normal-session handoff snapshots live here as
`HANDOFF_<YYYY-MM-DD>_<short-slug>.md`.

Before overwriting `.handoff/HANDOFF_TO_NEXT_SESSION.md` with a new session's content, copy the
current version into this folder. History is never deleted — only moved.
